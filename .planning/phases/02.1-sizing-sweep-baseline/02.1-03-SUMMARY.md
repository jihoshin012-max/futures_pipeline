---
phase: 02.1-sizing-sweep-baseline
plan: 03
subsystem: rotational-sizing-sweep
tags: [rotational, martingale, sizing-sweep, profiles, profile-infrastructure]

# Dependency graph
requires:
  - phase: 02.1-sizing-sweep-baseline/02.1-02
    provides: sizing_sweep_P1a.tsv with 966 rows and 18 metric columns including extended metrics
provides:
  - profiles/profile_definitions.json: scoring criteria for MAX_PROFIT, SAFEST, MOST_CONSISTENT
  - profiles/max_profit.json: MAX_PROFIT baseline config per bar type (250vol PF=2.20, 250tick PF=1.84, 10sec PF=1.72)
  - profiles/safest.json: SAFEST baseline config per bar type (minimises worst_cycle_dd)
  - profiles/most_consistent.json: MOST_CONSISTENT baseline config per bar type (maximises calmar_ratio)
  - rotational_engine.py: --profile and --bar-type flags for downstream phase experiments
  - sizing_sweep_results/sizing_sweep_report.md: analysis report with top-3, pure reversal vs martingale, MTP=1 full table
affects: [all downstream TDS calibration phases, Phase 3 planning]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Profile JSON schema: {profile, description, source, bar_types, top3_candidates, notes}
    - profile_definitions.json is data-driven: new profiles addable without code changes
    - --profile flag in engine loads profiles/{name}.json, overrides step_dist/max_levels/max_total_position/max_contract_size=16
    - --score-profiles flag in run_sizing_sweep.py skips sweep and re-scores from existing TSV
    - score_profiles() uses _build_sort_args() to convert profile metric hierarchy into pd.DataFrame.sort_values args

key-files:
  created:
    - shared/archetypes/rotational/profiles/profile_definitions.json
    - shared/archetypes/rotational/profiles/max_profit.json
    - shared/archetypes/rotational/profiles/safest.json
    - shared/archetypes/rotational/profiles/most_consistent.json
    - shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md
  modified:
    - shared/archetypes/rotational/rotational_engine.py
    - shared/archetypes/rotational/run_sizing_sweep.py

key-decisions:
  - "MAX_PROFIT and MOST_CONSISTENT select the same config for 250vol (SD=7.0 ML=1 MTP=2 PF=2.20) and 10sec (SD=10.0 ML=1 MTP=4 PF=1.72) — calmar_ratio optimisation converges to same result as PF maximisation for these bar types"
  - "SAFEST 250vol selects SD=1.0 ML=4 MTP=16 PF=1.02 — a pure spread-across-many-adds config with lowest worst_cycle_dd=753t and 0% max_level_exposure_pct despite ML=4; high MTP=16 makes adds economical"
  - "Martingale beats pure reversal (MTP=1) on all 3 bar types for MAX_PROFIT: 250vol +0.70 PF units, 250tick +0.00 (same), 10sec +0.62 PF units"
  - "SAFEST profile has no PF < 1.0 flags — all three bar types found PF >= 1.0 configs as safest choices"

patterns-established:
  - "Profile JSON schema is stable: bar_types + top3_candidates + pf_lt_1_flag per config"
  - "--profile + --bar-type as the canonical way to parametrize downstream experiment runs"

requirements-completed: [ROT-SIZ-04, ROT-SIZ-05]

# Metrics
duration: 5min
completed: 2026-03-16
---

# Phase 02.1 Plan 03: Profile Infrastructure and Sizing Sweep Report Summary

**4-file profile infrastructure (MAX_PROFIT/SAFEST/MOST_CONSISTENT per 3 bar types) with data-driven scoring, --profile engine flag, and programmatic sweep analysis report**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-16T03:27:00Z
- **Completed:** 2026-03-16T03:31:41Z
- **Tasks:** 1/2 (Task 2 is checkpoint:human-verify — awaiting review)
- **Files modified:** 2, files created: 5

## Accomplishments

- Created `profiles/profile_definitions.json` with scoring criteria for 3 profiles — data-driven, no code changes needed for new profiles
- Added `score_profiles()` and `generate_sizing_sweep_report()` to `run_sizing_sweep.py` with `--score-profiles` flag; re-runnable without re-executing the sweep
- Generated 3 profile JSONs from TSV data: max_profit.json, safest.json, most_consistent.json — each with per-bar-type winning config + top3 candidates
- Added `--profile` and `--bar-type` flags to `rotational_engine.py`; loads profiles/{name}.json, overrides martingale params, verified: `--profile max_profit --bar-type bar_data_250vol_rot` produces PF=2.2037 as expected
- Generated `sizing_sweep_results/sizing_sweep_report.md` programmatically with all 5 sections: summary, top-3 per profile per bar type, pure reversal vs martingale, notable findings, MTP=1 full table

## Task Commits

1. **Task 1: Build profile scorer, create profile JSONs, add --profile to engine** — `35f19da` (feat)

## Files Created/Modified

- `shared/archetypes/rotational/profiles/profile_definitions.json` — Scoring criteria for MAX_PROFIT, SAFEST, MOST_CONSISTENT
- `shared/archetypes/rotational/profiles/max_profit.json` — MAX_PROFIT configs: 250vol SD=7.0/ML=1/MTP=2 PF=2.20, 250tick SD=4.5/ML=1/MTP=1 PF=1.84, 10sec SD=10.0/ML=1/MTP=4 PF=1.72
- `shared/archetypes/rotational/profiles/safest.json` — SAFEST configs: 250vol SD=1.0/ML=4/MTP=16 PF=1.02, 250tick SD=5.0/ML=1/MTP=1 PF=1.22, 10sec SD=10.0/ML=1/MTP=1 PF=1.10
- `shared/archetypes/rotational/profiles/most_consistent.json` — MOST_CONSISTENT configs: 250vol SD=7.0/ML=1/MTP=2 PF=2.20, 250tick SD=4.5/ML=1/MTP=2 PF=1.79, 10sec SD=10.0/ML=1/MTP=4 PF=1.72
- `shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md` — Full analysis report
- `shared/archetypes/rotational/rotational_engine.py` — Added load_profile_config(), apply_profile_overrides(), --profile, --bar-type args
- `shared/archetypes/rotational/run_sizing_sweep.py` — Added score_profiles(), generate_sizing_sweep_report(), --score-profiles flag

## Decisions Made

- MAX_PROFIT and MOST_CONSISTENT converge on the same configs for 250vol and 10sec bar types — calmar_ratio optimisation selects identically to PF maximisation when the best PF config also has the best risk-adjusted return
- SAFEST 250vol: SD=1.0, ML=4, MTP=16, PF=1.0209 — worst_cycle_dd=753t is the lowest in the entire sweep for 250vol; despite ML=4 the 0% max_level_exposure_pct indicates adds rarely fire at this tight step_dist
- All SAFEST configs have PF >= 1.0, so no PF < 1.0 flags appear in the safest profile
- Martingale vs pure reversal: all bar types show martingale wins on MAX_PROFIT metric; 250tick best is pure reversal (MTP=1) at PF=1.84

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- 4 profile JSONs in `profiles/` directory ready for downstream consumption
- `--profile` flag enables downstream phases to parametrize experiments against any of the 3 profiles per bar type
- Report in `sizing_sweep_results/sizing_sweep_report.md` ready for human review (Task 2 checkpoint)
- Key review question per plan: does pure reversal (MTP=1) outperform martingale for any bar type? Answer: 250tick best PF=1.84 is MTP=1; martingale never significantly beats MTP=1 for 250tick
- No blockers pending human approval at checkpoint

---
*Phase: 02.1-sizing-sweep-baseline*
*Completed: 2026-03-16 (Task 2 pending checkpoint approval)*
