---
phase: 02.1-sizing-sweep-baseline
plan: 03
type: execute
wave: 3
depends_on: ["02.1-02"]
files_modified:
  - shared/archetypes/rotational/profiles/profile_definitions.json
  - shared/archetypes/rotational/profiles/max_profit.json
  - shared/archetypes/rotational/profiles/safest.json
  - shared/archetypes/rotational/profiles/most_consistent.json
  - shared/archetypes/rotational/rotational_engine.py
  - shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md
autonomous: false
requirements: [ROT-SIZ-04, ROT-SIZ-05]

must_haves:
  truths:
    - "3 profile JSONs exist with per-bar-type winning configs"
    - "profile_definitions.json defines scoring criteria without code changes needed for new profiles"
    - "rotational_engine.py --profile flag loads profile config and overrides martingale params"
    - "PF < 1.0 configs are included with a flag, not excluded"
    - "MaxTotalPosition=1 pure reversal results reported separately per bar type"
    - "Top 3 candidates per profile per bar type documented in report"
  artifacts:
    - path: "shared/archetypes/rotational/profiles/profile_definitions.json"
      provides: "Profile scoring criteria"
      contains: "MAX_PROFIT"
    - path: "shared/archetypes/rotational/profiles/max_profit.json"
      provides: "MAX_PROFIT baseline config per bar type"
      contains: "bar_types"
    - path: "shared/archetypes/rotational/profiles/safest.json"
      provides: "SAFEST baseline config per bar type"
      contains: "bar_types"
    - path: "shared/archetypes/rotational/profiles/most_consistent.json"
      provides: "MOST_CONSISTENT baseline config per bar type"
      contains: "bar_types"
    - path: "shared/archetypes/rotational/rotational_engine.py"
      provides: "--profile flag and profile loader"
      contains: "--profile"
    - path: "shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md"
      provides: "Human-readable report with top candidates and pure reversal comparison"
  key_links:
    - from: "rotational_engine.py"
      to: "profiles/*.json"
      via: "--profile flag loads profile JSON"
      pattern: "profiles.*\\.json"
    - from: "profiles/*.json"
      to: "sizing_sweep_results/sizing_sweep_P1a.tsv"
      via: "Profile selection from sweep results"
      pattern: "step_dist.*max_levels.*max_total_position"
---

<objective>
Identify 3 baseline profiles from sweep results, create permanent profile infrastructure, and add --profile flag to the engine.

Purpose: Profiles are the permanent pipeline infrastructure that all downstream phases consume. Each profile (MAX_PROFIT, SAFEST, MOST_CONSISTENT) represents a different risk/reward operating point. The --profile flag enables downstream phases to run experiments against specific baseline configurations. The report provides human-readable analysis including the pure reversal vs martingale comparison.

Output: profiles/ directory with 4 JSON files, --profile flag in engine, sizing sweep report.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-sizing-sweep-baseline/02.1-CONTEXT.md
@.planning/phases/02.1-sizing-sweep-baseline/02.1-RESEARCH.md
@.planning/phases/02.1-sizing-sweep-baseline/02.1-02-SUMMARY.md

@shared/archetypes/rotational/rotational_engine.py
@shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_P1a.tsv

<interfaces>
<!-- From rotational_engine.py — current CLI interface -->
```python
parser.add_argument("--config", required=True, help="Path to rotational config JSON")
parser.add_argument("--output", required=True, help="Path to write result.json")
# main(config_path: str, output_path: str)
```

<!-- Profile JSON schema (from RESEARCH.md) -->
```json
{
  "profile": "MAX_PROFIT",
  "bar_types": {
    "bar_data_250vol_rot": {"step_dist": 6.0, "max_levels": 3, "max_total_position": 4, "cycle_pf": 0.85, ...},
    "bar_data_250tick_rot": {...},
    "bar_data_10sec_rot": {...}
  },
  "pf_lt_1_flags": {"bar_data_250vol_rot": true, ...},
  "notes": "..."
}
```

<!-- profile_definitions.json schema (from RESEARCH.md) -->
```json
{
  "MAX_PROFIT": {
    "primary_metric": "cycle_pf",
    "primary_sort": "desc",
    "tiebreaker_metric": "total_pnl_ticks",
    "tiebreaker_sort": "desc"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build profile scorer, create profile JSONs, and add --profile to engine</name>
  <files>shared/archetypes/rotational/profiles/profile_definitions.json, shared/archetypes/rotational/profiles/max_profit.json, shared/archetypes/rotational/profiles/safest.json, shared/archetypes/rotational/profiles/most_consistent.json, shared/archetypes/rotational/rotational_engine.py, shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md</files>
  <action>
**Part A: Profile definitions JSON**

Create `shared/archetypes/rotational/profiles/profile_definitions.json` with the scoring criteria from CONTEXT.md:

```json
{
  "MAX_PROFIT": {
    "description": "Highest cycle PF regardless of risk",
    "primary_metric": "cycle_pf",
    "primary_sort": "desc",
    "tiebreaker_metric": "total_pnl_ticks",
    "tiebreaker_sort": "desc"
  },
  "SAFEST": {
    "description": "Best survival metrics",
    "primary_metric": "worst_cycle_dd",
    "primary_sort": "asc",
    "secondary_metric": "max_level_exposure_pct",
    "secondary_sort": "asc",
    "tertiary_metric": "tail_ratio",
    "tertiary_sort": "desc"
  },
  "MOST_CONSISTENT": {
    "description": "Best risk-adjusted returns",
    "primary_metric": "calmar_ratio",
    "primary_sort": "desc",
    "secondary_metric": "winning_session_pct",
    "secondary_sort": "desc",
    "tertiary_metric": "max_dd_duration_bars",
    "tertiary_sort": "asc"
  }
}
```

**Part B: Profile scorer script (inline in run_sizing_sweep.py or standalone)**

Write a function `score_profiles(tsv_path, definitions_path, output_dir)` — can be added to `run_sizing_sweep.py` as a post-sweep step, or as a small standalone script. Preference: add as functions in `run_sizing_sweep.py` with a `--score-profiles` flag so it can be re-run without re-executing the sweep.

For each profile definition, for each bar type:
1. Load the TSV results
2. Sort by the profile's metric hierarchy (primary, secondary, tertiary)
3. Select top 3 candidates
4. The #1 candidate becomes the profile's winning config for that bar type

Store each profile as a JSON file in `profiles/`:
```json
{
  "profile": "MAX_PROFIT",
  "source": "sizing_sweep_P1a.tsv",
  "bar_types": {
    "bar_data_250vol_rot": {
      "step_dist": X, "max_levels": Y, "max_total_position": Z,
      "cycle_pf": ..., "n_cycles": ..., "total_pnl_ticks": ...,
      "worst_cycle_dd": ..., "max_level_exposure_pct": ..., "tail_ratio": ...,
      "calmar_ratio": ..., "sortino_ratio": ..., "winning_session_pct": ...,
      "max_dd_duration_bars": ...,
      "pf_lt_1_flag": true/false
    },
    // ... other bar types
  },
  "notes": "PF < 1.0 configs flagged — requires hypothesis improvement to be viable"
}
```

Per locked decision: PF < 1.0 configs are VALID candidates. Flag them with `"pf_lt_1_flag": true` but do NOT exclude.

**Part C: --profile flag in rotational_engine.py**

1. Add `--profile` argument (optional): `parser.add_argument("--profile", default=None, help="Profile name to load (e.g., max_profit)")`
2. Add `--bar-type` argument (optional, for profile use): `parser.add_argument("--bar-type", default=None, help="Bar type for profile config (e.g., bar_data_250vol_rot)")`
3. In `main()`, after loading config, if `--profile` is provided:
   - Load `profiles/{profile_name}.json`
   - Extract the config for the specified `--bar-type` (or first bar type if not specified)
   - Override: `config["hypothesis"]["trigger_params"]["step_dist"]`, `config["martingale"]["max_levels"]`, `config["martingale"]["max_total_position"]`, `config["martingale"]["max_contract_size"] = 16`
   - If `--bar-type` given, also filter `config["bar_data_primary"]` to only that bar type
   - Print: "Loaded profile {name} for {bar_type}: StepDist={sd}, ML={ml}, MTP={mtp}"

**Part D: Sizing sweep report**

Create `sizing_sweep_results/sizing_sweep_report.md` with:
1. Summary: total runs, dedup savings, runtime
2. Per-profile, per-bar-type top 3 table with full metrics
3. Pure reversal (MTP=1) section: per-bar-type PF and comparison to best martingale config
4. Notable findings: any PF >= 1.0 configs, bar-type disagreements in profile selection
5. Table of all MTP=1 results across all StepDist values (14 rows x 3 bar types)

The report is generated programmatically from the TSV data, not hand-written.
  </action>
  <verify>
    <automated>cd C:/Projects/pipeline && python -c "
import json, os
# Check profile files exist and have correct structure
for name in ['max_profit', 'safest', 'most_consistent', 'profile_definitions']:
    path = f'shared/archetypes/rotational/profiles/{name}.json'
    assert os.path.exists(path), f'Missing {path}'
    with open(path) as f:
        data = json.load(f)
    if name != 'profile_definitions':
        assert 'bar_types' in data, f'{name}.json missing bar_types'
        assert len(data['bar_types']) == 3, f'{name}.json should have 3 bar types'
print('Profile JSONs: OK')
# Check report exists
assert os.path.exists('shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md')
print('Report: OK')
" && python shared/archetypes/rotational/rotational_engine.py --help 2>&1 | grep -c "profile"</automated>
  </verify>
  <done>4 profile JSONs created with correct structure. Each profile has per-bar-type winning config. --profile flag available in engine CLI. Report generated with top 3 per profile per bar type and pure reversal comparison.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of sizing sweep results and profile selections</name>
  <action>
Present the sizing sweep report and profile selections for human review. The report was generated programmatically from sweep results in Task 1. Key review points: (1) pure reversal vs martingale comparison, (2) profile selection intuition check, (3) PF flagging compliance, (4) cross-bar-type consistency.
  </action>
  <verify>Human reviews report and profile selections and provides approval or feedback</verify>
  <done>Human approves profile selections and sweep findings, or provides specific feedback for revision</done>
  <what-built>Complete sizing sweep infrastructure: 3-parameter sweep results (~924 runs), 3 baseline profiles (MAX_PROFIT, SAFEST, MOST_CONSISTENT) per bar type, profile loader in engine, and analysis report.</what-built>
  <how-to-verify>
    1. Review the sweep report: `cat shared/archetypes/rotational/sizing_sweep_results/sizing_sweep_report.md`
    2. Check if pure reversal (MTP=1) outperforms martingale configs for any bar type — this is a critical finding
    3. Review profile selections — do the MAX_PROFIT, SAFEST, MOST_CONSISTENT profiles make intuitive sense?
    4. Verify PF < 1.0 configs are flagged but not excluded
    5. Check if any bar types strongly disagree on profile selection (different optimal configs)
    6. Test the --profile flag: `python shared/archetypes/rotational/rotational_engine.py --config shared/archetypes/rotational/rotational_params.json --profile max_profit --bar-type bar_data_250vol_rot --output /tmp/test_profile.json`
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Phase 3 planning, or describe issues with profile selection / sweep results</resume-signal>
</task>

</tasks>

<verification>
- 4 JSON files in profiles/ directory
- profile_definitions.json has 3 profile definitions with metric hierarchies
- Each profile JSON has 3 bar types with full metrics and pf_lt_1_flag
- --profile flag works in rotational_engine.py
- Report contains top 3 per profile per bar type and pure reversal analysis
</verification>

<success_criteria>
- Profiles are permanent pipeline infrastructure (JSON files in profiles/)
- Profile definitions are data-driven (new profiles addable without code changes)
- --profile flag loads and overrides config correctly
- PF < 1.0 flagged but not excluded (per locked decision)
- Pure reversal (MTP=1) results reported separately (per locked decision)
- Top 3 candidates per profile per bar type documented (per locked decision)
- Human approves profile selections and sweep findings
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-sizing-sweep-baseline/02.1-03-SUMMARY.md`
</output>
