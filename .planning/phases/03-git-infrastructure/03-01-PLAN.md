---
phase: 03-git-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_git_infrastructure.py
  - autocommit.sh
  - .git/hooks/pre-commit
  - .git/hooks/post-commit
autonomous: true
requirements:
  - GIT-01
  - GIT-02
  - GIT-03

must_haves:
  truths:
    - "autocommit.sh polls every 30s and commits changed files with auto: prefix"
    - "pre-commit hook blocks commits touching any file under stages/04-backtest/p2_holdout/"
    - "pre-commit hook blocks deletion of lines from audit/audit_log.md"
    - "pre-commit hook auto-generates HYPOTHESIS_PROMOTED audit entry on promoted file commit"
    - "pre-commit hook auto-generates PERIOD_CONFIG_CHANGED audit entry on period_config.md change"
    - "post-commit hook appends every commit to .git/commit_log.txt"
    - "post-commit hook auto-generates OOS_RUN audit entry when holdout_locked flag is committed"
    - "post-commit hook auto-generates DEPLOYMENT_APPROVED audit entry when deployment_ready.flag is committed"
  artifacts:
    - path: "tests/test_git_infrastructure.py"
      provides: "Subprocess-based hook tests in temp git repo"
      min_lines: 100
    - path: "autocommit.sh"
      provides: "Polling autocommit watcher"
      min_lines: 10
    - path: ".git/hooks/pre-commit"
      provides: "Holdout guard + audit enforcement + auto-entries"
      min_lines: 40
    - path: ".git/hooks/post-commit"
      provides: "Commit log + OOS_RUN + DEPLOYMENT_APPROVED auto-entries"
      min_lines: 30
  key_links:
    - from: "autocommit.sh"
      to: "git commit"
      via: "git add -A && git commit with auto: prefix"
      pattern: "git commit -m \"auto:"
    - from: ".git/hooks/pre-commit"
      to: "audit/audit_log.md"
      via: "cat >> append and git add"
      pattern: "cat >> .*audit_log.md"
    - from: ".git/hooks/post-commit"
      to: ".git/commit_log.txt"
      via: "echo append"
      pattern: ">> .git/commit_log.txt"
    - from: ".git/hooks/post-commit"
      to: "audit/audit_log.md"
      via: "cat >> append + git commit --amend"
      pattern: "git commit --amend --no-edit"
---

<objective>
Create the three git infrastructure scripts (autocommit.sh, pre-commit hook, post-commit hook) and their test suite.

Purpose: Every file change during autoresearch must be automatically committed, holdout data must be structurally protected, and audit entries must be generated automatically -- these are prerequisites for the backtest engine (Phase 4).

Output: Four files -- test suite, autocommit watcher, pre-commit hook, post-commit hook.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-git-infrastructure/03-RESEARCH.md
@.planning/phases/03-git-infrastructure/03-VALIDATION.md

@audit/audit_log.md
@audit/audit_entry.sh

<interfaces>
<!-- Existing audit_log.md format (append target for hooks): -->
```markdown
# Futures Pipeline Audit Log
# APPEND-ONLY. Never delete or modify entries. Add correction notes instead.
# Entries are generated by: pre-commit hook, post-commit hook, audit_entry.sh

---

## 2026-03-14 | MANUAL_NOTE
- subject: Pipeline created
- detail: ...
- human: Ji
```

<!-- Existing audit_entry.sh pattern (heredoc append to audit_log.md): -->
```bash
cat >> "$AUDIT_LOG" << EOF

## $TIMESTAMP | EVENT_TYPE
- field: value
- generated_by: hook_name
EOF
git add audit/audit_log.md
```

<!-- Directory structure (holdout guard target): -->
```
stages/04-backtest/p2_holdout/   # Empty dir, will contain holdout files
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffold for git infrastructure</name>
  <files>tests/test_git_infrastructure.py</files>
  <behavior>
    - test_holdout_guard: In a temp git repo with pre-commit hook installed, staging a file under `stages/04-backtest/p2_holdout/` and running `git commit` returns non-zero exit code and stderr contains "holdout" or "P2"
    - test_holdout_guard_allows_other_files: Staging a file NOT under p2_holdout/ commits successfully
    - test_audit_append_only_blocks_deletion: In a temp repo with audit_log.md committed, staging a version with a line removed causes commit to fail with "append-only" in stderr
    - test_audit_append_only_allows_append: Staging a version with lines appended (no deletions) commits successfully
    - test_commit_log_written: After a successful commit in a temp repo with post-commit hook installed, `.git/commit_log.txt` exists and contains the commit hash
    - test_oos_run_entry: After committing a file matching `holdout_locked*.flag`, audit_log.md contains an OOS_RUN entry with period, pf, n_trades fields
    - test_hypothesis_promoted_entry: After committing a file under `03-hypothesis/output/promoted_hypotheses/`, audit_log.md contains a HYPOTHESIS_PROMOTED entry
    - test_period_config_changed_entry: After committing a change to `_config/period_config.md`, audit_log.md contains a PERIOD_CONFIG_CHANGED entry
  </behavior>
  <action>
Create `tests/test_git_infrastructure.py` with subprocess-based tests. Follow the project's existing test pattern (see test_scaffold_adapter.py):

**Test infrastructure pattern:**
- Each test creates a fresh temp git repo using `tmp_path` fixture
- A shared fixture `git_repo(tmp_path)` does: `git init`, `git config user.email/name`, copies the pre-commit and/or post-commit hook from the real repo into `.git/hooks/`, `chmod +x` both hooks
- Tests use `subprocess.run(["git", "commit", ...], capture_output=True, text=True, cwd=repo_path)` to exercise hooks
- Assert on `returncode` (0 for success, non-zero for blocked) and `stderr`/`stdout` content

**Important details:**
- The hooks are bash scripts -- they must be read from the REAL repo's `.git/hooks/` directory (the hooks don't exist yet, so write tests that will fail RED first and pass GREEN after Task 2 creates them)
- For the RED phase: create the test file referencing hook paths that don't exist yet. Use a fixture that copies hooks from a known location. Tests will fail because hooks don't exist -- that's correct TDD RED.
- Use `_REPO_ROOT = Path(__file__).resolve().parents[1]` to find the real repo root (same pattern as test_scaffold_adapter.py)
- For OOS_RUN test: create a fake `stages/05-assessment/output/verdict_report.json` with `{"pf": "1.5", "n_trades": "42", "verdict": "PASS"}` in the temp repo so the python -c JSON read works
- For holdout guard test: use path-prefix check `stages/04-backtest/p2_holdout/` (any file under that dir), not just the three specific files
- Windows compatibility: use `subprocess.run(["bash", hook_path, ...])` if needed, or let git invoke the hooks naturally via `git commit`
- Mark the autocommit test (GIT-01) as `@pytest.mark.skip(reason="requires background process -- manual verification")` since polling loops can't be unit-tested cleanly

Write FAILING tests first (RED). Do NOT create stub hook files. Run `python -m pytest tests/test_git_infrastructure.py -x -q` and confirm tests fail.
  </action>
  <verify>
    <automated>python -m pytest tests/test_git_infrastructure.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists with 8+ test functions. Tests fail (RED) because hook scripts don't exist yet. Test names match the behavior list above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create autocommit.sh, pre-commit hook, and post-commit hook</name>
  <files>autocommit.sh, .git/hooks/pre-commit, .git/hooks/post-commit</files>
  <behavior>
    - autocommit.sh: polls every 30s, detects changes via git diff/ls-files, commits with "auto: TIMESTAMP | changed_files" message truncated to 80 chars
    - pre-commit: (1) blocks p2_holdout/ path prefix, (2) blocks audit_log.md line deletions, (3) auto-generates HYPOTHESIS_PROMOTED entry, (4) auto-generates PERIOD_CONFIG_CHANGED entry, (5) prints rollover warning on period_config change (does NOT abort)
    - post-commit: (1) appends to commit_log.txt, (2) auto-generates OOS_RUN entry on holdout flag creation, (3) auto-generates DEPLOYMENT_APPROVED entry on deployment flag creation
  </behavior>
  <action>
**autocommit.sh** -- Transcribe verbatim from Architecture doc lines 546-563 (already in RESEARCH.md). Place at repo root. No modifications needed -- all commands work in MSYS2 bash. Make executable.

**pre-commit hook** -- Combine Architecture doc lines 573-592 (holdout guard) with lines 1313-1363 (audit additions). Apply these modifications per RESEARCH.md findings:

1. Change holdout guard from three-file list to path-prefix check: replace the PROTECTED array loop with `git diff --cached --name-only | grep -q "stages/04-backtest/p2_holdout/"` -- this satisfies the success criteria "any file under p2_holdout/" and is strictly safer.

2. Keep audit append-only check exactly as architecture doc: `git diff --cached -- audit/audit_log.md | grep -q "^-[^-]"`

3. HYPOTHESIS_PROMOTED block: transcribe lines 1322-1344 verbatim.

4. PERIOD_CONFIG_CHANGED block: transcribe lines 1346-1363 verbatim. After the audit entry append, add a rollover warning to stderr: `echo "WARNING: period_config.md changed. Verify rollover impact on active experiments." >&2` -- this warns but does NOT exit 1.

5. Order of checks must be: holdout guard (exit 1) -> audit append-only (exit 1) -> HYPOTHESIS_PROMOTED (auto-entry) -> PERIOD_CONFIG_CHANGED (auto-entry + warning) -> exit 0

**post-commit hook** -- Combine Architecture doc lines 566-570 (commit log) with lines 1369-1411 (audit additions). Apply these fixes per RESEARCH.md:

1. Commit log line: transcribe line 570 verbatim.

2. OOS_RUN block (lines 1369-1393): Replace `grep -oP 'locked_\K[^.]+'` with `sed 's/.*locked_\([^.]*\).*/\1/'` (Pitfall 1 fix). Replace all three `python3` calls with `python` (Pitfall 2 fix). Keep `|| echo "unknown"` fallbacks.

3. DEPLOYMENT_APPROVED block (lines 1395-1411): Transcribe verbatim. Replace `python3` with `python` if present.

4. Do NOT add a recursion guard for `git commit --amend --no-edit` -- git does not re-fire post-commit on amend (Pitfall 3).

After creating all three files, `chmod +x` both hooks and autocommit.sh. Run the test suite GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_git_infrastructure.py -x -q</automated>
  </verify>
  <done>All tests pass GREEN. autocommit.sh exists at repo root. Both hooks exist in .git/hooks/ and are executable. Pre-commit blocks holdout and audit deletions. Post-commit writes commit_log.txt and generates audit entries.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_git_infrastructure.py -x -q` -- all tests pass
- `python -m pytest tests/ -x -q` -- full suite still passes (no regressions)
- `ls -la .git/hooks/pre-commit .git/hooks/post-commit` -- both exist and are executable
- `head -1 autocommit.sh` -- shows `#!/bin/bash`
- `grep "stages/04-backtest/p2_holdout/" .git/hooks/pre-commit` -- path-prefix guard present
- `grep "python " .git/hooks/post-commit` -- uses `python` not `python3`
- `grep -c "grep -P" .git/hooks/post-commit` -- returns 0 (no grep -P usage)
</verification>

<success_criteria>
All three scripts exist, are executable, and pass automated tests. Pre-commit blocks holdout modifications and audit deletions. Post-commit writes commit log and generates audit entries. No grep -P or python3 usage in any hook script.
</success_criteria>

<output>
After completion, create `.planning/phases/03-git-infrastructure/03-01-SUMMARY.md`
</output>
