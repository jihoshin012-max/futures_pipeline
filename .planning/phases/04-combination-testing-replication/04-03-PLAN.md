---
phase: 04-combination-testing-replication
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - shared/archetypes/rotational/run_p1b_replication.py
  - shared/archetypes/rotational/test_p1b_replication.py
  - shared/archetypes/rotational/combination_results/p1b_replication.tsv
  - shared/archetypes/rotational/combination_results/p1b_replication.json
  - shared/archetypes/rotational/combination_results/phase4_report.md
autonomous: false
requirements: [ROT-RES-07]

must_haves:
  truths:
    - "Final candidates from Plan 02 are run on P1b data (period=P1b, ~2025-11-02 to 2025-12-14) with frozen parameters"
    - "Replication verdict is classified as PASS/CONDITIONAL/WEAK_REPLICATION per spec Section 7.3 thresholds"
    - "WEAK_REPLICATION is a soft gate — flagged for human review, NOT a hard block on P2 advancement"
    - "P1b data is NOT the P2 holdout — P1b runs are safe to repeat (second half of IS window)"
    - "phase4_report.md summarizes entire Phase 4: param sweep findings, combination results, TDS impact, P1b verdicts"
    - "Human approves final advancement decision before Phase 5"
  artifacts:
    - path: "shared/archetypes/rotational/run_p1b_replication.py"
      provides: "P1b replication runner with verdict classification and report generation"
      min_lines: 100
    - path: "shared/archetypes/rotational/test_p1b_replication.py"
      provides: "Unit tests for replication verdict classification and period usage"
      min_lines: 40
    - path: "shared/archetypes/rotational/combination_results/p1b_replication.tsv"
      provides: "P1b replication results for all final candidates"
    - path: "shared/archetypes/rotational/combination_results/phase4_report.md"
      provides: "Human-readable Phase 4 summary for advancement decision"
  key_links:
    - from: "run_p1b_replication.py"
      to: "final_candidates.json"
      via: "Reads frozen configs from Plan 02"
      pattern: "final_candidates"
    - from: "run_p1b_replication.py"
      to: "RotationalSimulator"
      via: "Runs candidates with period=P1b"
      pattern: 'period.*P1b'
    - from: "phase4_report.md"
      to: "Phase 5 assessment"
      via: "Human reads report and decides advancement"
---

<objective>
Run P1b replication gate on final candidates from Plan 02. Classify each candidate as PASS/CONDITIONAL/WEAK_REPLICATION per spec thresholds. Generate phase4_report.md summarizing all of Phase 4. Human approves advancement to Phase 5.

Purpose: P1b replication is the internal validation step (Pipeline Rule 4) — the candidate must show its edge persists on unseen-but-IS data (~2025-11-02 to 2025-12-14). This is a soft gate: WEAK_REPLICATION surfaces for human review rather than hard-blocking. The Phase 4 report provides the complete decision context for advancement.

Output: run_p1b_replication.py, p1b_replication.tsv, p1b_replication.json, phase4_report.md
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-combination-testing-replication/04-RESEARCH.md
@.planning/phases/04-combination-testing-replication/04-01-SUMMARY.md
@.planning/phases/04-combination-testing-replication/04-02-SUMMARY.md

@shared/archetypes/rotational/combination_results/final_candidates.json
@shared/archetypes/rotational/rotational_engine.py
@shared/archetypes/rotational/run_hypothesis_screening.py

<interfaces>
<!-- Key APIs for P1b replication -->

From rotational_engine.py:
```python
def compute_cycle_metrics(cycles: pd.DataFrame, cost_ticks: float) -> dict
def compute_extended_metrics(cycles: pd.DataFrame, cost_ticks: float, bars_df: pd.DataFrame, max_levels: int) -> dict
```

From RotationalSimulator:
```python
# config["period"] = "P1b" triggers date filtering to ~2025-11-02 to 2025-12-14
sim = RotationalSimulator(config, bars)
result = sim.run()  # returns SimulationResult with .cycles DataFrame
```

Replication verdict thresholds (spec Section 7.3):
- PASS: P1b cycle_pf >= 1.3 AND P1a cycle_pf >= 1.5
- CONDITIONAL: P1b cycle_pf >= 1.1
- WEAK_REPLICATION: P1b cycle_pf < 1.1 (soft gate: flag_and_review)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build P1b replication runner, execute replication, and generate Phase 4 report</name>
  <files>shared/archetypes/rotational/run_p1b_replication.py, shared/archetypes/rotational/test_p1b_replication.py, shared/archetypes/rotational/combination_results/p1b_replication.tsv, shared/archetypes/rotational/combination_results/p1b_replication.json, shared/archetypes/rotational/combination_results/phase4_report.md</files>
  <behavior>
    - test_classify_replication_pass: P1b_pf=1.5, P1a_pf=1.6 -> "PASS"
    - test_classify_replication_conditional: P1b_pf=1.2, P1a_pf=1.4 -> "CONDITIONAL"
    - test_classify_replication_weak: P1b_pf=0.9, P1a_pf=1.5 -> "WEAK_REPLICATION"
    - test_classify_replication_pass_boundary: P1b_pf=1.3, P1a_pf=1.5 -> "PASS" (exact threshold)
    - test_p1b_period_used: config passed to simulator has period="P1b"
    - test_weak_replication_is_soft_gate: WEAK_REPLICATION verdict sets advance_to_p2=True with flag_for_review=True (not hard block)
  </behavior>
  <action>
    Create `shared/archetypes/rotational/run_p1b_replication.py` (line 1: `# archetype: rotational`) with:

    1. `classify_replication(p1a_pf: float, p1b_pf: float) -> str` — return "PASS" if p1b_pf >= 1.3 and p1a_pf >= 1.5; "CONDITIONAL" if p1b_pf >= 1.1; else "WEAK_REPLICATION".

    2. `run_p1b_replication(final_candidates: list[dict]) -> list[dict]` — for each candidate config in final_candidates.json:
       - Deep copy config, set period="P1b"
       - For each source_id: load bars, run RotationalSimulator, compute metrics
       - Record: candidate_id, profile, source_id, p1a_cycle_pf (from final_candidates), p1b_cycle_pf, delta_pf_p1b_vs_p1a, replication_verdict, advance_to_p2 (True for all — soft gate), flag_for_review (True if WEAK_REPLICATION), all extended metrics
       - Read instrument constants from _config/instruments.md (never hardcode)

    3. `generate_phase4_report(param_sweep_path, combinations_path, tds_combined_path, p1b_path) -> str` — generate comprehensive markdown report:
       - Section 1: Param Sweep Summary — N hypotheses tested, N winners per dimension, best delta_pf
       - Section 2: Combination Results — N combinations tested, best combinations, cross-bar-type consistency
       - Section 3: TDS Integration — TDS impact per (profile, bar_type), confirms vol/tick disabled
       - Section 4: P1b Replication — verdict per candidate per bar_type, overall assessment
       - Section 5: Advancement Recommendation — which candidates to advance to Phase 5 (P2 one-shot)

    4. CLI: `python run_p1b_replication.py` runs replication + generates report.

    CRITICAL: P1b is NOT the P2 holdout. P1b is the second half of the P1 in-sample window. It is safe to run multiple times. Do NOT check holdout_locked_P2.flag before running P1b. Only P2 runs are irreversible.

    IMPORTANT: If final_candidates.json contains raw profile configs (no hypothesis overlay — the "empty winners" case from Plan 02), the P1b replication still runs: it verifies the profile baselines replicate on P1b data. This is valuable even without hypothesis improvements.

    Create `test_p1b_replication.py` (line 1: `# archetype: rotational`) with the 6 behavior tests above.

    Then execute:
    ```bash
    cd shared/archetypes/rotational
    python run_p1b_replication.py
    ```

    This writes: p1b_replication.tsv, p1b_replication.json, phase4_report.md to combination_results/.
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_p1b_replication.py -x -q && python -c "import os; d='shared/archetypes/rotational/combination_results'; assert os.path.exists(f'{d}/p1b_replication.tsv'), 'P1b TSV missing'; assert os.path.exists(f'{d}/phase4_report.md'), 'Report missing'; print('P1b replication outputs verified')"</automated>
  </verify>
  <done>P1b replication executed on all final candidates, verdicts classified, phase4_report.md generated with comprehensive Phase 4 summary, all tests passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of Phase 4 report and advancement decision</name>
  <files>shared/archetypes/rotational/combination_results/phase4_report.md</files>
  <action>
    Present Phase 4 report to user for final review. Complete Phase 4 execution: (1) per-hypothesis parameter sweep identified dimensional winners, (2) combination sweep + TDS integration identified final candidates, (3) P1b replication gate classified candidates as PASS/CONDITIONAL/WEAK_REPLICATION. All results in combination_results/ directory.
  </action>
  <verify>Human confirms which candidates to advance to Phase 5 (P2 one-shot)</verify>
  <done>Human has reviewed phase4_report.md, P1b replication verdicts, and approved specific candidates for Phase 5 advancement (or rejected all)</done>
  <what-built>
    Complete Phase 4 execution: (1) per-hypothesis parameter sweep identified dimensional winners, (2) combination sweep + TDS integration identified final candidates, (3) P1b replication gate classified candidates as PASS/CONDITIONAL/WEAK_REPLICATION. All results in combination_results/ directory.
  </what-built>
  <how-to-verify>
    1. Read `shared/archetypes/rotational/combination_results/phase4_report.md` — the comprehensive Phase 4 summary
    2. Review P1b replication verdicts in `combination_results/p1b_replication.tsv`:
       - PASS: P1b cycle_pf >= 1.3 AND P1a >= 1.5 — strong replication
       - CONDITIONAL: P1b cycle_pf >= 1.1 — degraded but viable
       - WEAK_REPLICATION: P1b cycle_pf < 1.1 — needs human judgment
    3. For any WEAK_REPLICATION candidates: decide whether to advance to Phase 5 anyway (soft gate)
    4. Confirm which candidates should advance to Phase 5 (P2 one-shot run)

    Key decision: Which candidates advance to Phase 5 for the irreversible P2 one-shot run? This is the last review before frozen parameters are tested on holdout data.
  </how-to-verify>
  <resume-signal>Type "approved" with list of candidates to advance, or "reject" with reasoning</resume-signal>
</task>

</tasks>

<verification>
- All tests pass: `pytest shared/archetypes/rotational/ -x -q`
- p1b_replication.tsv has one row per (candidate, bar_type) with verdict column
- phase4_report.md exists and covers all 5 sections
- Human has reviewed and approved advancement decision
</verification>

<success_criteria>
- P1b replication completed on all final candidates from Plan 02
- Every candidate classified as PASS/CONDITIONAL/WEAK_REPLICATION
- phase4_report.md generated with comprehensive Phase 4 summary
- Human approved advancement to Phase 5 with explicit candidate list
- All existing rotational tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/04-combination-testing-replication/04-03-SUMMARY.md`
</output>
