---
phase: 04-combination-testing-replication
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - shared/archetypes/rotational/run_combination_sweep.py
  - shared/archetypes/rotational/test_combination_sweep.py
  - shared/archetypes/rotational/combination_results/phase4_combinations.tsv
  - shared/archetypes/rotational/combination_results/phase4_combinations.json
  - shared/archetypes/rotational/combination_results/phase4_tds_combined.tsv
  - shared/archetypes/rotational/combination_results/final_candidates.json
autonomous: false
requirements: [ROT-RES-05, ROT-RES-06]

must_haves:
  truths:
    - "Dimensional winners from Plan 01 are combined across dimensions (Dim A x H2 on/off x Dim C x Dim D x H18/H19)"
    - "Cross-bar-type consistency is checked: a combination must improve on >= 2 of 3 bar types to advance"
    - "TDS configs from best_tds_configs.json are injected per (profile, bar_type): disabled for vol/tick, velocity-only L1 for 10sec MAX_PROFIT/MOST_CONSISTENT, disabled for 10sec SAFEST"
    - "Combination budget is controlled: greedy stacking fallback if combination count exceeds 500"
    - "Final candidates include both with-TDS and without-TDS flavors for 10sec MAX_PROFIT/MOST_CONSISTENT"
    - "Human reviews combination results before P1b replication"
  artifacts:
    - path: "shared/archetypes/rotational/combination_results/phase4_combinations.tsv"
      provides: "All combination sweep results with delta_pf vs profile baselines"
    - path: "shared/archetypes/rotational/combination_results/phase4_tds_combined.tsv"
      provides: "Best combinations with TDS injected, showing TDS impact"
    - path: "shared/archetypes/rotational/combination_results/final_candidates.json"
      provides: "Frozen configs ready for P1b replication in Plan 03"
  key_links:
    - from: "run_combination_sweep.py (Plan 02 mode)"
      to: "combination_results/dimensional_winners.json"
      via: "Reads Plan 01 winners as combination inputs"
      pattern: "dimensional_winners"
    - from: "run_combination_sweep.py"
      to: "best_tds_configs.json"
      via: "TDS injection per (profile, bar_type)"
      pattern: "inject_tds|best_tds_configs"
    - from: "final_candidates.json"
      to: "Plan 03 P1b replication"
      via: "Frozen configs consumed by run_p1b_replication.py"
      pattern: "final_candidates"
---

<objective>
Combine dimensional winners from Plan 01 across hypothesis dimensions, integrate calibrated TDS configs from Phase 03.1, and identify final candidate configurations that beat the profile baselines consistently across bar types. Produce final_candidates.json for P1b replication in Plan 03.

Purpose: Individual hypothesis winners are necessary but not sufficient. The rotational spec requires testing combinations (Dim A trigger x H2 symmetry x Dim C filters x Dim D mods x H18/H19). This plan builds the combination sweep, applies TDS integration per the human-approved Phase 03.1 disposition, and surfaces the best candidates for human review before P1b replication.

Output: phase4_combinations.tsv (combination sweep results), phase4_tds_combined.tsv (TDS-integrated results), final_candidates.json (frozen configs for P1b)
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-combination-testing-replication/04-RESEARCH.md
@.planning/phases/04-combination-testing-replication/04-01-SUMMARY.md

@shared/archetypes/rotational/hypothesis_configs.py
@shared/archetypes/rotational/run_combination_sweep.py
@shared/archetypes/rotational/combination_results/dimensional_winners.json
@shared/archetypes/rotational/tds_profiles/best_tds_configs.json

<interfaces>
<!-- From Plan 01 outputs (executor reads these at execution time) -->

From dimensional_winners.json (Plan 01 output):
```json
{
  "H1": {
    "MAX_PROFIT": {
      "bar_data_250vol_rot": {"best_params": {...}, "delta_pf": 0.15, "cycle_pf": 2.35},
      "bar_data_250tick_rot": {...},
      "bar_data_10sec_rot": {...}
    },
    ...
  },
  ...
}
```

From best_tds_configs.json (approved_disposition):
- vol/tick: TDS disabled (enabled: false)
- 10sec MAX_PROFIT/MOST_CONSISTENT: velocity-only L1 (60sec)
- 10sec SAFEST: TDS disabled

From hypothesis_configs.py:
```python
# Dimension mapping for combination logic:
# A = Trigger mechanisms (H1, H3, H8, H9, H10)
# B = Symmetry (H2)
# C = Activity filters (H4-H7, H11-H17, H33-H37, H39-H41)
# D = Structural mods (H20-H26, H23)
# F = Combined filters (H27-H32)
# Special: H18 (session filter), H19 (cross-bar-type divergence)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build combination sweep + TDS integration and execute experiments</name>
  <files>shared/archetypes/rotational/run_combination_sweep.py, shared/archetypes/rotational/test_combination_sweep.py, shared/archetypes/rotational/combination_results/phase4_combinations.tsv, shared/archetypes/rotational/combination_results/phase4_combinations.json, shared/archetypes/rotational/combination_results/phase4_tds_combined.tsv, shared/archetypes/rotational/combination_results/final_candidates.json</files>
  <behavior>
    - test_build_combination_config: stacks Dim A winner + H2 + Dim C filter + Dim D mod into single config without overwriting each other
    - test_inject_tds_disabled_for_vol_tick: TDS injection for vol/tick sets trend_defense.enabled=false
    - test_inject_tds_velocity_l1_10sec: TDS injection for 10sec MAX_PROFIT reads velocity L1 config from best_tds_configs.json
    - test_inject_tds_disabled_safest_10sec: TDS injection for 10sec SAFEST sets trend_defense.enabled=false
    - test_greedy_stack_budget: when combination count exceeds budget, greedy stacking adds one winner at a time and keeps only if improvement maintained
    - test_cross_bar_type_consistency: combination must beat baseline on >= 2 bar types to be a final candidate
  </behavior>
  <action>
    Add the following functions to `run_combination_sweep.py`:

    1. `build_combination_config(dim_a_config, h2_enabled, h2_params, filter_winners, h18_enabled, profile_data, source_id) -> dict` — merge a Dim A winner config with optional H2, a list of Dim C/D/F filter winners, and H18 session filter. Use build_experiment_config() for each layer, stacking config_patches. Inject profile martingale per bar_type.

    2. `inject_tds_config(config, best_tds_configs, profile_name, source_id) -> dict` — read TDS entry from best_tds_configs[profile_name][source_id], deep-copy trend_defense block into config. Per Phase 03.1 disposition: vol/tick get enabled=false; 10sec MAX_PROFIT/MOST_CONSISTENT get velocity L1; 10sec SAFEST gets enabled=false.

    3. `enumerate_combinations(winners, budget=500) -> list[dict]` — from dimensional_winners.json, enumerate Dim A winner x H2 on/off x Dim C winners x Dim D winners x H18 on/off. If total exceeds budget, switch to greedy stacking: start with best Dim A winner alone, add one filter at a time (sorted by individual delta_pf), keep only additions that maintain/improve aggregate delta_pf. H19 is deferred (not implemented in this phase unless CBT feature layer exists).

    4. `run_combination_sweep(combinations, profiles, baselines) -> list[dict]` — for each combination config x 3 profiles x 3 bar types: run simulator, compute extended metrics, compute delta_pf vs baseline, check cross-bar-type consistency (>= 2 bar types beating baseline).

    5. `run_tds_integration(top_combinations, best_tds_configs, profiles, baselines) -> list[dict]` — take the top N combinations (those beating baseline on >= 2 bar types), inject TDS per (profile, bar_type), re-run, measure TDS impact. For 10sec MAX_PROFIT/MOST_CONSISTENT: emit both with-TDS and without-TDS results (since only 10sec gets TDS).

    6. `select_final_candidates(combination_results, tds_results) -> list[dict]` — select best combination per profile, emit final_candidates.json with frozen configs (both no-TDS and with-TDS flavors where applicable).

    7. CLI: `--plan 2` runs combination sweep + TDS integration. `--budget N` sets combination count cap (default 500).

    CRITICAL EDGE CASE: If dimensional_winners.json from Plan 01 is EMPTY (no hypotheses beat baselines), Plan 02 should:
    - Log "No dimensional winners found — profile baselines are already optimal"
    - Write final_candidates.json with the raw profile configs (no hypothesis overlay) as the candidates for P1b
    - Still inject TDS for 10sec MAX_PROFIT/MOST_CONSISTENT entries
    - This is a valid outcome, not a failure

    Add the 6 behavior tests to test_combination_sweep.py.

    Then execute: `python run_combination_sweep.py --plan 2`

    Write outputs to combination_results/:
    - phase4_combinations.tsv + .json — all combination sweep results
    - phase4_tds_combined.tsv — TDS integration results
    - final_candidates.json — frozen configs for P1b replication

    Print summary: N combinations tested, N beating baseline on >= 2 bar types, N final candidates, best delta_pf, TDS impact summary.
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_combination_sweep.py -x -q && python -c "import json, os; d='shared/archetypes/rotational/combination_results'; assert os.path.exists(f'{d}/final_candidates.json'), 'final_candidates.json missing'; fc=json.load(open(f'{d}/final_candidates.json')); print(f'Final candidates: {len(fc)} entries')"</automated>
  </verify>
  <done>Combination sweep executed, TDS integrated per Phase 03.1 disposition, final_candidates.json written with frozen configs, all new + existing tests passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of combination results before P1b replication</name>
  <files>shared/archetypes/rotational/combination_results/final_candidates.json</files>
  <action>
    Present combination results to user for review. Phase 4 combination testing: per-hypothesis parameter sweep (Plan 01) identified dimensional winners, then combination sweep (this plan) combined winners across dimensions with TDS integration. Results in combination_results/ directory.
  </action>
  <verify>Human confirms combination results are reasonable for P1b replication advancement</verify>
  <done>Human has reviewed dimensional_winners.json, phase4_combinations.tsv, phase4_tds_combined.tsv, and final_candidates.json and approved advancement to P1b replication</done>
  <what-built>
    Phase 4 combination testing: per-hypothesis parameter sweep (Plan 01) identified dimensional winners, then combination sweep (this plan) combined winners across dimensions with TDS integration. Results in combination_results/ directory.
  </what-built>
  <how-to-verify>
    1. Review `shared/archetypes/rotational/combination_results/dimensional_winners.json` — how many hypotheses beat the profile baselines? Which dimensions are represented?
    2. Review `shared/archetypes/rotational/combination_results/phase4_combinations.tsv` — do the best combinations show consistent improvement across bar types?
    3. Review `shared/archetypes/rotational/combination_results/phase4_tds_combined.tsv` — does TDS add value for 10sec bars? Are vol/tick correctly showing TDS disabled?
    4. Review `shared/archetypes/rotational/combination_results/final_candidates.json` — are these configs reasonable for P1b replication?
    5. Special case: if dimensional_winners.json is empty, the profile baselines are already optimal — approve advancing raw profiles to P1b

    Key questions:
    - Are the winning delta_pf values statistically meaningful (not just noise)?
    - Do combinations show improvement on >= 2 bar types?
    - Is it reasonable to advance these candidates to P1b replication?
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to P1b replication (Plan 03), or describe concerns</resume-signal>
</task>

</tasks>

<verification>
- All tests pass: `pytest shared/archetypes/rotational/ -x -q`
- final_candidates.json exists with valid configs
- TDS configs match Phase 03.1 approved disposition (disabled for vol/tick, velocity L1 for 10sec MAX_PROFIT/MOST_CONSISTENT)
- Human has reviewed and approved results
</verification>

<success_criteria>
- Combination sweep completed across all relevant dimensional combinations
- TDS integrated per human-approved Phase 03.1 disposition
- final_candidates.json produced with frozen configs for P1b replication
- Cross-bar-type consistency verified (>= 2 bar types)
- Human approved results before P1b replication proceeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-combination-testing-replication/04-02-SUMMARY.md`
</output>
