---
phase: 03.1-tds-profile-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/archetypes/rotational/run_tds_sweep.py
  - shared/archetypes/rotational/test_tds_sweep.py
  - shared/archetypes/rotational/tds_profiles/.gitkeep
autonomous: true
requirements: [TDS-CAL-01, TDS-CAL-02]

must_haves:
  truths:
    - "run_tds_sweep.py produces a TSV with one row per (profile x bar_type x tds_config) combination"
    - "Each row includes both raw extended metrics AND survival deltas vs the profile no-TDS baseline"
    - "TDS is provably active in sweep runs (l1/l2/l3_triggers > 0 for non-trivial configs)"
    - "The harness loads profile configs from profiles/*.json and reads bar data via load_bars()"
  artifacts:
    - path: "shared/archetypes/rotational/run_tds_sweep.py"
      provides: "TDS calibration sweep harness"
      min_lines: 200
    - path: "shared/archetypes/rotational/test_tds_sweep.py"
      provides: "Unit tests for harness config assembly and survival metrics"
      min_lines: 80
    - path: "shared/archetypes/rotational/tds_profiles/tds_sweep_P1a.tsv"
      provides: "2,880 sweep result rows"
      contains: "profile\tsource_id"
    - path: "shared/archetypes/rotational/tds_profiles/tds_sweep_P1a.json"
      provides: "Machine-readable sweep results"
  key_links:
    - from: "run_tds_sweep.py"
      to: "profiles/*.json"
      via: "json.load for martingale params"
      pattern: "json\\.load.*profile"
    - from: "run_tds_sweep.py"
      to: "RotationalSimulator"
      via: "simulator instantiation per sweep combo"
      pattern: "RotationalSimulator\\(config.*bars\\)"
    - from: "run_tds_sweep.py"
      to: "compute_extended_metrics"
      via: "import from rotational_engine"
      pattern: "compute_extended_metrics"
---

<objective>
Build the TDS calibration sweep harness and execute the primary 2,880-run sweep.

Purpose: Systematically explore TDS threshold parameter space against the 3 baseline profiles (MAX_PROFIT, SAFEST, MOST_CONSISTENT) on all 3 bar types. This produces the raw data needed by Plan 02 to select best TDS configurations.

Output: `run_tds_sweep.py`, `test_tds_sweep.py`, `tds_profiles/tds_sweep_P1a.tsv`, `tds_profiles/tds_sweep_P1a.json`
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-tds-profile-calibration/03.1-RESEARCH.md
@.planning/phases/03.1-tds-profile-calibration/03.1-VALIDATION.md

@shared/archetypes/rotational/run_sizing_sweep.py
@shared/archetypes/rotational/rotational_simulator.py
@shared/archetypes/rotational/rotational_engine.py
@shared/archetypes/rotational/trend_defense.py
@shared/archetypes/rotational/profiles/max_profit.json
@shared/archetypes/rotational/profiles/safest.json
@shared/archetypes/rotational/profiles/most_consistent.json
@shared/archetypes/rotational/rotational_params.json

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From rotational_simulator.py:
```python
class RotationalSimulator:
    def __init__(self, config: dict, bars: pd.DataFrame): ...
    def run(self) -> SimulationResult: ...
    # self._tds is set when config["trend_defense"]["enabled"] is True
```

From rotational_engine.py:
```python
def compute_extended_metrics(
    cycles: pd.DataFrame,
    cost_ticks: float,
    bars_df: pd.DataFrame,
    max_levels: int,
) -> dict:
```

From trend_defense.py:
```python
class TrendDefenseSystem:
    def __init__(self, config: dict, median_bar_sec: float): ...
    def get_summary(self) -> dict:  # returns {l1_triggers, l2_triggers, l3_triggers, ...}
```

From profiles/max_profit.json (schema):
```json
{
  "profile": "MAX_PROFIT",
  "bar_types": {
    "bar_data_250vol_rot": {
      "step_dist": 7.0, "max_levels": 1, "max_total_position": 2,
      "cycle_pf": 2.2037, "worst_cycle_dd": 8569.0, "max_level_exposure_pct": 0.0,
      "tail_ratio": 0.0958, "total_pnl_ticks": 10537.0, ...
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build run_tds_sweep.py harness and test_tds_sweep.py</name>
  <files>
    shared/archetypes/rotational/run_tds_sweep.py,
    shared/archetypes/rotational/test_tds_sweep.py,
    shared/archetypes/rotational/tds_profiles/.gitkeep
  </files>
  <behavior>
    - test_build_run_config: build_run_config() assembles a valid config dict with profile martingale params + TDS config + correct instrument constants; config["trend_defense"]["enabled"] is True
    - test_build_tds_grid: build_tds_grid() returns 320 combos (5 drawdown x 4 velocity x 4 consecutive x 4 cooldown) with Level 1 and precursor at spec defaults
    - test_compute_tds_survival_metrics: compute_tds_survival_metrics() returns dict with worst_dd_reduction, max_level_pct_reduction, pnl_impact_ticks, tail_ratio_delta, n_td_flatten_cycles, l3_recovery_bars_avg; worst_dd_reduction = baseline_worst_cycle_dd - run_worst_cycle_dd
    - test_n_td_flatten_cycles: counts exit_reason='td_flatten' rows correctly from cycles DataFrame
    - test_dry_run_smoke: `python run_tds_sweep.py --dry-run --limit 3` prints configs without running simulator, exits 0
  </behavior>
  <action>
    Model `run_tds_sweep.py` directly on `run_sizing_sweep.py`. Line 1: `# archetype: rotational`. Key functions:

    1. `build_tds_grid()` -> list of dicts. Primary sweep: drawdown_budget_ticks=[25,50,100,200,500], velocity_threshold_sec=[30,60,120,300], consecutive_adds_threshold=[2,3,4,5], cooldown_sec=[60,180,300,600]. Fix Level 1 at spec defaults (step_widen_factor=1.5, max_levels_reduction=1), precursor at (precursor_min_signals=2, all thresholds=1.0), retracement_reset_pct=0.3. Grid: 5x4x4x4 = 320 combos.

    2. `build_run_config(profile_data, source_id, tds_combo, base_params)` -> dict. Assemble full config from profile martingale params + TDS combo. Use the exact config structure from RESEARCH.md "Full Config Assembly" code example. Read instrument constants from `_config/instruments.md` via `parse_instruments_md()`. max_contract_size=16 (fixed per Phase 02.1).

    3. `compute_tds_survival_metrics(result, tds, baseline_metrics, bars_df, max_levels, cost_ticks)` -> dict. Compute extended metrics via `compute_extended_metrics()`, get TDS summary via `tds.get_summary()`, compute deltas: worst_dd_reduction = baseline["worst_cycle_dd"] - extended["worst_cycle_dd"], max_level_pct_reduction = baseline["max_level_exposure_pct"] - extended["max_level_exposure_pct"], pnl_impact_ticks = extended["total_pnl_ticks"] - baseline["total_pnl_ticks"], tail_ratio_delta = extended["tail_ratio"] - baseline["tail_ratio"]. Count td_flatten cycles from result.cycles["exit_reason"]. Compute l3_recovery_bars_avg from td_flatten cycle durations.

    4. `load_profiles()` -> dict. Load all 3 profile JSONs from `profiles/` directory.

    5. `run_sweep(args)` -> None. Main loop: for each profile, for each bar_type in profile, load bars via `load_bars()`, run no-TDS baseline to get fresh baseline_metrics (do NOT trust profile JSON metrics — they were computed with different cost_ticks handling), then for each tds_combo: build config, run RotationalSimulator, compute survival metrics, append row. Write TSV and JSON to `tds_profiles/`.

    6. CLI: `--output-dir` (default tds_profiles/), `--config` (default rotational_params.json), `--dry-run` (print grid size + first N configs, exit), `--limit N` (limit combos for testing), `--profile` (run only one profile, default all).

    TSV columns: use TDS_TSV_COLUMNS from RESEARCH.md (identity + TDS params + core metrics + TDS triggers + survival deltas).

    IMPORTANT ANTI-PATTERNS TO AVOID:
    - Do NOT add deduplication logic. Every TDS combo is unique (Pitfall 3 from RESEARCH.md).
    - Do NOT sweep precursor thresholds beyond adverse_speed_threshold — H33/H38/H40 not active in baseline profiles (Pitfall 4).
    - Do NOT vary retracement_reset_pct — locked at 0.3 per Phase 03-01 decision.
    - ALWAYS verify sim._tds is not None after init (Pitfall 1).
    - Use period="P1a" only. NEVER target P2 data.

    For `test_tds_sweep.py`: Write TDD tests first. Use synthetic data for unit tests (small DataFrames with known values). The dry-run smoke test should invoke the script as a subprocess. Verify config assembly produces all required keys. Verify survival metric math with hand-computed values.
  </action>
  <verify>
    <automated>cd shared/archetypes/rotational && python -m pytest test_tds_sweep.py -x -q</automated>
  </verify>
  <done>
    run_tds_sweep.py exists with all 6 functions, test_tds_sweep.py has 5+ tests all passing, --dry-run mode works, tds_profiles/ directory exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute primary TDS sweep (2,880 runs) on P1a</name>
  <files>
    shared/archetypes/rotational/tds_profiles/tds_sweep_P1a.tsv,
    shared/archetypes/rotational/tds_profiles/tds_sweep_P1a.json
  </files>
  <action>
    Run the full TDS sweep:
    ```
    cd shared/archetypes/rotational && python run_tds_sweep.py
    ```

    Expected: 320 TDS combos x 3 profiles x 3 bar types = 2,880 rows in tds_sweep_P1a.tsv.

    After sweep completes, validate output:
    1. Verify row count: `wc -l tds_profiles/tds_sweep_P1a.tsv` should be ~2881 (header + 2880 rows). If some combos were skipped due to errors, the count may be lower — investigate any missing rows.
    2. Verify TDS is active: check that l1_triggers + l2_triggers + l3_triggers > 0 for at least 50% of rows. If most rows show 0 triggers, there is a wiring bug.
    3. Verify survival deltas are plausible: worst_dd_reduction should be > 0 for aggressive configs (small drawdown_budget_ticks), pnl_impact_ticks should be negative for aggressive configs (expected trade-off).
    4. Verify no P2 data was used: grep for "P2" in output and config.
    5. Run full test suite to confirm no regressions: `pytest shared/archetypes/rotational/ -x -q`

    If the sweep takes longer than 2 hours, consider running with `--limit 100` first to validate output format, then run the full sweep.

    IMPORTANT: If any runs produce errors (e.g., TDS not initializing), fix the harness code first, then re-run. Do not ship partial results.
  </action>
  <verify>
    <automated>cd shared/archetypes/rotational && python -c "import pandas as pd; df=pd.read_csv('tds_profiles/tds_sweep_P1a.tsv',sep='\t'); assert len(df)>=2800, f'Only {len(df)} rows'; assert (df['l1_triggers']+df['l2_triggers']+df['l3_triggers']).gt(0).mean()>0.3, 'TDS not firing'; print(f'OK: {len(df)} rows, TDS active in {(df[\"l1_triggers\"]+df[\"l2_triggers\"]+df[\"l3_triggers\"]).gt(0).mean()*100:.0f}% of runs')"</automated>
  </verify>
  <done>
    tds_sweep_P1a.tsv has ~2,880 rows with all TDS_TSV_COLUMNS populated, TDS triggers > 0 in majority of rows, survival deltas computed correctly, full test suite passes
  </done>
</task>

</tasks>

<verification>
1. `pytest shared/archetypes/rotational/test_tds_sweep.py -x -q` — all harness unit tests pass
2. `pytest shared/archetypes/rotational/ -x -q` — full suite passes (no regressions)
3. TSV has ~2,880 rows with correct columns
4. TDS is provably active (trigger counts > 0 for non-trivial configs)
5. Survival deltas are directionally correct (aggressive TDS = more DD reduction, more PnL loss)
</verification>

<success_criteria>
- run_tds_sweep.py produces reproducible sweep output
- tds_sweep_P1a.tsv contains all (profile x bar_type x tds_config) combinations
- Each row has both raw metrics and survival deltas vs profile baseline
- All automated tests pass
- Output ready for Plan 02 best-config selection
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-tds-profile-calibration/03.1-01-SUMMARY.md`
</output>
