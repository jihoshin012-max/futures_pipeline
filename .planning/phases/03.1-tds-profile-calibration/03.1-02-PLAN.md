---
phase: 03.1-tds-profile-calibration
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - shared/archetypes/rotational/run_tds_calibration.py
  - shared/archetypes/rotational/test_tds_calibration.py
  - shared/archetypes/rotational/tds_profiles/exp1_isolated_detectors.tsv
  - shared/archetypes/rotational/tds_profiles/exp2_drawdown_sweep.tsv
  - shared/archetypes/rotational/tds_profiles/exp3_combined.tsv
  - shared/archetypes/rotational/tds_profiles/best_tds_configs.json
  - shared/archetypes/rotational/tds_profiles/tds_calibration_report.md
autonomous: false
requirements: [TDS-CAL-01, TDS-CAL-02, TDS-CAL-03, TDS-CAL-04]

must_haves:
  truths:
    - "Experiment 1 identifies which individual detector provides the most survival improvement per profile per bar type"
    - "Experiment 2 identifies the best drawdown_budget_ticks threshold per profile per bar type"
    - "Experiment 3 verifies the best-L1 + best-L3 combination works and measures combined survival improvement"
    - "best_tds_configs.json contains the final calibrated TDS config per profile per bar type ready for Phase 04"
    - "Calibration report shows human-readable summary with per-experiment findings"
    - "Over-trigger configs (n_td_flatten_cycles > 5% of n_cycles) are flagged"
  artifacts:
    - path: "shared/archetypes/rotational/tds_profiles/exp1_isolated_detectors.tsv"
      provides: "36 rows: 4 detectors x 3 profiles x 3 bar types"
      contains: "profile\tsource_id"
    - path: "shared/archetypes/rotational/tds_profiles/exp2_drawdown_sweep.tsv"
      provides: "54 rows: 6 thresholds x 3 profiles x 3 bar types"
      contains: "drawdown_budget_ticks"
    - path: "shared/archetypes/rotational/tds_profiles/exp3_combined.tsv"
      provides: "9 rows: 1 combined config x 3 profiles x 3 bar types"
      contains: "combined"
    - path: "shared/archetypes/rotational/tds_profiles/best_tds_configs.json"
      provides: "Calibrated TDS configs per bar_type per profile"
      contains: "trend_defense"
    - path: "shared/archetypes/rotational/tds_profiles/tds_calibration_report.md"
      provides: "Human-readable calibration summary with 3-experiment analysis"
      contains: "worst_dd_reduction"
  key_links:
    - from: "run_tds_calibration.py"
      to: "tds_profiles/exp1_isolated_detectors.tsv"
      via: "experiment 1 output"
      pattern: "exp1.*tsv"
    - from: "tds_profiles/best_tds_configs.json"
      to: "profiles/*.json"
      via: "profile name reference"
      pattern: "MAX_PROFIT|SAFEST|MOST_CONSISTENT"
---

<objective>
Execute all 3 TDS calibration experiments (~99 runs), analyze results, select best configs, and produce calibrated TDS profiles.

Purpose: Transform hypothesis-driven experiment results into actionable TDS configurations per profile per bar type. Experiment 1 finds the best individual detector, Experiment 2 finds the best drawdown budget threshold, Experiment 3 verifies the combination. The final output is best_tds_configs.json consumed by Phase 04.

Output: 3 experiment TSVs, `best_tds_configs.json`, `tds_calibration_report.md`, selection logic in `run_tds_calibration.py`
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-tds-profile-calibration/03.1-RESEARCH.md
@.planning/phases/03.1-tds-profile-calibration/03.1-VALIDATION.md
@.planning/phases/03.1-tds-profile-calibration/03.1-01-SUMMARY.md

@shared/archetypes/rotational/run_tds_calibration.py
@shared/archetypes/rotational/test_tds_calibration.py
@shared/archetypes/rotational/trend_defense.py
@shared/archetypes/rotational/profiles/max_profit.json
@shared/archetypes/rotational/profiles/safest.json
@shared/archetypes/rotational/profiles/most_consistent.json

<interfaces>
<!-- From Plan 01 outputs — the harness API -->

From run_tds_calibration.py (built in Plan 01):
```python
def build_isolated_detector_configs() -> list[dict]:
    """Returns 4 TDS configs, one per detector (retracement, velocity, consecutive_adds, drawdown_budget)."""

def build_drawdown_sweep_configs(thresholds=[30, 40, 50, 60, 80, 100]) -> list[dict]:
    """Returns 6 TDS configs sweeping drawdown_budget_ticks."""

def build_combined_config(best_l1_detector: str, best_l3_threshold: float) -> dict:
    """Returns 1 TDS config combining winning L1 detector + winning L3 threshold."""

def build_run_config(profile_data, source_id, tds_config, base_params) -> dict:
    """Assembles full simulator config from profile + TDS config."""

def compute_survival_metrics(result, tds, baseline_metrics, bars_df, max_levels, cost_ticks) -> dict:
    """Returns dict with survival deltas vs no-TDS baseline."""

def run_experiment(experiment_num, args) -> pd.DataFrame:
    """Runs one experiment, returns results DataFrame."""
```

Expected TSV columns:
```
experiment, detector_name, profile, source_id,
step_widen_factor, max_levels_reduction,
velocity_threshold_sec, consecutive_adds_threshold,
drawdown_budget_ticks, cooldown_sec,
cycle_pf, n_cycles, win_rate, total_pnl_ticks,
worst_cycle_dd, max_level_exposure_pct, tail_ratio,
l1_triggers, l2_triggers, l3_triggers,
worst_dd_reduction, max_level_pct_reduction,
pnl_impact_ticks, tail_ratio_delta,
n_td_flatten_cycles, l3_recovery_bars_avg
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Experiments 1 and 2, analyze results, run Experiment 3, build selection and reporting</name>
  <files>
    shared/archetypes/rotational/run_tds_calibration.py,
    shared/archetypes/rotational/test_tds_calibration.py,
    shared/archetypes/rotational/tds_profiles/exp1_isolated_detectors.tsv,
    shared/archetypes/rotational/tds_profiles/exp2_drawdown_sweep.tsv,
    shared/archetypes/rotational/tds_profiles/exp3_combined.tsv,
    shared/archetypes/rotational/tds_profiles/best_tds_configs.json,
    shared/archetypes/rotational/tds_profiles/tds_calibration_report.md
  </files>
  <action>
    This task executes the 3 experiments sequentially (Exp 3 depends on results of Exp 1 + 2), analyzes results, and produces final calibrated configs.

    **Step 1: Run Experiment 1 (36 runs)**

    ```
    cd shared/archetypes/rotational && python run_tds_calibration.py --experiment 1
    ```

    Expected: 36 rows in `tds_profiles/exp1_isolated_detectors.tsv` (4 detectors x 3 profiles x 3 bar types).

    Validate:
    - Row count = 36
    - TDS triggers > 0 for at least some rows (if ALL zeros, TDS config is broken)
    - Check which detector produces the largest worst_dd_reduction per (profile, bar_type)
    - Note: Detector 4 (drawdown budget) will show L3 triggers (force_flatten behavior), not L1. This is expected.
    - Note: Detector 1 (retracement) may show low/zero triggers if retracement patterns are rare in P1a data. This is valid data.

    **Step 2: Run Experiment 2 (54 runs)**

    ```
    cd shared/archetypes/rotational && python run_tds_calibration.py --experiment 2
    ```

    Expected: 54 rows in `tds_profiles/exp2_drawdown_sweep.tsv` (6 thresholds x 3 profiles x 3 bar types).

    Validate:
    - Row count = 54
    - l3_triggers should increase as drawdown_budget_ticks decreases (tighter budget = more triggers)
    - Flag over-trigger configs: n_td_flatten_cycles > 5% of n_cycles
    - Check PnL impact: aggressive thresholds (25, 50) should show more PnL degradation

    **Step 3: Analyze Exp 1 + 2 results to determine winners**

    For Experiment 1 — per (profile, bar_type), rank detectors by worst_dd_reduction (positive = better). The best L1 detector is the one that produces the most drawdown reduction while keeping pnl_impact_ticks within the -20% guard.

    For Experiment 2 — per (profile, bar_type), rank thresholds by worst_dd_reduction. Apply PnL guard (exclude configs where pnl_impact_ticks < -0.20 * baseline total_pnl_ticks). The best L3 threshold is the one with highest worst_dd_reduction that passes the guard.

    IMPORTANT: Winners may differ across (profile, bar_type) combinations. That is expected and correct — each combination gets its own best config.

    **Step 4: Run Experiment 3 (9 runs)**

    Using the winners from Steps 3:

    ```
    cd shared/archetypes/rotational && python run_tds_calibration.py --experiment 3 --best-l1 <winner> --best-l3 <threshold>
    ```

    NOTE: If winners differ per (profile, bar_type), the harness should accept per-combination winners. If the harness CLI only accepts a single --best-l1 / --best-l3, either (a) run Experiment 3 multiple times with different args, or (b) add a `--winners-json` flag that reads a JSON mapping `{profile: {bar_type: {best_l1, best_l3}}}`. Approach (b) is preferred for automation.

    Actually, the simplest approach: after analyzing Exp 1+2, add a `select_and_run_exp3()` function to the harness that programmatically determines winners from the Exp 1+2 TSVs and runs Exp 3 with per-combination winners. This avoids manual CLI arg juggling. Add a `--auto-exp3` flag that chains: read exp1 + exp2 TSVs -> determine winners -> run exp3.

    Validate Experiment 3:
    - Row count = 9 (1 config x 3 profiles x 3 bar types) — actually may be up to 9 if winners differ per combination, still 9 rows total
    - Combined survival improvement should be >= the better of Exp 1 winner or Exp 2 winner alone (synergy expected)
    - If combined is WORSE than individual, flag as interference — the detectors may conflict

    **Step 5: Build selection logic and generate outputs**

    Add to `run_tds_calibration.py`:

    1. `select_best_configs(exp1_df, exp2_df, exp3_df)` -> dict. For each (profile, bar_type):
       a. Compare Exp 3 combined result vs best Exp 1 result vs best Exp 2 result
       b. Select the config that maximizes composite score: worst_dd_reduction + max_level_pct_reduction
       c. Apply PnL guard: exclude configs where pnl_impact_ticks < -0.20 * baseline total_pnl_ticks
       d. Flag over-trigger configs: n_td_flatten_cycles > 5% of n_cycles
       e. Build output TDS config dict

    2. `generate_calibration_report(exp1_df, exp2_df, exp3_df, best_configs)` -> str. Markdown report with:
       - Experiment 1 findings: per (profile, bar_type), which detector won, trigger counts, survival deltas
       - Experiment 2 findings: threshold curve showing worst_dd_reduction vs pnl_impact at each threshold
       - Experiment 3 findings: combined improvement vs individual, any interference detected
       - Final selection: best TDS config per (profile, bar_type) with full parameter listing
       - Over-trigger warnings and PnL guard exclusions

    3. `write_best_configs(best_configs, output_dir)` -> None. Write `best_tds_configs.json`:
       ```json
       {
         "calibration_method": "hypothesis_driven_3_experiments",
         "total_runs": 99,
         "MAX_PROFIT": {
           "bar_data_250vol_rot": {
             "trend_defense": { "enabled": true, "level_1": {...}, "level_2": {...}, "level_3": {...}, "precursor": {...} },
             "survival_deltas": { "worst_dd_reduction": N, "max_level_pct_reduction": N, "pnl_impact_ticks": N, ... },
             "experiment_source": "exp3_combined" | "exp1_velocity" | "exp2_threshold_100",
             "metadata": { "over_trigger": false, "pnl_guard_passed": true }
           }, ...
         },
         "no_tds_baselines": { ... }
       }
       ```

    4. CLI: Add `--select-best` flag that reads existing experiment TSVs, runs selection + report generation without re-running experiments. Also add `--auto-exp3` flag described above.

    Add TDD tests for selection logic:
    - test_select_best_prefers_combined: when Exp 3 combined has higher composite score than Exp 1/2 individual winners, it is selected
    - test_pnl_guard_excludes: configs exceeding -20% PnL impact are excluded even if survival improvement is high
    - test_over_trigger_flag: configs with n_td_flatten_cycles > 5% of n_cycles get flagged
    - test_output_json_schema: best_tds_configs.json has all 9 entries (3 profiles x 3 bar types) + baselines

    **Step 6: Validate all outputs**

    Run full test suite:
    ```
    cd shared/archetypes/rotational && python -m pytest test_tds_calibration.py -x -q
    cd shared/archetypes/rotational && python -m pytest . -x -q
    ```

    Validate TSV row counts: exp1=36, exp2=54, exp3=9 (or close if per-combination winners vary).

    Validate best_tds_configs.json has 9 entries.

    IMPORTANT: Selection is per-bar-type per-profile. Do NOT select a single TDS config across all bar types (Pitfall 5 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd shared/archetypes/rotational && python -m pytest test_tds_calibration.py -x -q && python -c "import pandas as pd; e1=pd.read_csv('tds_profiles/exp1_isolated_detectors.tsv',sep='\t'); e2=pd.read_csv('tds_profiles/exp2_drawdown_sweep.tsv',sep='\t'); e3=pd.read_csv('tds_profiles/exp3_combined.tsv',sep='\t'); assert len(e1)>=30, f'Exp1 only {len(e1)} rows'; assert len(e2)>=45, f'Exp2 only {len(e2)} rows'; assert len(e3)>=9, f'Exp3 only {len(e3)} rows'; import json; d=json.load(open('tds_profiles/best_tds_configs.json')); assert 'MAX_PROFIT' in d; assert 'SAFEST' in d; assert 'MOST_CONSISTENT' in d; print(f'OK: Exp1={len(e1)}, Exp2={len(e2)}, Exp3={len(e3)} rows, best_tds_configs.json valid')"</automated>
  </verify>
  <done>
    All 3 experiments executed (~99 total runs), best TDS config identified per bar_type per profile, best_tds_configs.json has 9 entries with survival deltas, tds_calibration_report.md summarizes 3-experiment findings, all tests pass, --select-best flag works
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of TDS calibration results</name>
  <files>
    shared/archetypes/rotational/tds_profiles/tds_calibration_report.md,
    shared/archetypes/rotational/tds_profiles/best_tds_configs.json
  </files>
  <action>
    Present calibration results for human review. Three hypothesis-driven TDS calibration experiments (~99 runs total) have been completed:
    - Experiment 1: Each of 4 detectors tested individually (36 runs)
    - Experiment 2: Drawdown budget threshold swept at 6 values (54 runs)
    - Experiment 3: Best L1 + best L3 combined (9 runs)
    Best TDS configuration selected per profile per bar type with PnL guard and over-trigger flagging.

    Human must verify:
    1. Read `tds_profiles/tds_calibration_report.md` — verify the 3-experiment structure makes domain sense
    2. Check Experiment 1 findings: does the winning detector per bar type make intuitive sense?
    3. Check Experiment 2 findings: does the threshold curve show expected behavior? (tighter budget = more triggers = more PnL loss = more DD reduction)
    4. Check Experiment 3: does the combination improve over individual detectors, or is there interference?
    5. Review `tds_profiles/best_tds_configs.json` — verify configs look reasonable for Phase 04 consumption
    6. Check for any over-trigger warnings or PnL guard exclusions that need human judgment
  </action>
  <verify>
    <automated>cd shared/archetypes/rotational && python -c "import json; d=json.load(open('tds_profiles/best_tds_configs.json')); profiles=['MAX_PROFIT','SAFEST','MOST_CONSISTENT']; bars=['bar_data_250vol_rot','bar_data_250tick_rot','bar_data_10sec_rot']; [d[p][b] for p in profiles for b in bars]; print('Schema OK: all 9 entries present')"</automated>
  </verify>
  <done>Human has reviewed tds_calibration_report.md and approved the calibrated TDS configs for Phase 04 use</done>
</task>

</tasks>

<verification>
1. `pytest shared/archetypes/rotational/test_tds_calibration.py -x -q` -- all tests pass including selection logic
2. `pytest shared/archetypes/rotational/ -x -q` -- full suite passes (no regressions)
3. exp1 TSV has ~36 rows, exp2 has ~54, exp3 has ~9 (total ~99)
4. best_tds_configs.json has 3 profiles x 3 bar_types = 9 entries + baselines
5. tds_calibration_report.md covers all 3 experiments with findings
6. Human review of calibration results (checkpoint)
</verification>

<success_criteria>
- All 3 experiments completed with ~99 total runs (NOT 2,880)
- Best L1 detector identified per profile per bar type (Experiment 1)
- Best L3 drawdown threshold identified per profile per bar type (Experiment 2)
- Combined config validated (Experiment 3)
- PnL guard applied (no config degrades PnL by > 20%)
- Over-trigger configs flagged
- best_tds_configs.json ready for Phase 04 consumption
- Human approves calibration results
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-tds-profile-calibration/03.1-02-SUMMARY.md`
</output>
