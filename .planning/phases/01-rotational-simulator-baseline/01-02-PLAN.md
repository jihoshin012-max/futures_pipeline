---
phase: 01-rotational-simulator-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - shared/archetypes/rotational/test_rotational_simulator.py
  - shared/archetypes/rotational/baseline_results/
autonomous: true
requirements:
  - ROT-SIM-05
  - ROT-SIM-06

must_haves:
  truths:
    - "Running the simulator twice with identical config+data produces identical output (determinism)"
    - "Baseline run (StepDist=2.0) completes on all 3 bar types using P1a data"
    - "Baseline metrics (cycle_pf, n_cycles, win_rate, total_pnl_ticks) are captured per bar type"
  artifacts:
    - path: "shared/archetypes/rotational/baseline_results/baseline_P1a.json"
      provides: "Baseline metrics for all 3 bar types at StepDist=2.0"
      contains: "per_source"
    - path: "shared/archetypes/rotational/test_rotational_simulator.py"
      provides: "Determinism verification test added"
      contains: "determinism"
  key_links:
    - from: "shared/archetypes/rotational/rotational_engine.py"
      to: "shared/archetypes/rotational/rotational_simulator.py"
      via: "engine dispatches to simulator for each bar type"
      pattern: "RotationalSimulator.*run"
---

<objective>
Verify simulator determinism (identical config+data -> identical output) and run the C++ defaults baseline (StepDist=2.0, MaxLevels=4) on all 3 bar types using P1a data. This establishes the raw baseline metrics that all Phase C hypotheses must beat.

Purpose: Determinism is a pipeline requirement (non-negotiable). The baseline run produces the reference performance numbers.
Output: Determinism test, baseline_P1a.json with per-bar-type metrics.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rotational-simulator-baseline/01-CONTEXT.md
@.planning/phases/01-rotational-simulator-baseline/01-01-SUMMARY.md
@shared/archetypes/rotational/simulation_rules.md
@shared/archetypes/rotational/rotational_engine.py
@shared/archetypes/rotational/rotational_params.json
@shared/archetypes/rotational/rotational_simulator.py

<interfaces>
From rotational_engine.py main():
```python
# CLI: python rotational_engine.py --config rotational_params.json --output result.json
# Engine loads config, loads bars, runs simulator per bar type, writes JSON
```

From rotational_params.json:
```json
{
  "period": "P1a",
  "bar_data_primary": {
    "bar_data_250vol_rot": "stages/01-data/data/bar_data/volume/NQ_BarData_250vol_rot_P1.csv",
    "bar_data_250tick_rot": "stages/01-data/data/bar_data/tick/NQ_BarData_250tick_rot_P1.csv",
    "bar_data_10sec_rot": "stages/01-data/data/bar_data/time/NQ_BarData_10sec_rot_P1.csv"
  }
}
```

SimulationResult contract (from Plan 01):
```python
@dataclass
class SimulationResult:
    trades: pd.DataFrame
    cycles: pd.DataFrame
    bars_processed: int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determinism verification test on real P1a data</name>
  <files>shared/archetypes/rotational/test_rotational_simulator.py</files>
  <action>
Add a determinism test to the existing test file (from Plan 01). This test uses REAL bar data (not synthetic) to verify that two consecutive runs produce identical results.

**Test implementation:**
1. Load one bar data file (use 250-vol as it's smallest). Use load_bars() from shared.data_loader.
2. Create a config dict matching rotational_params.json structure with period="P1a", step_dist=2.0, initial_qty=1, max_levels=4, max_contract_size=8, and _instrument={tick_size: 0.25, tick_value: 5.0, cost_ticks: 3}.
3. Run RotationalSimulator(config, bar_data).run() twice.
4. Assert: run1.bars_processed == run2.bars_processed
5. Assert: run1.trades.equals(run2.trades) (exact DataFrame equality)
6. Assert: run1.cycles.equals(run2.cycles) (exact DataFrame equality)
7. Assert bars_processed > 0 (sanity check that filtering worked)

Mark this test with `@pytest.mark.slow` since it loads real data.

**Path resolution:** The test file is in shared/archetypes/rotational/. Use Path(__file__).resolve().parents[3] to get repo root, then construct absolute paths to CSV files.

**Do NOT modify existing tests from Plan 01.** Only add new test functions.
  </action>
  <verify>
    <automated>cd C:/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_rotational_simulator.py -v -k "determinism" --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Determinism test passes: two runs with same config+data produce identical trades and cycles DataFrames</done>
</task>

<task type="auto">
  <name>Task 2: Run C++ defaults baseline (StepDist=2.0) on P1a, all 3 bar types</name>
  <files>shared/archetypes/rotational/baseline_results/baseline_P1a.json</files>
  <action>
Run the rotational engine with default config (StepDist=2.0, period=P1a) on all 3 bar types and capture results.

**Steps:**
1. Create directory: `shared/archetypes/rotational/baseline_results/`
2. Run the engine from repo root:
   ```
   cd C:/Projects/pipeline
   python shared/archetypes/rotational/rotational_engine.py \
     --config shared/archetypes/rotational/rotational_params.json \
     --output shared/archetypes/rotational/baseline_results/baseline_P1a.json
   ```
3. The config already has period="P1a" and all 3 bar types. The engine iterates all primary sources and writes per_source metrics.
4. Verify the output JSON contains per_source keys for all 3 bar types.
5. Print a summary table of key metrics per bar type: n_cycles, cycle_pf, win_rate, total_pnl_ticks, sharpe, max_drawdown_ticks, bars_processed.

**Expected output format (from rotational_engine.py lines 339-347):**
```json
{
  "archetype": "rotational",
  "period": "P1a",
  "bar_data_primary": ["bar_data_250vol_rot", "bar_data_250tick_rot", "bar_data_10sec_rot"],
  "per_source": {
    "bar_data_250vol_rot": { "cycle_pf": ..., "n_cycles": ..., ... },
    "bar_data_250tick_rot": { ... },
    "bar_data_10sec_rot": { ... }
  }
}
```

**Sanity checks after run:**
- All 3 bar types present in per_source
- n_cycles > 0 for each bar type
- bars_processed > 0 for each bar type
- 10-sec bars_processed should be significantly less than its raw count (477k) due to P1a date filter + RTH filter
- 250-vol and 250-tick bars_processed should be roughly half their raw counts (P1a = first half of P1)

If the engine errors, diagnose and fix the simulator (not the engine). The engine is already tested infrastructure.
  </action>
  <verify>
    <automated>cd C:/Projects/pipeline && python -c "import json; d=json.load(open('shared/archetypes/rotational/baseline_results/baseline_P1a.json')); ps=d['per_source']; assert len(ps)==3, f'Expected 3 bar types, got {len(ps)}'; [print(f'{k}: {v[\"n_cycles\"]} cycles, PF={v[\"cycle_pf\"]}, PnL={v[\"total_pnl_ticks\"]}t') for k,v in ps.items()]"</automated>
  </verify>
  <done>
    - baseline_P1a.json exists with metrics for all 3 bar types
    - Each bar type has n_cycles > 0 and bars_processed > 0
    - Metrics are reasonable (no NaN, no infinite values except possibly cycle_pf if no losers)
  </done>
</task>

</tasks>

<verification>
- Determinism test passes: `python -m pytest ... -k determinism` green
- baseline_P1a.json exists and contains per_source with 3 bar types
- All metrics are finite numbers (except cycle_pf which may be inf if no losing cycles)
- bars_processed counts are consistent with P1a date filtering
</verification>

<success_criteria>
- Determinism verified on real P1a data (two runs produce identical output)
- Baseline metrics captured for all 3 bar types at StepDist=2.0 on P1a
- Results written to baseline_results/baseline_P1a.json
- Metrics serve as reference for Phase C hypothesis comparison
</success_criteria>

<output>
After completion, create `.planning/phases/01-rotational-simulator-baseline/01-02-SUMMARY.md`
</output>
