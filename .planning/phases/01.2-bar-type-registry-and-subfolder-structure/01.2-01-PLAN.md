---
phase: 01.2-bar-type-registry-and-subfolder-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bar_type_registry.py
  - stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt
  - stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P2.txt
  - stages/01-data/data/bar_data/time/.gitkeep
  - stages/01-data/data/bar_data/tick/.gitkeep
  - _config/data_registry.md
  - stages/01-data/references/bar_data_volume_schema.md
  - stages/01-data/references/bar_data_schema.md
autonomous: true
requirements:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04

must_haves:
  truths:
    - "Bar data files live in typed subfolders (volume/) not flat bar_data/"
    - "data_registry.md uses bar_data_volume source_id, not bare bar_data"
    - "Schema file is named bar_data_volume_schema.md matching source_id convention"
    - "Placeholder subfolders (time/, tick/) exist for future bar types"
  artifacts:
    - path: "stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt"
      provides: "P1 bar data in typed subfolder"
    - path: "stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P2.txt"
      provides: "P2 bar data in typed subfolder"
    - path: "_config/data_registry.md"
      provides: "Typed source_id entries (bar_data_volume, bar_data_time, bar_data_tick)"
      contains: "bar_data_volume"
    - path: "stages/01-data/references/bar_data_volume_schema.md"
      provides: "Schema file with correct source_id naming convention"
      contains: "bar_data_volume"
    - path: "tests/test_bar_type_registry.py"
      provides: "Smoke tests for all phase behaviors"
      min_lines: 40
  key_links:
    - from: "_config/data_registry.md"
      to: "stages/01-data/references/bar_data_volume_schema.md"
      via: "source_id = schema filename convention"
      pattern: "bar_data_volume"
    - from: "_config/data_registry.md"
      to: "stages/01-data/data/bar_data/volume/"
      via: "source_id encodes bar type subfolder"
      pattern: "bar_data_volume.*NQ_BarData_250vol"
---

<objective>
Migrate bar data files into typed subfolders, update the data registry with typed source_ids, and rename the schema file to match the source_id naming convention.

Purpose: The backtest engine config (Phase 4) expects `bar_data/volume/` paths. Without this structural migration, all downstream phases referencing bar data paths will fail. The architecture doc explicitly specifies this layout.

Output: volume/ subfolder with bar files, typed registry entries, renamed schema, placeholder subfolders for future bar types.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-bar-type-registry-and-subfolder-structure/01.2-RESEARCH.md

@_config/data_registry.md
@stages/01-data/references/bar_data_schema.md
</context>

<interfaces>
<!-- Current data_registry.md row to replace -->
| bar_data | price | 1-min OHLCV bar data (tick bars) | P1, P2 | NQ_BarData_*.txt | 02-features, 04-backtest |

<!-- Current schema file to rename: stages/01-data/references/bar_data_schema.md -->
<!-- Naming convention established by: stages/01-data/references/zone_csv_v2_schema.md -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write smoke tests for bar type registry (Wave 0)</name>
  <files>tests/test_bar_type_registry.py</files>
  <action>
Create `tests/test_bar_type_registry.py` with 11 assertions covering all 6 phase behaviors. Use the exact test code from the RESEARCH.md Code Examples section (the `test_bar_type_registry.py` block). This file uses only `pathlib.Path` and `re` — no external dependencies, no mocking needed.

Tests to include:
- `test_volume_subfolder_exists` — `bar_data/volume/` is a directory
- `test_time_subfolder_exists` — `bar_data/time/` is a directory
- `test_tick_subfolder_exists` — `bar_data/tick/` is a directory
- `test_p1_bar_file_in_volume_subfolder` — at least one `*_P1.txt` in volume/
- `test_p2_bar_file_in_volume_subfolder` — at least one `*_P2.txt` in volume/
- `test_no_bar_files_in_root_bar_data` — no `.txt` files in flat `bar_data/`
- `test_registry_contains_bar_data_volume` — `bar_data_volume` string in registry
- `test_registry_does_not_contain_bare_bar_data_source_id` — regex `\|\s*bar_data\s*\|` must NOT match
- `test_bar_data_volume_schema_exists` — `bar_data_volume_schema.md` exists in references/
- `test_old_bar_data_schema_renamed` — `bar_data_schema.md` must NOT exist

Run tests — they MUST all fail (RED state, nothing migrated yet). This confirms the tests are actually checking real conditions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_bar_type_registry.py -x -q 2>&1 | head -5</automated>
  </verify>
  <done>All 10 tests exist and all fail (RED). No false greens.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate files, update registry, rename schema</name>
  <files>
    stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt
    stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P2.txt
    stages/01-data/data/bar_data/time/.gitkeep
    stages/01-data/data/bar_data/tick/.gitkeep
    _config/data_registry.md
    stages/01-data/references/bar_data_volume_schema.md
    stages/01-data/references/bar_data_schema.md
  </files>
  <action>
Three sub-steps, all filesystem + markdown edits:

**Step A — Create subfolders and move files:**
```bash
mkdir -p stages/01-data/data/bar_data/volume
mkdir -p stages/01-data/data/bar_data/time
mkdir -p stages/01-data/data/bar_data/tick
git mv stages/01-data/data/bar_data/NQ_BarData_250vol_P1.txt stages/01-data/data/bar_data/volume/
git mv stages/01-data/data/bar_data/NQ_BarData_250vol_P2.txt stages/01-data/data/bar_data/volume/
touch stages/01-data/data/bar_data/time/.gitkeep
touch stages/01-data/data/bar_data/tick/.gitkeep
```
Use `git mv` (not plain `mv`) to preserve file history.

**Step B — Update data_registry.md:**
Replace the single `bar_data` row with three typed rows. Keep the `zone_csv_v2` row unchanged. Updated Registered Sources table:

```
| source_id       | type    | description                                                  | periods | file_pattern              | required_by                |
|-----------------|---------|--------------------------------------------------------------|---------|---------------------------|----------------------------|
| bar_data_volume | price   | 250-vol OHLCV bars (250 contracts/bar; zone_touch archetype) | P1, P2  | NQ_BarData_250vol_*.txt   | 02-features, 04-backtest   |
| bar_data_time   | price   | 1-min time OHLCV bars (future strategies)                    | none    | NQ_BarData_1min_*.txt     | TBD                        |
| bar_data_tick   | price   | Tick OHLCV bars (future strategies)                          | none    | NQ_BarData_tick_*.txt     | TBD                        |
| zone_csv_v2     | touches | ZRA zone touch events (V4/ZRA, 32 cols)                      | P1, P2  | ZRA_Hist_*.csv            | 03-hypothesis, 04-backtest |
```

Also update the "To Add a New Source" section to clarify the bar_data subfolder convention:
- Step 1 should note: "For bar data: drop files into `01-data/data/bar_data/<bar_type>/`. For all other sources: `01-data/data/<source_id>/`."

**Step C — Rename and update schema file:**
```bash
git mv stages/01-data/references/bar_data_schema.md stages/01-data/references/bar_data_volume_schema.md
```
Then update the content of `bar_data_volume_schema.md`:
- Title: `# Bar Data Volume Schema`
- Source ID line: `# Source ID: bar_data_volume — must match data_registry.md entry.`
- Add bar type description: `# Bar type: Volume bars — each bar closes after exactly N contracts traded.`
- Add bar size note: `# Bar size (N=250 for NQ zone_touch): encoded in filename, not in this schema.`
- Fix the Resolution line: remove "1-minute bars required" (these are volume bars, not time bars). Replace with "Volume bars: each bar represents exactly N contracts traded. N is encoded in filename (e.g., 250vol)."
- Fix Notes section: change "NQ tick bars" to "NQ 250-vol bars" and add "Files live in stages/01-data/data/bar_data/volume/"

Do NOT hand-edit data_manifest.json (it is a generated artifact — Pitfall 1 from research).
  </action>
  <verify>
    <automated>python -m pytest tests/test_bar_type_registry.py -x -q && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 bar type registry tests pass (GREEN). All existing tests (test_scaffold_adapter.py, test_hmm_regime_fitter.py) still pass. No .txt files remain in flat bar_data/. data_registry.md contains bar_data_volume, bar_data_time, bar_data_tick. bar_data_volume_schema.md exists with accurate volume bar description.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_bar_type_registry.py -v` — all 10 tests green
2. `python -m pytest tests/ -v` — full suite green (no regressions)
3. `ls stages/01-data/data/bar_data/volume/` — shows both P1 and P2 files
4. `ls stages/01-data/data/bar_data/` — no .txt files at root level
5. `grep bar_data_volume _config/data_registry.md` — returns the typed row
6. `grep -P '\|\s*bar_data\s*\|' _config/data_registry.md` — returns nothing (bare source_id gone)
7. `ls stages/01-data/references/bar_data_volume_schema.md` — exists
8. `ls stages/01-data/references/bar_data_schema.md 2>/dev/null` — does not exist
</verification>

<success_criteria>
- Bar data files physically located at `stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P{1,2}.txt`
- No bar data files remain in flat `stages/01-data/data/bar_data/`
- `data_registry.md` has three typed rows (bar_data_volume, bar_data_time, bar_data_tick) and no bare `bar_data` row
- `bar_data_volume_schema.md` exists with correct source_id reference and volume bar description
- `bar_data_schema.md` no longer exists (renamed)
- `time/` and `tick/` placeholder subfolders exist
- All tests (new + existing) pass
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-bar-type-registry-and-subfolder-structure/01.2-01-SUMMARY.md`
</output>
