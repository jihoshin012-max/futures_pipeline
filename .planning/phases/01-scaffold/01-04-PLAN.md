---
phase: 01-scaffold
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - stages/01-data/CONTEXT.md
  - stages/01-data/references/bar_data_schema.md
  - stages/01-data/references/data_manifest_schema.md
  - stages/02-features/CONTEXT.md
  - stages/03-hypothesis/CONTEXT.md
  - stages/04-backtest/CONTEXT.md
  - shared/archetypes/zone_touch/exit_templates.md
autonomous: true
requirements: [SCAF-15, SCAF-16, SCAF-17, SCAF-18]

must_haves:
  truths:
    - "Stage 01 CONTEXT.md has validation checklist and data_manifest.json output contract"
    - "Stage 02 CONTEXT.md directs agent to edit feature_engine.py only with 300 experiment budget"
    - "Stage 03 CONTEXT.md directs agent to edit hypothesis_config.json with Rule 4 local repeat"
    - "Stage 04 CONTEXT.md directs agent to edit exit_params.json only with 500 experiment budget"
    - "exit_templates.md defines trail mechanics and optimization surface"
    - "Schema files exist for each registered data source in data_registry.md"
  artifacts:
    - path: "stages/01-data/CONTEXT.md"
      provides: "Stage 01 data validation contract"
      min_lines: 20
    - path: "stages/01-data/references/bar_data_schema.md"
      provides: "Bar data column contract"
      contains: "datetime"
    - path: "stages/02-features/CONTEXT.md"
      provides: "Stage 02 feature engineering contract"
      contains: "feature_engine.py"
    - path: "stages/03-hypothesis/CONTEXT.md"
      provides: "Stage 03 hypothesis generation contract"
      contains: "hypothesis_config.json"
    - path: "stages/04-backtest/CONTEXT.md"
      provides: "Stage 04 backtest simulation contract"
      contains: "exit_params.json"
    - path: "shared/archetypes/zone_touch/exit_templates.md"
      provides: "Exit structure patterns for zone_touch archetype"
      contains: "trail_steps"
  key_links:
    - from: "stages/01-data/CONTEXT.md"
      to: "stages/01-data/output/data_manifest.json"
      via: "Stage 01 produces data_manifest.json"
      pattern: "data_manifest.json"
    - from: "stages/04-backtest/CONTEXT.md"
      to: "shared/archetypes/zone_touch/exit_templates.md"
      via: "Stage 04 agent reads archetype exit templates"
      pattern: "exit_templates.md"
    - from: "stages/01-data/references/bar_data_schema.md"
      to: "_config/data_registry.md"
      via: "Schema filename matches source_id in registry"
      pattern: "bar_data"
---

<objective>
Create CONTEXT.md files for Stages 01-04 plus their reference docs (schema files, exit templates, data_manifest schema).

Purpose: Each stage CONTEXT.md is the runtime contract the agent reads. Stage 01 also needs schema reference files and the data_manifest.json schema spec. Stage 04 needs exit_templates.md for the zone_touch archetype.
Output: 4 stage CONTEXT.md files, schema reference files, exit_templates.md.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Futures_Pipeline_Functional_Spec.md (Tasks 1-14, 1-14b, 1-14c, 1-15, 1-16, 1-17, 1-17b)
@Futures_Pipeline_Architecture_ICM.md (data_manifest.json schema, strategy extensibility)

<interfaces>
<!-- From _config/data_registry.md (created in Plan 02): -->
<!-- source_id values registered there MUST match schema filenames here -->
<!-- e.g., source_id "bar_data" -> references/bar_data_schema.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Stage 01 CONTEXT.md and reference files</name>
  <files>stages/01-data/CONTEXT.md, stages/01-data/references/bar_data_schema.md, stages/01-data/references/data_manifest_schema.md</files>
  <action>
1. Write stages/01-data/CONTEXT.md (Task 1-14):
   - Front-matter: last_reviewed + reviewed_by
   - First 5 lines: "Stage 01: Data Foundation" + "YOUR TASK: Validate and register data. Do not modify any _config/ files."
   - I/O table: Inputs (raw source files), Process (validate schemas, check date coverage, discover bar offsets), Outputs (data_manifest.json, validation_report.md), Human checkpoint
   - CONSTRAINT section: no backtests, no _config/ changes, only outputs are data_manifest.json and validation_report.md
   - VALIDATION CHECKLIST: schema check, date coverage, row counts, bar offset, label columns, regime_labels.csv, data_manifest.json completeness
   - NEW DATA TYPES section: how to add schema docs and validation blocks
   - Must be <=80 lines

2. Write stages/01-data/references/bar_data_schema.md (Task 1-14b):
   - Front-matter with last_reviewed
   - Required columns: datetime (str), open (float), high (float), low (float), close (float), volume (int)
   - Resolution note: 1-minute bars required, gaps > 5 bars flagged
   - Bar offset note: validated in Stage 01

3. Create additional schema files for each source_id registered in _config/data_registry.md. At minimum, if touch/signal data was registered with a source_id (e.g., "zone_csv_v2"), create stages/01-data/references/{source_id}_schema.md. The filename MUST exactly match the source_id from data_registry.md. Use the schema template from the spec, populating columns based on what the actual touch data files contain (columns will need to be confirmed from the migrated data files).

4. Write stages/01-data/references/data_manifest_schema.md (Task 1-14c):
   Document the data_manifest.json schema. This is a reference doc (not the actual JSON file -- that is produced by Stage 01 validation). Include:
   - Full JSON structure: generated, periods (P1/P2 each with start, end, sources), bar_offset (verified, offset_bars, verified_date, method), validation_summary (status, warnings, errors)
   - Each source entry: path, row_count, schema_version, validation_status
   - How downstream stages consume it: feature_evaluator reads paths, hypothesis_generator date-slices, backtest_engine does NOT read it (takes explicit paths from caller per Q1 answer)
  </action>
  <verify>
    <automated>wc -l stages/01-data/CONTEXT.md && echo "---" && test -f stages/01-data/references/bar_data_schema.md && echo "bar_data_schema EXISTS" && echo "---" && test -f stages/01-data/references/data_manifest_schema.md && echo "manifest_schema EXISTS" && echo "---" && echo "Source-ID schema cross-check:" && for sid in $(grep -oP '(?<=source_id:\s)[\w]+' _config/data_registry.md 2>/dev/null || grep -oE 'source_id: [a-zA-Z0-9_]+' _config/data_registry.md 2>/dev/null | awk '{print $2}'); do test -f "stages/01-data/references/${sid}_schema.md" && echo "  ${sid}_schema.md EXISTS" || echo "  ${sid}_schema.md MISSING"; done</automated>
  </verify>
  <done>Stage 01 CONTEXT.md <=80 lines with validation checklist. bar_data_schema.md with 6 required columns. data_manifest_schema.md documenting the JSON schema. Additional source schema files matching data_registry source_ids -- each source_id has a corresponding {source_id}_schema.md in references/.</done>
</task>

<task type="auto">
  <name>Task 2: Create Stage 02-04 CONTEXT.md files and exit_templates.md</name>
  <files>stages/02-features/CONTEXT.md, stages/03-hypothesis/CONTEXT.md, stages/04-backtest/CONTEXT.md, shared/archetypes/zone_touch/exit_templates.md</files>
  <action>
1. Write stages/02-features/CONTEXT.md (Task 1-15):
   - Front-matter, <=80 lines
   - First 5 lines: "Stage 02: Feature Engineering" + "YOUR TASK: Edit feature_engine.py only. Metric: predictive spread on P1." + "CONSTRAINT: Do not touch evaluate_features.py (fixed harness)."
   - I/O table: You edit (feature_engine.py), Dispatcher (evaluate_features.py), Evaluator (feature_evaluator.py), Metric (spread), Keep rule (spread > threshold), Outputs (results.tsv, frozen_features.json)
   - ARCHETYPE REFERENCES: read program.md for active archetype
   - ENTRY-TIME RULE local repeat (Rule 3)
   - ITERATION BUDGET: 300 from statistical_gates.md
   - AFTER HUMAN PROMOTION: cp frozen_features.json to Stage 03

2. Write stages/03-hypothesis/CONTEXT.md (Task 1-16):
   - Front-matter, <=80 lines
   - First 5 lines: "Stage 03: Hypothesis Generation" + "YOUR TASK: Edit hypothesis_config.json only. Metric: P1 PF at 3t cost." + "CONSTRAINT: Do not touch hypothesis_generator.py (fixed harness)."
   - I/O table: You edit (hypothesis_config.json), Fixed harness (hypothesis_generator.py), Metric (P1 PF at 3t, min 30 trades), Keep rule (PF improves > 0.1), Outputs (results.tsv, promoted_hypotheses/), Reads (frozen_features.json, prior_results.md)
   - INTERNAL REPLICATION RULE local repeat (Rule 4)
   - ITERATION BUDGET: 200
   - PROGRAM: read program.md + prior_results.md

3. Write stages/04-backtest/CONTEXT.md (Task 1-17):
   - Front-matter, <=80 lines
   - First 5 lines: "Stage 04: Backtest Simulation" + "YOUR TASK: Edit exit params JSON only. Metric: P1 PF at 3t, min 30 trades." + "CONSTRAINT: Do NOT edit backtest_engine.py. It is a fixed engine."
   - I/O table: You edit (exit_params.json), Fixed engine (backtest_engine.py), Call signature, Metric (PF at 3t on P1), Keep rule (PF improves > 0.05)
   - ARCHETYPE REFERENCES: simulation_rules.md + exit_templates.md in shared/archetypes/{archetype}/
   - P2 HOLDOUT RULE local repeat (Rule 2)
   - ITERATION BUDGET: 500

4. Write shared/archetypes/zone_touch/exit_templates.md (Task 1-17b):
   - <=40 lines
   - Multi-leg partial exit pattern (Leg 1 smallest target, Leg 2 mid, Leg 3 runner; all share stop; BE trigger after first leg fills; trail after BE; time cap)
   - Single-leg exit pattern (one target, one stop, BE trigger via trail_steps[0] with new_stop_ticks=0, time cap)
   - Trail mechanics + BE unified: trail_steps list of {trigger_ticks, new_stop_ticks}, BE IS trail_steps[0] where new_stop_ticks=0, NO separate be_trigger_ticks field
   - Optimization surface: stop_ticks, targets, trail_steps (full array), time_cap_bars
   - FIXED: cost_ticks, score thresholds, filter configs, simulator_module
  </action>
  <verify>
    <automated>for f in stages/02-features/CONTEXT.md stages/03-hypothesis/CONTEXT.md stages/04-backtest/CONTEXT.md shared/archetypes/zone_touch/exit_templates.md; do echo "=== $f ==="; wc -l "$f" 2>/dev/null || echo "MISSING"; done</automated>
  </verify>
  <done>Stage 02 CONTEXT.md <=80 lines with feature_engine.py as only editable file and 300 budget. Stage 03 CONTEXT.md <=80 lines with Rule 4 local repeat and 200 budget. Stage 04 CONTEXT.md <=80 lines with holdout rule and 500 budget. exit_templates.md <=40 lines with trail mechanics and optimization surface.</done>
</task>

</tasks>

<verification>
- All 4 stage CONTEXT.md files <=80 lines
- Each has front-matter, operative instruction in first 5 lines, local rule repeat
- bar_data_schema.md and data_manifest_schema.md exist
- Schema filenames match source_ids from data_registry.md (verified by loop in Task 1 verify)
- exit_templates.md has trail_steps documentation and optimization surface
</verification>

<success_criteria>
SCAF-15 through SCAF-18 complete. Stages 01-04 have CONTEXT.md files with reference docs. exit_templates.md committed for zone_touch archetype.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-04-SUMMARY.md`
</output>
