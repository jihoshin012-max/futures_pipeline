---
phase: 05-stage-04-autoresearch
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - stages/04-backtest/autoresearch/results.tsv
autonomous: false
requirements: [AUTO-03]

must_haves:
  truths:
    - "50-experiment run completes without human intervention"
    - "results.tsv has 50 new rows (51 total including seeded) with monotonically increasing experiment IDs"
    - "driver stops when budget is reached (verified by setting budget=3)"
    - "A kept experiment's config change is visible in git history"
    - "A reverted experiment's config is identical to prior kept state"
  artifacts:
    - path: "stages/04-backtest/autoresearch/results.tsv"
      provides: "50 experiment rows plus seeded baseline"
      min_lines: 51
  key_links:
    - from: "stages/04-backtest/autoresearch/driver.py"
      to: "stages/04-backtest/autoresearch/results.tsv"
      via: "append row per experiment"
      pattern: "results\\.tsv"
---

<objective>
Run the Stage 04 overnight test: 50 experiments via driver.py, verifying the loop completes unattended, results.tsv is correctly populated, and budget enforcement works.

Purpose: Validates that the autoresearch loop is operationally sound before scaling to the full 500-experiment budget.
Output: results.tsv with 50+ rows, verified budget enforcement, human-confirmed operational status.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-stage-04-autoresearch/05-RESEARCH.md
@.planning/phases/05-stage-04-autoresearch/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Budget enforcement smoke test and 50-experiment run</name>
  <files>stages/04-backtest/autoresearch/results.tsv</files>
  <action>
    **Step 1: Budget enforcement smoke test**
    Temporarily set BUDGET in program.md to 3. Run:
    ```
    python stages/04-backtest/autoresearch/driver.py
    ```
    Verify it stops after exactly 3 experiments (results.tsv will have 4 rows: 1 seeded + 3 new).
    Count rows: `python -c "print(sum(1 for _ in open('stages/04-backtest/autoresearch/results.tsv'))-1)"`
    Assert row count == 4.

    **Step 2: Reset and run 50 experiments**
    Reset results.tsv to just header + seeded row (restore from git or re-seed).
    Set BUDGET in program.md to 50.
    Run driver.py and let it complete 50 experiments:
    ```
    python stages/04-backtest/autoresearch/driver.py
    ```
    This will take several minutes depending on engine speed. Monitor for completion.

    **Step 3: Verify results**
    - Count rows in results.tsv: should be 51 (header + seeded + 50 experiments)
    - Verify all 50 experiment rows have non-empty run_id, timestamp, verdict columns
    - Check at least one "kept" and at least one "reverted" verdict exists (if all 50 are reverted, the random proposer may need wider ranges — but this is acceptable for loop health)
    - Verify n_prior_tests column is monotonically non-decreasing across rows
    - Check no EXPERIMENT_ANOMALY entries in audit_log.md (all experiments should succeed with valid configs)

    **Step 4: Verify git integration**
    After the run (with autocommit.sh running or after manual commits):
    - `git log --oneline -20` should show auto: commits from the experiment runs
    - A kept experiment's exit_params.json change should be visible in commit history

    **Step 5: Restore program.md to production budget**
    Set BUDGET back to 500 in program.md for production use.
  </action>
  <verify>
    <automated>python -c "rows=open('stages/04-backtest/autoresearch/results.tsv').readlines(); data=[r for r in rows[1:] if r.strip()]; assert len(data)>=51, f'Expected >=51 data rows (1 seeded + 50 experiments), got {len(data)}'; verdicts=[r.split('\t')[15] for r in data[1:]]; assert all(v in ('kept','reverted') for v in verdicts), f'Invalid verdicts found: {set(verdicts)}'; print(f'OK: {len(data)} rows, {verdicts.count(\"kept\")} kept, {verdicts.count(\"reverted\")} reverted')"</automated>
  </verify>
  <done>50-experiment run completed; results.tsv has 51+ rows; budget enforcement verified at budget=3; all verdicts are kept or reverted</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of overnight loop operational status</name>
  <files>stages/04-backtest/autoresearch/results.tsv</files>
  <action>
    Human verifies the Stage 04 autoresearch loop is operational.

    What was built: driver.py running 50 unattended experiments against the fixed backtest engine, with keep/revert decisions logged to results.tsv and budget enforcement verified.

    Verification steps:
    1. Open stages/04-backtest/autoresearch/results.tsv — confirm 51+ rows, tab-delimited, with seeded + experiment rows
    2. Check verdict column: mix of "kept" and "reverted" entries (if all reverted, that is OK for uncalibrated model but note it)
    3. Check stages/04-backtest/autoresearch/current_best/exit_params.json — should reflect the last kept experiment's params (or seeded if all reverted)
    4. Run `git log --oneline -10` — confirm auto: commits captured experiment changes
    5. Verify stages/04-backtest/autoresearch/program.md has BUDGET: 500 (restored to production)
    6. Optional: run `python stages/04-backtest/autoresearch/driver.py` with BUDGET=2 to see it stop quickly
  </action>
  <verify>
    <automated>python -c "import csv; rows=list(csv.reader(open('stages/04-backtest/autoresearch/results.tsv'),delimiter='\t')); print(f'results.tsv: {len(rows)-1} data rows'); assert len(rows)>=52, f'Expected >=52 rows (header+seed+50), got {len(rows)}'"</automated>
  </verify>
  <done>Human confirms overnight loop is operational — results.tsv populated, budget enforcement works, git integration captures experiment changes</done>
</task>

</tasks>

<verification>
1. results.tsv row count >= 51 (header + seeded + 50 experiments)
2. Budget enforcement: budget=3 produced exactly 3+1 rows
3. All verdicts are "kept" or "reverted" (no anomalies for valid configs)
4. program.md BUDGET restored to 500
5. Human confirms loop is operational
</verification>

<success_criteria>
- 50 experiments completed without human intervention
- results.tsv correctly populated with monotonic experiment IDs
- Budget enforcement works (verified at budget=3)
- Keep/revert decisions visible in git history
- Human approves operational status
</success_criteria>

<output>
After completion, create `.planning/phases/05-stage-04-autoresearch/05-03-SUMMARY.md`
</output>
