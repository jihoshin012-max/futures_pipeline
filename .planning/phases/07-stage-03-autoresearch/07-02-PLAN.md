---
phase: 07-stage-03-autoresearch
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - stages/03-hypothesis/autoresearch/results.tsv
  - stages/03-hypothesis/autoresearch/result.json
  - stages/03-hypothesis/autoresearch/result_p1b.json
  - stages/03-hypothesis/autoresearch/hypothesis_config.json
autonomous: false
requirements:
  - AUTO-12

must_haves:
  truths:
    - "12-experiment smoke test completes without human intervention"
    - "results.tsv has 12 data rows with monotonically increasing n_prior_tests"
    - "replication_pass column is populated for every experiment that passed P1 keep rule"
    - "At least one experiment has replication_pass=True or replication_pass=False (not all empty)"
    - "Budget enforcement works: setting budget=3 stops driver after 3 experiments"
  artifacts:
    - path: "stages/03-hypothesis/autoresearch/results.tsv"
      provides: "12-row results with replication_pass column"
      contains: "replication_pass"
  key_links:
    - from: "stages/03-hypothesis/autoresearch/driver.py"
      to: "stages/03-hypothesis/autoresearch/hypothesis_generator.py"
      via: "subprocess call producing result.json and result_p1b.json"
      pattern: "hypothesis_generator"
    - from: "stages/03-hypothesis/autoresearch/hypothesis_generator.py"
      to: "stages/04-backtest/autoresearch/backtest_engine.py"
      via: "subprocess call for each engine run"
      pattern: "backtest_engine"
---

<objective>
Run a 12-experiment smoke test of the Stage 03 autoresearch loop to verify the full end-to-end pipeline: driver proposes parameters, hypothesis_generator runs P1 + P1b engine calls, replication enforcement works, results.tsv is populated with replication_pass column.

Purpose: Validates that the Stage 03 loop works against real data before any overnight run. Catches integration issues (data paths, engine compatibility, P1b date filtering, TSV format) that unit tests with mocks cannot detect.

Output: results.tsv with 12 rows, each containing P1 PF, verdict, and replication_pass values.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-stage-03-autoresearch/07-01-SUMMARY.md

@stages/03-hypothesis/autoresearch/driver.py
@stages/03-hypothesis/autoresearch/hypothesis_generator.py
@stages/03-hypothesis/CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run budget enforcement test, 12-experiment smoke test, and human verification</name>
  <files>
    stages/03-hypothesis/autoresearch/results.tsv,
    stages/03-hypothesis/autoresearch/result.json,
    stages/03-hypothesis/autoresearch/result_p1b.json,
    stages/03-hypothesis/autoresearch/hypothesis_config.json
  </files>
  <action>
    Claude runs both the budget test and the 12-experiment smoke test before presenting results for human verification.

    1. **Budget enforcement test (budget=3):**
       - Temporarily edit program.md to set BUDGET=3 (save original).
       - Run: `python stages/03-hypothesis/autoresearch/driver.py --n-experiments 5 --repo-root .`
       - Verify the driver stops after 3 experiments (not 5).
       - Restore program.md to BUDGET=200.
       - Delete the results.tsv from budget test (clean slate for smoke test).

    2. **12-experiment smoke test:**
       - Ensure current_best/hypothesis_config.json exists (seeded from Plan 01).
       - Run: `python stages/03-hypothesis/autoresearch/driver.py --n-experiments 12 --repo-root .`
       - Wait for completion (expect ~5-15 minutes depending on engine speed — each experiment runs engine twice: P1 + P1b).

    3. **Validate results.tsv:**
       - Verify exactly 12 data rows (excluding header).
       - Verify 25 tab-separated columns per row.
       - Verify `replication_pass` column (column 25) is populated for experiments where P1 passed keep rule.
       - Verify `n_prior_tests` column shows monotonically increasing values (0, 1, 2, ..., 11).
       - Verify `stage` column = '03-hypothesis' for all rows.
       - Verify at least one row has a non-empty `replication_pass` value (True or False).
       - Print summary: count of kept/reverted/p1b_replication_fail/kept_weak_replication verdicts.

    4. **If any experiment fails (ANOMALY):** Check audit_log.md for the EXPERIMENT_ANOMALY entry. Diagnose the error. Common issues:
       - Engine path resolution: hypothesis_generator.py must resolve `stages/04-backtest/autoresearch/backtest_engine.py` relative to repo root
       - P1b touches CSV path: must use the full P1 touches file and filter by date, not look for a separate P1b file
       - Config JSON schema: hypothesis_config.json must match exit_params.json schema exactly (engine validates)
       Fix the issue in hypothesis_generator.py or driver.py and re-run. Do NOT modify backtest_engine.py (HARD PROHIBITION).

    5. **Git commit the results:** Verify git log shows "auto: kept experiment" or "auto: stage-03" commits.

    Human verification steps:
    1. Open `stages/03-hypothesis/autoresearch/results.tsv` — verify 12 rows with realistic PF values and replication_pass column
    2. Check `git log --oneline -20` — verify "auto: kept experiment" commits from the smoke test
    3. Check `stages/03-hypothesis/autoresearch/current_best/hypothesis_config.json` — verify it reflects the last kept experiment's config (or original seed if all reverted)
    4. Verify no EXPERIMENT_ANOMALY entries in `audit/audit_log.md` from this run (or that any anomalies were resolved)
    5. Verify results make sense: PF values, n_trades, replication_pass True/False distribution
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
tsv = Path('stages/03-hypothesis/autoresearch/results.tsv')
lines = tsv.read_text(encoding='utf-8').splitlines()
header = lines[0].split('\t')
data = [l for l in lines[1:] if l.strip()]
assert len(data) == 12, f'Expected 12 rows, got {len(data)}'
assert len(header) == 25, f'Expected 25 columns, got {len(header)}'
assert header[-1] == 'replication_pass', f'Last column is {header[-1]}'
has_repl = any(row.split('\t')[-1].strip() in ('True','False') for row in data)
assert has_repl, 'No replication_pass values found'
print(f'PASS: 12 rows, 25 cols, replication_pass populated')
for row in data:
    cols = row.split('\t')
    print(f'  exp {cols[14]}: verdict={cols[15]}, repl_pass={cols[-1]}')
"
    </automated>
  </verify>
  <done>
    - 12-experiment smoke test completed without human intervention
    - results.tsv has exactly 12 data rows with 25 columns each
    - replication_pass column populated for experiments that passed P1 keep rule
    - Budget enforcement verified: budget=3 stopped at 3
    - No unresolved EXPERIMENT_ANOMALY entries
    - Human approved results
  </done>
</task>

</tasks>

<verification>
1. results.tsv has 12 rows with 25 columns
2. replication_pass column has at least one True or False value
3. Budget enforcement works (budget=3 stops at 3)
4. No regressions: `pytest tests/ -x -q` still green
</verification>

<success_criteria>
- 12-experiment smoke test completed end-to-end without human intervention
- results.tsv has correct 25-column format with replication_pass populated
- Budget enforcement verified independently
- Human approved results after visual inspection
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-03-autoresearch/07-02-SUMMARY.md`
</output>
