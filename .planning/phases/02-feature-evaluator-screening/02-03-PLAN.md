---
phase: 02-feature-evaluator-screening
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - shared/archetypes/rotational/run_phase1b_classification.py
  - shared/archetypes/rotational/screening_results/phase1b_classification.md
  - shared/archetypes/rotational/screening_results/phase1b_classification.json
  - shared/archetypes/rotational/test_phase1b_classification.py
autonomous: false
requirements: [ROT-RES-03]

must_haves:
  truths:
    - "Every hypothesis is classified into one of: ROBUST, ACTIVITY_DEPENDENT, SAMPLING_COUPLED, TIME_DEPENDENT, NO_SIGNAL"
    - "H37 is classified as ROBUST_ACTIVITY or SAMPLING_COUPLED or NO_SIGNAL (only vol+tick evaluated)"
    - "Classification is based on whether hypothesis beats the per-bar-type StepDist=6.0 baseline"
    - "Ranked advancement list produced with ADVANCE_HIGH, ADVANCE_FLAGGED, DO_NOT_ADVANCE decisions"
    - "Human reviews and confirms advancement decisions before Phase 3 proceeds"
  artifacts:
    - path: "shared/archetypes/rotational/run_phase1b_classification.py"
      provides: "Classification script that reads phase1_results.tsv and applies Section 3.7 framework"
      contains: "classify_hypothesis"
    - path: "shared/archetypes/rotational/screening_results/phase1b_classification.md"
      provides: "Human-readable ranked advancement list with notes"
    - path: "shared/archetypes/rotational/screening_results/phase1b_classification.json"
      provides: "Machine-readable classification results"
    - path: "shared/archetypes/rotational/test_phase1b_classification.py"
      provides: "Tests for classification logic correctness"
  key_links:
    - from: "shared/archetypes/rotational/run_phase1b_classification.py"
      to: "shared/archetypes/rotational/screening_results/phase1_results.tsv"
      via: "Reads experiment results for classification"
      pattern: "phase1_results.tsv"
    - from: "shared/archetypes/rotational/screening_results/phase1b_classification.md"
      to: "Phase 3 (TDS) and Phase 4 (Combinations)"
      via: "Advancement decisions determine which hypotheses enter combination testing"
      pattern: "ADVANCE_HIGH|ADVANCE_FLAGGED"
---

<objective>
Apply the Phase 1b cross-bar-type robustness classification framework and produce the ranked advancement list.

Purpose: Before combination testing (Phase 4), systematically classify each hypothesis by how consistently it improves on the baseline across the three bar types. This analytical step produces advancement decisions that determine which hypotheses enter combination testing and with what caveats.

Output: run_phase1b_classification.py script, phase1b_classification.md (human-readable ranked list), phase1b_classification.json (machine-readable), human verification checkpoint.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feature-evaluator-screening/02-RESEARCH.md
@.planning/phases/02-feature-evaluator-screening/02-02-SUMMARY.md
@shared/archetypes/rotational/screening_results/phase1_results.tsv
@xtra/Rotational_Archetype_Spec.md (Section 3.7 classification framework)

<interfaces>
<!-- Classification framework from spec Section 3.7 -->
```
Pattern                                          | Classification
All 3 bar types win                              | ROBUST
Vol + tick win, 10-sec fails                     | ACTIVITY_DEPENDENT
One activity-sampled type only                   | SAMPLING_COUPLED
10-sec only wins                                 | TIME_DEPENDENT
All 3 fail                                       | NO_SIGNAL

Advancement decisions:
ROBUST                -> ADVANCE_HIGH
ACTIVITY_DEPENDENT    -> ADVANCE_FLAGGED (requires session-filtered deployment)
TIME_DEPENDENT        -> ADVANCE_FLAGGED (cautious -- time-based filter only)
SAMPLING_COUPLED      -> DO_NOT_ADVANCE
NO_SIGNAL             -> DO_NOT_ADVANCE

H37 special case: only vol + tick evaluated.
  Both win  -> ROBUST_ACTIVITY
  One wins  -> SAMPLING_COUPLED
  Both fail -> NO_SIGNAL
```

<!-- phase1_results.tsv expected columns -->
```
hypothesis_id, hypothesis_name, dimension, source_id, cycle_pf, delta_pf,
total_pnl_ticks, delta_pnl, n_cycles, win_rate, sharpe, max_drawdown_ticks,
avg_winner_ticks, avg_loser_ticks, beats_baseline, feature_compute_sec,
simulator_sec, total_sec, classification
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Phase 1b classification script and generate results</name>
  <files>shared/archetypes/rotational/run_phase1b_classification.py, shared/archetypes/rotational/screening_results/phase1b_classification.md, shared/archetypes/rotational/screening_results/phase1b_classification.json, shared/archetypes/rotational/test_phase1b_classification.py</files>
  <behavior>
    - Test: classify_hypothesis(wins={"vol": True, "tick": True, "10sec": True}, h_id="H1") returns "ROBUST"
    - Test: classify_hypothesis(wins={"vol": True, "tick": True, "10sec": False}, h_id="H1") returns "ACTIVITY_DEPENDENT"
    - Test: classify_hypothesis(wins={"vol": True, "tick": False, "10sec": False}, h_id="H1") returns "SAMPLING_COUPLED"
    - Test: classify_hypothesis(wins={"vol": False, "tick": False, "10sec": True}, h_id="H1") returns "TIME_DEPENDENT"
    - Test: classify_hypothesis(wins={"vol": False, "tick": False, "10sec": False}, h_id="H1") returns "NO_SIGNAL"
    - Test: classify_hypothesis(wins={"vol": True, "tick": True, "10sec": None}, h_id="H37") returns "ROBUST_ACTIVITY"
    - Test: classify_hypothesis(wins={"vol": True, "tick": False, "10sec": None}, h_id="H37") returns "SAMPLING_COUPLED"
    - Test: classify_hypothesis(wins={"vol": False, "tick": False, "10sec": None}, h_id="H37") returns "NO_SIGNAL"
    - Test: advancement_decision("ROBUST") returns "ADVANCE_HIGH"
    - Test: advancement_decision("NO_SIGNAL") returns "DO_NOT_ADVANCE"
    - Test: load_results() correctly aggregates 3 bar-type results per hypothesis (122 rows -> 41 hypotheses)
  </behavior>
  <action>
Create `shared/archetypes/rotational/run_phase1b_classification.py` (line 1: `# archetype: rotational`):

1. **load_results(tsv_path) -> pd.DataFrame**: Load phase1_results.tsv

2. **classify_hypothesis(wins: dict, h_id: str) -> str**: Apply the classification matrix from spec Section 3.7:
   - H37 special case: only vol + tick. Both win -> ROBUST_ACTIVITY, one -> SAMPLING_COUPLED, none -> NO_SIGNAL
   - All 3 win -> ROBUST
   - Vol + tick win, 10sec fail -> ACTIVITY_DEPENDENT
   - One activity-sampled only -> SAMPLING_COUPLED
   - 10sec only -> TIME_DEPENDENT
   - All fail -> NO_SIGNAL

3. **advancement_decision(classification: str) -> str**:
   - ROBUST -> ADVANCE_HIGH
   - ROBUST_ACTIVITY -> ADVANCE_HIGH
   - ACTIVITY_DEPENDENT -> ADVANCE_FLAGGED
   - TIME_DEPENDENT -> ADVANCE_FLAGGED
   - SAMPLING_COUPLED -> DO_NOT_ADVANCE
   - NO_SIGNAL -> DO_NOT_ADVANCE

4. **classify_all(results_df) -> list[dict]**: For each hypothesis:
   - Aggregate results across 3 bar types (or 2 for H37)
   - Determine wins dict: `beats_baseline` column from TSV
   - Apply classify_hypothesis()
   - Apply advancement_decision()
   - Record: hypothesis_id, hypothesis_name, dimension, classification, advancement_decision, wins per bar type, best cycle_pf per bar type, delta_pf per bar type, notes

5. **write_classification_md(classifications, output_path)**: Generate human-readable markdown report:
   - Title: "Phase 1b Cross-Bar-Type Robustness Classification"
   - Summary table: count per classification category
   - Ranked list sorted by: ADVANCE_HIGH first (sorted by max delta_pf desc), then ADVANCE_FLAGGED (sorted by delta_pf desc), then DO_NOT_ADVANCE
   - Per-hypothesis detail: classification, wins pattern, metrics, notes
   - Cross-bar-type analysis questions from spec Section 3.7 (answered with data):
     a. Do Dimension A winners agree across bar types?
     b. Where do time-based filters add value?
     c. Cycle characteristics differences across bar types?

6. **write_classification_json(classifications, output_path)**: Machine-readable output matching CLASSIFICATION_SCHEMA from research.

7. **CLI entry point**: `python run_phase1b_classification.py [--results-tsv screening_results/phase1_results.tsv] [--output-dir screening_results/]`

8. Create `shared/archetypes/rotational/test_phase1b_classification.py`:
   - Test all classification patterns (8 tests covering all branches)
   - Test advancement_decision mapping
   - Test H37 special handling
   - Test classify_all with synthetic results DataFrame

9. Run the classification: `python run_phase1b_classification.py`

10. Review output and ensure classification makes sense given the raw results.
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_phase1b_classification.py -x -q</automated>
  </verify>
  <done>Classification script produces correct categorization for all win patterns. phase1b_classification.md and .json generated. All 41 hypotheses classified. Test coverage for all classification branches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of Phase 1b advancement decisions</name>
  <files>shared/archetypes/rotational/screening_results/phase1b_classification.md</files>
  <action>
Phase 1b cross-bar-type robustness classification of all 41 hypotheses has been generated. The classification determines which hypotheses advance to combination testing in Phase 4.

Human review steps:
1. Open `shared/archetypes/rotational/screening_results/phase1b_classification.md`
2. Review the summary table: how many ROBUST vs ACTIVITY_DEPENDENT vs NO_SIGNAL?
3. Review the ranked advancement list:
   - ADVANCE_HIGH hypotheses: Do these make sense as the strongest signals?
   - ADVANCE_FLAGGED hypotheses: Are the caveats reasonable?
   - DO_NOT_ADVANCE hypotheses: Anything surprising that should be reconsidered?
4. Check the cross-bar-type analysis section:
   - Do Dimension A winners agree across bar types?
   - Are there surprises in which hypotheses are time-dependent vs activity-dependent?
5. Open `shared/archetypes/rotational/screening_results/phase1_results.tsv` for raw numbers
6. Verify no hypothesis has suspiciously high delta_pf (could indicate a bug)
7. Confirm the advancement decisions align with your research intuition
  </action>
  <verify>Human types "approved" to confirm advancement list, or describes any hypotheses that should be reclassified or investigated further</verify>
  <done>Human has reviewed and approved (or revised) the Phase 1b advancement decisions. Advancement list is locked for Phase 4 consumption.</done>
</task>

</tasks>

<verification>
- All 41 hypotheses have a classification and advancement decision
- H37 classified using 2-bar-type framework (vol + tick only)
- Classification logic matches spec Section 3.7 exactly
- Ranked advancement list sorted correctly (ADVANCE_HIGH first)
- Human reviewed and approved advancement decisions
- Results ready for Phase 4 combination testing
</verification>

<success_criteria>
- phase1b_classification.md provides clear, ranked advancement list
- phase1b_classification.json is machine-readable for Phase 4 consumption
- All classification branches tested
- Human has reviewed and approved the advancement decisions
- Clear count of: N hypotheses advancing, M hypotheses flagged, K hypotheses dropped
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-evaluator-screening/02-03-SUMMARY.md`
</output>
