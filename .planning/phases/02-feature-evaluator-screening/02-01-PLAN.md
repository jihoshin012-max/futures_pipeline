---
phase: 02-feature-evaluator-screening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/archetypes/rotational/hypothesis_configs.py
  - shared/archetypes/rotational/feature_engine.py
  - shared/archetypes/rotational/feature_evaluator.py
  - shared/archetypes/rotational/rotational_simulator.py
  - shared/archetypes/rotational/test_feature_evaluator_rotational.py
  - shared/archetypes/rotational/test_hypothesis_configs.py
autonomous: true
requirements: [ROT-RES-01]

must_haves:
  truths:
    - "FeatureComputer.compute_static_features() dispatches to feature_engine based on hypothesis config"
    - "feature_engine.compute_hypothesis_features() returns vectorized columns for any of the 41 hypotheses"
    - "All computed features are entry-time safe (rolling windows, no future data)"
    - "Hypothesis config definitions exist for all 41 hypotheses with correct parameter grids"
    - "feature_evaluator.py supports all 3 outcome types: direction, reversal quality, add quality"
    - "rotational_feature_evaluator.py is importable via dynamic dispatch (Gap G-09)"
  artifacts:
    - path: "shared/archetypes/rotational/hypothesis_configs.py"
      provides: "All 41 hypothesis definitions with config patches and param grids"
      contains: "HYPOTHESIS_REGISTRY"
    - path: "shared/archetypes/rotational/feature_engine.py"
      provides: "Vectorized hypothesis feature computation"
      contains: "compute_hypothesis_features"
    - path: "shared/archetypes/rotational/feature_evaluator.py"
      provides: "Bar-level MWU evaluator with all 3 outcome types (direction, reversal quality, add quality)"
      contains: "evaluate"
    - path: "shared/archetypes/rotational/rotational_simulator.py"
      provides: "FeatureComputer dispatches to feature_engine for hypothesis features"
      contains: "compute_hypothesis_features"
    - path: "shared/archetypes/rotational/test_feature_evaluator_rotational.py"
      provides: "Tests for feature computation, entry-time safety, MWU spread, evaluator outcome types"
    - path: "shared/archetypes/rotational/test_hypothesis_configs.py"
      provides: "Tests for hypothesis config validity"
  key_links:
    - from: "shared/archetypes/rotational/rotational_simulator.py"
      to: "shared/archetypes/rotational/feature_engine.py"
      via: "FeatureComputer calls compute_hypothesis_features"
      pattern: "compute_hypothesis_features"
    - from: "shared/archetypes/rotational/hypothesis_configs.py"
      to: "shared/archetypes/rotational/rotational_params.json"
      via: "Config patches applied on top of base config"
      pattern: "deepcopy.*rotational_params"
    - from: "stages/02-features/autoresearch/evaluate_features.py"
      to: "shared/archetypes/rotational/feature_evaluator.py"
      via: "Dynamic dispatch loads rotational evaluator (Gap G-09)"
      pattern: "feature_evaluator"
---

<objective>
Build the hypothesis feature computation infrastructure, hypothesis config registry, and upgrade the feature evaluator for Phase 2 screening.

Purpose: Before executing 122 experiments, we need (1) the ability to compute hypothesis-specific features vectorized on bar DataFrames, (2) FeatureComputer in the simulator to dispatch to these feature computations, (3) a registry of all 41 hypothesis definitions with their config patches and parameter grids, and (4) the feature_evaluator.py upgraded for all 3 outcome types (direction, reversal quality, add quality) per Gap G-04/G-09. This is the tooling foundation that Plan 02's experiment runner will consume.

Output: hypothesis_configs.py with all 41 definitions, extended feature_engine.py with vectorized feature computation, extended FeatureComputer in rotational_simulator.py, upgraded feature_evaluator.py with 3 outcome types, and test coverage.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feature-evaluator-screening/02-RESEARCH.md
@shared/archetypes/rotational/rotational_simulator.py
@shared/archetypes/rotational/feature_engine.py
@shared/archetypes/rotational/feature_evaluator.py
@shared/archetypes/rotational/rotational_params.json
@shared/archetypes/rotational/run_sweep.py
@shared/archetypes/rotational/rotational_engine.py
@xtra/Rotational_Archetype_Spec.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From shared/archetypes/rotational/rotational_simulator.py:
```python
class FeatureComputer:
    def __init__(self, config: dict) -> None: ...
    def compute_static_features(self, bar_df: pd.DataFrame) -> pd.DataFrame:
        """Return bar_df with any additional feature columns appended.
        For the baseline, returns bar_df unchanged."""
        return bar_df

class SimulationResult:
    trades: pd.DataFrame
    cycles: pd.DataFrame
    bars_processed: int
```

From shared/archetypes/rotational/feature_engine.py:
```python
def compute_features(bar_df) -> dict:
    """Current stub -- empty dict. To be extended."""
    features = {}
    return features
```

From shared/archetypes/rotational/feature_evaluator.py:
```python
def evaluate(source_id: str = "bar_data_250vol_rot") -> dict:
    """Existing: direction outcome only. Needs upgrade for reversal_quality + add_quality."""
    # Uses _compute_direction_outcome, _compute_mwu_spread
    # Dynamic dispatch via _load_feature_engine() already exists
```

From shared/archetypes/rotational/rotational_params.json:
```json
{
  "hypothesis": {
    "trigger_mechanism": "fixed",
    "trigger_params": {"step_dist": 2.0},
    "symmetry": "symmetric",
    "symmetry_params": {},
    "active_filters": [],
    "filter_params": {},
    "structural_mods": [],
    "structural_params": {}
  }
}
```

From shared/archetypes/rotational/rotational_engine.py:
```python
def compute_cycle_metrics(cycles: pd.DataFrame, cost_ticks: float) -> dict:
    """Returns: cycle_pf, n_cycles, win_rate, total_pnl_ticks, max_drawdown_ticks,
    avg_cycle_duration_bars, sharpe, avg_winner_ticks, avg_loser_ticks,
    max_consecutive_losing_cycles"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build hypothesis config registry with all 41 hypothesis definitions</name>
  <files>shared/archetypes/rotational/hypothesis_configs.py, shared/archetypes/rotational/test_hypothesis_configs.py</files>
  <behavior>
    - Test: HYPOTHESIS_REGISTRY contains exactly 41 entries with keys H1..H41 (H3 has sub-variants H3a/H3b/H3c/H3d counted as one hypothesis)
    - Test: Each hypothesis entry has required fields: id, name, dimension (A-F), config_patch (dict), param_grid (list of dicts or empty), description
    - Test: H37 entry has `exclude_10sec: true` flag
    - Test: H19 entry has `requires_reference: true` flag
    - Test: Dimension A hypotheses (H1, H3, H8, H9, H10) have `trigger_mechanism` in their config_patch
    - Test: Dimension C filter hypotheses have `active_filters` in their config_patch
    - Test: Dimension counts are A=5, B=1, C=16, D=10, E=2, F=7 totaling 41
    - Test: get_screening_experiments() returns 122 entries (41 x 3 - 1 for H37/10sec)
    - Test: H19 experiments are marked with requires_reference=True
    - Test: build_experiment_config produces valid config with correct overrides
  </behavior>
  <action>
Create `shared/archetypes/rotational/hypothesis_configs.py` (line 1: `# archetype: rotational`):

1. Define `HYPOTHESIS_REGISTRY` dict mapping hypothesis ID (str) to hypothesis definition dict. Each definition:
   - `id`: str (e.g., "H1")
   - `name`: str (e.g., "ATR-scaled step")
   - `dimension`: str (A/B/C/D/E/F)
   - `config_patch`: dict of dotted key paths to values that override the base config
   - `param_grid`: list of dicts for parameter sweep. Empty list for hypotheses with no parameter sweep.
   - `default_params`: dict of the "most likely" single parameter combination for initial screening
   - `exclude_10sec`: bool (True only for H37)
   - `requires_reference`: bool (True only for H19)
   - `requires_dynamic_features`: bool (True for H17, H36, H39)
   - `computed_features`: list of str -- feature column names this hypothesis adds to bar_df

2. Populate all 41 hypotheses from spec Sections 3.1-3.6. The EXACT dimension breakdown (per spec, verified):
   - **Dimension A (5):** H1, H3 (with sub-variants H3a/H3b/H3c/H3d as param_grid entries), H8, H9, H10
   - **Dimension B (1):** H2
   - **Dimension C (16):** H4, H5, H6, H7, H11, H12, H16, H17, H33, H34, H35, H36, H37, H39, H40, H41
   - **Dimension D (10):** H13, H14, H15, H20, H21, H22, H23, H24, H25, H26
   - **Dimension E (2):** H18, H19
   - **Dimension F (7):** H27, H28, H29, H30, H31, H32, H38

   NOTE: H23 is in Dimension D ONLY (per spec Section 3.4 "Conditional adds"). It is NOT in Dimension C. The count is: A=5 + B=1 + C=16 + D=10 + E=2 + F=7 = 41.

   For `default_params`, pick the single "most likely" parameter value per hypothesis:
   - H1: multiplier=0.5
   - H3: H3a variant (stddev1)
   - H8: multiplier=0.75, lookback=50
   - H9: k=2.0, vwap_reset="session"
   - H10: threshold=2.0, lookback=100
   - H2: rev_add_ratio=1.5
   - (For C/D/E/F filters with thresholds, pick the middle value)

3. Add helper functions:
   - `get_hypothesis(h_id: str) -> dict` -- lookup by ID
   - `get_hypotheses_by_dimension(dim: str) -> list[dict]` -- filter by dimension
   - `build_experiment_config(base_config: dict, hypothesis: dict, params: dict | None = None) -> dict` -- deep-copy base_config and apply hypothesis config_patch + optional param overrides using dotted-path set. Returns ready-to-use config.
   - `get_screening_experiments() -> list[dict]` -- returns list of {hypothesis_id, source_id, params, requires_reference} for all 122 meaningful experiments (41 x 3 bar types, H37 excluded from bar_data_10sec_rot). H19 experiments must have `requires_reference=True`.

4. Create test file `shared/archetypes/rotational/test_hypothesis_configs.py`:
   - Test HYPOTHESIS_REGISTRY has 41 entries
   - Test dimension counts: A=5, B=1, C=16, D=10, E=2, F=7
   - Test required fields on each entry
   - Test H37 has exclude_10sec=True
   - Test H19 has requires_reference=True
   - Test H23 is in dimension D (NOT C)
   - Test get_screening_experiments() returns 122 entries
   - Test build_experiment_config produces valid config with correct overrides
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_hypothesis_configs.py -x -q</automated>
  </verify>
  <done>hypothesis_configs.py defines all 41 hypotheses with correct dimension assignments (C=16, D=10), config patches, and param grids. get_screening_experiments() produces 122 experiment definitions. H23 is in dimension D only. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend feature_engine with vectorized feature computation and FeatureComputer dispatch</name>
  <files>shared/archetypes/rotational/feature_engine.py, shared/archetypes/rotational/rotational_simulator.py, shared/archetypes/rotational/test_feature_evaluator_rotational.py</files>
  <behavior>
    - Test: compute_hypothesis_features(bar_df, hypothesis_config) returns DataFrame with added columns for H1 (atr_scaled_step), H8 (rolling_sd, sd_scaled_step), H27 (atr_roc), H28 (price_roc), H33 (price_speed)
    - Test: Computed rolling features have NaN for warmup period (entry-time safe)
    - Test: H9 VWAP feature computation is vectorized and session-aware
    - Test: baseline config (trigger_mechanism="fixed", no filters) still returns bar_df unchanged
    - Test: H1 config (trigger_mechanism="atr_scaled") adds "atr_scaled_step" column to bar_df
    - Test: H19 config returns NaN columns with SKIPPED_REFERENCE_REQUIRED note (single-source runner cannot load all 3 bar types)
    - Test: H17/H36/H39 (requires_dynamic_features=true) return NaN placeholder columns
    - Test: entry-time safety -- rolling features at bar index 5 with lookback=200 are NaN
    - Test: MWU spread computation from feature_evaluator.py works with synthetic data
  </behavior>
  <action>
1. Extend `shared/archetypes/rotational/feature_engine.py`:
   - Keep existing `compute_features()` for backward compat
   - Add `compute_hypothesis_features(bar_df: pd.DataFrame, hypothesis_config: dict) -> pd.DataFrame` that takes the full bar DataFrame and adds computed feature columns. ALL computations are vectorized using pandas rolling/pct_change/cumsum -- NO per-bar loops.
   - Implement feature computations for ALL hypotheses needing computed columns (from spec Section 6.3):
     - H1: `atr_scaled_step = multiplier * ATR` (ATR already in CSV col 35)
     - H8: `rolling_sd = Close.rolling(lookback).std()`, `sd_scaled_step = multiplier * rolling_sd`
     - H9: `vwap = (price * volume).cumsum() / volume.cumsum()` with session reset via date groupby
     - H10: `z_score = (Close - Close.rolling(lookback).mean()) / Close.rolling(lookback).std()`
     - H27-H32, H33-H35, H37-H38, H40-H41 as specified in spec Section 6.3
   - For H17, H36, H39 (requires_dynamic_features=True): add placeholder columns with NaN. These require simulator state and will be populated by the simulator in a future extension.
   - For H19 (requires_reference=True): add placeholder returning NaN columns. Log "SKIPPED_REFERENCE_REQUIRED" as these need all 3 bar types loaded simultaneously, which the single-source runner pattern cannot support. Document this limitation.

2. Modify `FeatureComputer.compute_static_features()` in `rotational_simulator.py`:
   - Check `self._config.get("hypothesis", {}).get("trigger_mechanism", "fixed")` -- if not "fixed", OR if `active_filters` is non-empty, OR if `structural_mods` is non-empty, import and call `feature_engine.compute_hypothesis_features(bar_df, self._config["hypothesis"])`.
   - Return the bar_df with added feature columns.
   - If trigger_mechanism == "fixed" and no filters/mods, return bar_df unchanged (baseline behavior preserved).
   IMPORTANT: Do NOT modify the simulation loop logic (run method), state machine methods (_seed, _reversal, _add), or TradeLogger. Only modify FeatureComputer.compute_static_features().

3. Create `shared/archetypes/rotational/test_feature_evaluator_rotational.py` (line 1: `# archetype: rotational`):
   - Test baseline config returns bar_df unchanged
   - Test H1/H8/H33 configs add expected columns
   - Test H19 returns NaN columns with skip note
   - Test H17/H36/H39 return NaN placeholder columns
   - Test entry-time safety
   - Test MWU spread with synthetic data
   - Use synthetic bar DataFrames (50-100 rows) with columns: Last, ATR, Time, datetime, Volume, Ask Volume, Bid Volume, StdDev_1_Top, StdDev_1_Bottom, StdDev_2_Top, StdDev_2_Bottom

4. Verify backward compatibility: run existing test suite to confirm baseline simulator behavior unchanged.
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_feature_evaluator_rotational.py shared/archetypes/rotational/test_rotational_simulator.py -x -q</automated>
  </verify>
  <done>feature_engine.py computes vectorized features for all hypotheses. FeatureComputer dispatches to feature_engine for non-baseline configs. H19 returns NaN with SKIPPED_REFERENCE_REQUIRED. H17/H36/H39 return NaN placeholders. Baseline behavior unchanged. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade feature_evaluator.py with reversal quality and add quality outcome types (Gap G-04)</name>
  <files>shared/archetypes/rotational/feature_evaluator.py, shared/archetypes/rotational/test_feature_evaluator_rotational.py</files>
  <action>
Upgrade `shared/archetypes/rotational/feature_evaluator.py` to support all 3 outcome types per spec Gap G-04:

1. **Outcome 1 (direction):** Already implemented as `_compute_direction_outcome()`. No changes needed.

2. **Add Outcome 2 (reversal quality):** `_compute_reversal_quality_outcome(bar_df, bar_idx, lookforward)`:
   - At bars where a reversal occurred (requires running simulator context or identifying reversal bars from cycle data), compute: was the reversal profitable within M bars?
   - Returns +1 if reversal led to net positive PnL within M bars, -1 if not, NaN if not a reversal bar.
   - For Phase 2 initial screening, this can be approximated: at each bar, compute if the next N bars moved in the opposite direction of the prior N bars (directional reversal quality proxy).

3. **Add Outcome 3 (add quality):** `_compute_add_quality_outcome(bar_df, bar_idx, lookforward)`:
   - At bars where an add occurred, did the position recover within M bars?
   - Returns +1 if price reverted toward anchor within M bars (add was justified), -1 if it continued against (add was a bad escalation).
   - Proxy: at each bar, compute if price mean-reverted within N bars.

4. **Add `outcome_type` parameter to evaluate():**
   - `evaluate(source_id, outcome_type="direction")` -- backward compatible
   - `outcome_type` in ("direction", "reversal_quality", "add_quality")
   - Each outcome type uses the same MWU spread framework but different outcome variable

5. **Ensure dynamic dispatch compatibility (Gap G-09):**
   - The existing `evaluate_features.py` dispatcher already loads `feature_evaluator.py` dynamically via archetype name. Verify this works by checking the import path matches what the dispatcher expects.
   - Add to test_feature_evaluator_rotational.py: test that `evaluate(source_id, outcome_type="reversal_quality")` returns valid dict structure, test that `evaluate(source_id, outcome_type="add_quality")` returns valid dict structure.

IMPORTANT: Do NOT break the existing `evaluate()` interface. The new `outcome_type` parameter must default to "direction" for backward compatibility.
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_feature_evaluator_rotational.py -x -q -k "outcome"</automated>
  </verify>
  <done>feature_evaluator.py supports all 3 outcome types (direction, reversal_quality, add_quality). evaluate() backward compatible with default outcome_type="direction". Dynamic dispatch via evaluate_features.py works. Tests pass for all 3 outcome types.</done>
</task>

</tasks>

<verification>
- All 46+ existing rotational simulator tests pass (no regression)
- New test files cover ROT-RES-01 requirements: feature computation, entry-time safety, MWU spread, 3 outcome types
- HYPOTHESIS_REGISTRY contains 41 entries matching spec Section 3 (C=16, D=10; H23 in D only)
- get_screening_experiments() returns exactly 122 experiments
- compute_hypothesis_features returns correct columns for sampled hypotheses
- feature_evaluator.py supports direction, reversal_quality, add_quality outcomes
- H19 gracefully returns NaN with SKIPPED_REFERENCE_REQUIRED (single-source limitation)
- H17/H36/H39 return NaN placeholders (require simulator state)
</verification>

<success_criteria>
- hypothesis_configs.py defines all 41 hypotheses with correct dimension assignments and config patches
- feature_engine.py computes vectorized features for all hypotheses needing computed columns
- FeatureComputer in rotational_simulator.py dispatches to feature_engine for non-baseline configs
- feature_evaluator.py upgraded with 3 outcome types per Gap G-04
- 122 experiment definitions generated (41 x 3 bar types, H37 excluded from 10sec)
- H19 documented as requiring multi-source loading (SKIPPED_REFERENCE_REQUIRED)
- All tests pass including existing test suite (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-evaluator-screening/02-01-SUMMARY.md`
</output>
