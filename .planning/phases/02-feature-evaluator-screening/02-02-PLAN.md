---
phase: 02-feature-evaluator-screening
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - shared/archetypes/rotational/run_hypothesis_screening.py
  - shared/archetypes/rotational/screening_results/phase1_results.tsv
  - shared/archetypes/rotational/screening_results/phase1_results_rth.tsv
  - shared/archetypes/rotational/test_hypothesis_screening.py
autonomous: true
requirements: [ROT-RES-02]

must_haves:
  truths:
    - "Hypothesis runner executes all 122 experiments (41 x 3 bar types, H37 excluded from 10sec)"
    - "Each experiment result includes cycle_pf, delta_pf vs baseline, and all standard cycle metrics"
    - "H37 is auto-skipped for bar_data_10sec_rot and recorded as N/A_10SEC"
    - "H19 experiments recorded as SKIPPED_REFERENCE_REQUIRED (cannot load all 3 bar types in single-source runner)"
    - "All experiments use StepDist=6.0 as the base config for Dimension C/D/E/F tests"
    - "Results TSV has consistent schema with one row per experiment"
    - "Both unfiltered and RTH-filtered runs are produced for Phase 1b cross-bar-type comparison"
    - "Tooling checkpoint completed: feature_compute vs simulator time profiled"
    - "Baseline schema validated: sweep_P1a.json has best_per_source with all 3 source IDs"
  artifacts:
    - path: "shared/archetypes/rotational/run_hypothesis_screening.py"
      provides: "Experiment runner for all 122 hypothesis x bar-type combinations"
      contains: "run_screening"
    - path: "shared/archetypes/rotational/screening_results/phase1_results.tsv"
      provides: "All 122 experiment results with metrics and baseline comparison (unfiltered)"
    - path: "shared/archetypes/rotational/screening_results/phase1_results_rth.tsv"
      provides: "RTH-filtered experiment results for Phase 1b cross-bar-type comparison"
    - path: "shared/archetypes/rotational/test_hypothesis_screening.py"
      provides: "Tests for runner correctness, H37 exclusion, H19 skip, TSV schema, baseline schema"
  key_links:
    - from: "shared/archetypes/rotational/run_hypothesis_screening.py"
      to: "shared/archetypes/rotational/hypothesis_configs.py"
      via: "get_screening_experiments(), build_experiment_config()"
      pattern: "get_screening_experiments|build_experiment_config"
    - from: "shared/archetypes/rotational/run_hypothesis_screening.py"
      to: "shared/archetypes/rotational/rotational_simulator.py"
      via: "RotationalSimulator(config, bar_data).run()"
      pattern: "RotationalSimulator"
    - from: "shared/archetypes/rotational/run_hypothesis_screening.py"
      to: "shared/archetypes/rotational/baseline_results/sweep_P1a.json"
      via: "Loads frozen baselines for delta comparison"
      pattern: "sweep_P1a.json"
---

<objective>
Execute the 122 Phase 1 independent screening experiments and record results.

Purpose: Run each of the 41 hypotheses independently against the StepDist=6.0 optimized baseline on all 3 bar types (excluding H37 on 10-sec, skipping H19 with REFERENCE_REQUIRED note). This produces the raw results that Plan 03 will classify for cross-bar-type robustness. Includes the tooling checkpoint to profile feature compute vs simulator time. Produces both unfiltered and RTH-filtered results for Phase 1b comparison consistency (per spec Section 3.7 and RESEARCH Pitfall 6).

Output: run_hypothesis_screening.py runner script, screening_results/phase1_results.tsv and phase1_results_rth.tsv with all experiment results, timing profile data.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feature-evaluator-screening/02-RESEARCH.md
@.planning/phases/02-feature-evaluator-screening/02-01-SUMMARY.md
@shared/archetypes/rotational/rotational_simulator.py
@shared/archetypes/rotational/rotational_engine.py
@shared/archetypes/rotational/run_sweep.py
@shared/archetypes/rotational/rotational_params.json
@shared/archetypes/rotational/baseline_results/sweep_P1a.json

<interfaces>
<!-- From Plan 01 outputs (hypothesis_configs.py) -->
```python
# Expected exports from hypothesis_configs.py (created in Plan 01)
HYPOTHESIS_REGISTRY: dict  # 41 entries, key=hypothesis_id
def get_hypothesis(h_id: str) -> dict
def get_hypotheses_by_dimension(dim: str) -> list[dict]
def build_experiment_config(base_config: dict, hypothesis: dict, params: dict | None = None) -> dict
def get_screening_experiments() -> list[dict]  # Returns 122 {hypothesis_id, source_id, params, requires_reference}
```

```python
# Expected from feature_engine.py (extended in Plan 01)
def compute_hypothesis_features(bar_df: pd.DataFrame, hypothesis_config: dict) -> pd.DataFrame
```

<!-- Existing interfaces -->
```python
# From rotational_engine.py
def compute_cycle_metrics(cycles: pd.DataFrame, cost_ticks: float) -> dict

# From rotational_simulator.py
class RotationalSimulator:
    def __init__(self, config: dict, bar_data: pd.DataFrame, reference_data=None): ...
    def run(self) -> SimulationResult: ...

# Baselines from sweep_P1a.json best_per_source (VALIDATED SCHEMA):
# Top-level key: "best_per_source" (dict)
# Sub-keys: "bar_data_250vol_rot", "bar_data_250tick_rot", "bar_data_10sec_rot"
# Each has: step_dist, cycle_pf, total_pnl_ticks, n_cycles, win_rate, sharpe, max_drawdown_ticks
# bar_data_250vol_rot: cycle_pf=0.6714, step_dist=6.0
# bar_data_250tick_rot: cycle_pf=0.7339, step_dist=6.0
# bar_data_10sec_rot: cycle_pf=0.7550, step_dist=6.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build hypothesis screening runner with baseline validation and RTH mode</name>
  <files>shared/archetypes/rotational/run_hypothesis_screening.py, shared/archetypes/rotational/test_hypothesis_screening.py</files>
  <behavior>
    - Test: load_baselines() validates sweep_P1a.json has "best_per_source" key with all 3 source IDs ("bar_data_250vol_rot", "bar_data_250tick_rot", "bar_data_10sec_rot"); raises ValueError if missing
    - Test: load_baselines() returns dict with 3 source_ids and correct cycle_pf values
    - Test: run_single_experiment() with H1 config on synthetic data returns dict with keys: hypothesis_id, source_id, cycle_pf, delta_pf, total_pnl_ticks, n_cycles, win_rate, sharpe, max_drawdown_ticks, feature_compute_sec, simulator_sec, total_sec, beats_baseline
    - Test: H37 on bar_data_10sec_rot returns result with classification="N/A_10SEC" and cycle_pf=None
    - Test: H37 on bar_data_250vol_rot runs normally (not excluded)
    - Test: H19 on any bar type returns result with classification="SKIPPED_REFERENCE_REQUIRED" and cycle_pf=None
    - Test: TSV output has expected header columns
    - Test: get_base_config() returns config with StepDist=6.0 (not 2.0) and period=P1a
    - Test: beats_baseline column is stored as boolean-parseable string ("True"/"False"), and verify script handles string comparison correctly
  </behavior>
  <action>
Create `shared/archetypes/rotational/run_hypothesis_screening.py` (line 1: `# archetype: rotational`), modeled on `run_sweep.py` pattern:

1. **load_baselines()**: Read `baseline_results/sweep_P1a.json`. PREFLIGHT VALIDATION: assert top-level key `best_per_source` exists, assert it contains all 3 source IDs: `bar_data_250vol_rot`, `bar_data_250tick_rot`, `bar_data_10sec_rot`. Raise ValueError with clear message if schema invalid. Extract and return dict mapping source_id to baseline metrics.

2. **get_base_config()**: Load `rotational_params.json`, set `period="P1a"`, set `hypothesis.trigger_params.step_dist=6.0` (optimized baseline, NOT 2.0 default).

3. **run_single_experiment(base_config, hypothesis, source_id, bar_data, instrument_info, baseline_metrics, force_rth=False) -> dict**:
   - If hypothesis has `exclude_10sec=True` and source_id contains "10sec": return skip result with classification="N/A_10SEC"
   - If hypothesis has `requires_reference=True`: return skip result with classification="SKIPPED_REFERENCE_REQUIRED" (single-source runner cannot load all 3 bar types simultaneously for H19 cross-bar-type features)
   - Deep-copy base_config via `build_experiment_config(base_config, hypothesis)`
   - If `force_rth=True`: set config flag to force RTH filtering on all bar types (not just 10sec). Implementation: add a `"force_rth": True` key to config that FeatureComputer/simulator reads. If the existing `_filter_bars` method only filters 10sec, apply RTH filter manually to bar_data before passing to simulator.
   - Time feature computation separately: measure `feature_compute_sec`
   - Time simulation: run `RotationalSimulator(config, bar_data).run()`, measure `simulator_sec`
   - Compute metrics via `compute_cycle_metrics()`
   - Compute `delta_pf = metrics["cycle_pf"] - baseline_metrics[source_id]["cycle_pf"]`
   - Store `beats_baseline` as Python bool (True/False). When writing to TSV, ensure it is written as "True"/"False" string.
   - Return result dict with all fields including timing

4. **run_screening(experiments, base_config, bar_data_dict, instrument_info, baselines, force_rth=False) -> list[dict]**:
   - Load bar data ONCE per bar type
   - If `force_rth=True`: pre-filter vol/tick bar data through RTH filter (09:30-16:00 ET) before passing to experiments. 10sec already RTH-filtered by simulator. This ensures cross-bar-type comparisons use the same time window (spec Section 3.7, RESEARCH Pitfall 6).
   - Iterate experiments, collect results

5. **write_results_tsv(results, output_path)**: Write TSV with columns:
   hypothesis_id, hypothesis_name, dimension, source_id, cycle_pf, delta_pf, total_pnl_ticks, delta_pnl, n_cycles, win_rate, sharpe, max_drawdown_ticks, avg_winner_ticks, avg_loser_ticks, beats_baseline, feature_compute_sec, simulator_sec, total_sec, classification

6. **print_timing_profile(results)**: Summarize feature_compute_sec vs simulator_sec.

7. **CLI entry point**: `python run_hypothesis_screening.py [--output-dir screening_results/] [--single H1] [--dry-run] [--rth-only]`
   - `--rth-only`: Run with force_rth=True, output to phase1_results_rth.tsv
   - Default (no --rth-only): Run unfiltered, output to phase1_results.tsv

8. Create `shared/archetypes/rotational/test_hypothesis_screening.py`:
   - Test load_baselines validates schema (best_per_source key exists, all 3 source IDs present)
   - Test load_baselines raises ValueError on missing key
   - Test get_base_config has StepDist=6.0
   - Test H37 exclusion on 10sec
   - Test H37 NOT excluded on vol/tick
   - Test H19 skipped with SKIPPED_REFERENCE_REQUIRED
   - Test run_single_experiment returns all required fields
   - Test TSV column headers match expected schema
   - Test beats_baseline stored as string-parseable boolean
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -m pytest shared/archetypes/rotational/test_hypothesis_screening.py -x -q</automated>
  </verify>
  <done>run_hypothesis_screening.py exists with baseline schema validation, H37 exclusion, H19 skip, force_rth mode, timing profiler. Tests pass for baselines loading, schema validation, H37/H19 handling, TSV schema, StepDist=6.0.</done>
</task>

<task type="auto">
  <name>Task 2: Execute full 122-experiment screening on P1a data (unfiltered + RTH-filtered)</name>
  <files>shared/archetypes/rotational/screening_results/phase1_results.tsv, shared/archetypes/rotational/screening_results/phase1_results_rth.tsv</files>
  <action>
Run the full hypothesis screening in both modes:

1. Create output directory: `shared/archetypes/rotational/screening_results/`

2. **Unfiltered run:** `cd shared/archetypes/rotational && python run_hypothesis_screening.py --output-dir screening_results/`
   - Produces `screening_results/phase1_results.tsv` with standalone metrics for each hypothesis

3. **RTH-filtered run:** `cd shared/archetypes/rotational && python run_hypothesis_screening.py --output-dir screening_results/ --rth-only`
   - Produces `screening_results/phase1_results_rth.tsv` with RTH-filtered metrics for cross-bar-type comparison
   - This ensures vol/tick bars are filtered to same RTH window as 10sec, making Phase 1b classification reliable (RESEARCH Pitfall 6)

4. After execution, verify both files:
   - Each TSV has 122 data rows + header
   - No experiments crashed (all rows have non-null cycle_pf except H37/10sec=N/A_10SEC, H19=SKIPPED_REFERENCE_REQUIRED)
   - Print timing profile
   - Print top-10 hypotheses by delta_pf for each bar type
   - Count how many hypotheses beat baseline on each bar type

5. If any experiments fail, diagnose and fix in feature_engine.py or hypothesis_configs.py. Do NOT modify rotational_simulator.py's simulation loop or rotational_engine.py.

6. Record timing profile in `screening_results/timing_profile.txt`.

IMPORTANT: All experiments run on P1a data only. The holdout_locked_P2.flag exists -- do NOT attempt P2 runs.

NOTE on experiment scale: Each hypothesis uses its `default_params` for initial screening. Full parameter sweeps come later.

NOTE on H19: Will be recorded as SKIPPED_REFERENCE_REQUIRED in the TSV. Phase 1b classification will handle this as a special case.
  </action>
  <verify>
    <automated>cd /c/Projects/pipeline && python -c "
import pandas as pd
# Check unfiltered results
df = pd.read_csv('shared/archetypes/rotational/screening_results/phase1_results.tsv', sep='\t')
assert len(df) == 122, f'Expected 122 rows, got {len(df)}'
assert 'cycle_pf' in df.columns
assert 'delta_pf' in df.columns
# Handle beats_baseline as string
bb = df['beats_baseline'].astype(str).str.strip().str.lower()
n_beat = (bb == 'true').sum()
# H37 on 10sec
h37_10sec = df[(df['hypothesis_id']=='H37') & (df['source_id']=='bar_data_10sec_rot')]
assert len(h37_10sec) == 1
assert h37_10sec.iloc[0]['classification'] == 'N/A_10SEC'
# H19 skipped
h19 = df[df['hypothesis_id']=='H19']
assert all(h19['classification'] == 'SKIPPED_REFERENCE_REQUIRED'), 'H19 should be SKIPPED_REFERENCE_REQUIRED'
# Check RTH results
df_rth = pd.read_csv('shared/archetypes/rotational/screening_results/phase1_results_rth.tsv', sep='\t')
assert len(df_rth) == 122, f'Expected 122 RTH rows, got {len(df_rth)}'
print(f'OK: {len(df)} unfiltered + {len(df_rth)} RTH-filtered experiments, {n_beat} beat baseline (unfiltered)')
"</automated>
  </verify>
  <done>phase1_results.tsv and phase1_results_rth.tsv each contain 122 experiment results. H37 on 10sec is marked N/A_10SEC. H19 is marked SKIPPED_REFERENCE_REQUIRED. RTH-filtered results available for Phase 1b cross-bar-type comparison. Timing profile shows feature_compute vs simulator breakdown. No P2 data was touched.</done>
</task>

</tasks>

<verification>
- phase1_results.tsv has exactly 122 rows (unfiltered, standalone metrics)
- phase1_results_rth.tsv has exactly 122 rows (RTH-filtered, for Phase 1b comparison)
- All cycle_pf values are numeric except H37/10sec (N/A_10SEC) and H19 (SKIPPED_REFERENCE_REQUIRED)
- Baseline comparison uses StepDist=6.0 baselines (vol=0.6714, tick=0.7339, 10sec=0.7550)
- Baseline schema validated before any experiments run
- beats_baseline column handles string/bool correctly
- Timing profile generated and tooling checkpoint addressed
- No P2 data accessed (holdout flag respected)
- All existing tests still pass
</verification>

<success_criteria>
- 122 experiments completed and recorded in both TSV files
- Each result has: hypothesis_id, source_id, cycle_pf, delta_pf, n_cycles, beats_baseline
- H37 excluded from 10-sec, recorded as N/A_10SEC
- H19 recorded as SKIPPED_REFERENCE_REQUIRED on all bar types
- RTH-filtered results produced for Phase 1b cross-bar-type consistency
- Timing profile shows feature_compute percentage of wall clock
- Results ready for Phase 1b classification (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-evaluator-screening/02-02-SUMMARY.md`
</output>
