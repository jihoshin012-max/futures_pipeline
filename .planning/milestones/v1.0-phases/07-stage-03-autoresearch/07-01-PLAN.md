---
phase: 07-stage-03-autoresearch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stages/03-hypothesis/autoresearch/hypothesis_generator.py
  - stages/03-hypothesis/autoresearch/driver.py
  - stages/03-hypothesis/autoresearch/program.md
  - stages/03-hypothesis/autoresearch/current_best/hypothesis_config.json
  - stages/03-hypothesis/references/frozen_features.json
  - tests/test_hypothesis_generator.py
  - tests/test_stage03_driver.py
autonomous: true
requirements:
  - AUTO-09
  - AUTO-10
  - AUTO-11

must_haves:
  truths:
    - "hypothesis_generator.py runs backtest_engine.py twice: once for full P1, once for P1b-filtered touches"
    - "driver.py enforces 200-experiment budget and stops at limit"
    - "driver.py reverts hypotheses that pass P1 but fail P1b replication when replication_gate=hard_block"
    - "driver.py flags weak replication when replication_gate=flag_and_review (current setting)"
    - "program.md parses correctly with METRIC=pf, KEEP RULE=0.1, BUDGET=200"
    - "current_best/hypothesis_config.json seeded from Stage 04 exit_params.json"
    - "results.tsv has replication_pass as column 25"
  artifacts:
    - path: "stages/03-hypothesis/autoresearch/hypothesis_generator.py"
      provides: "Fixed harness wrapping backtest_engine.py for P1 and P1b runs"
      min_lines: 60
    - path: "stages/03-hypothesis/autoresearch/driver.py"
      provides: "Keep/revert loop with replication enforcement and budget control"
      min_lines: 200
    - path: "stages/03-hypothesis/autoresearch/program.md"
      provides: "Machine-readable agent steering file"
      contains: "METRIC: pf"
    - path: "stages/03-hypothesis/autoresearch/current_best/hypothesis_config.json"
      provides: "Seeded baseline from Stage 04 current best"
      contains: "zone_touch"
    - path: "tests/test_hypothesis_generator.py"
      provides: "Unit tests for hypothesis generator"
      min_lines: 40
    - path: "tests/test_stage03_driver.py"
      provides: "Unit tests for Stage 03 driver"
      min_lines: 100
  key_links:
    - from: "stages/03-hypothesis/autoresearch/driver.py"
      to: "stages/03-hypothesis/autoresearch/hypothesis_generator.py"
      via: "subprocess call to hypothesis_generator.py"
      pattern: "subprocess\\.run.*hypothesis_generator"
    - from: "stages/03-hypothesis/autoresearch/hypothesis_generator.py"
      to: "stages/04-backtest/autoresearch/backtest_engine.py"
      via: "subprocess call for P1 and P1b engine runs"
      pattern: "subprocess\\.run.*backtest_engine"
    - from: "stages/03-hypothesis/autoresearch/driver.py"
      to: "_config/period_config.md"
      via: "reads replication_gate at startup"
      pattern: "replication_gate"
---

<objective>
Build the Stage 03 hypothesis autoresearch loop: hypothesis_generator.py (fixed harness running backtest_engine.py for P1 and P1b), driver.py (keep/revert loop with P1b replication enforcement and 200-experiment budget), program.md (machine-readable steering), and seeded baseline config.

Purpose: Implements the core Stage 03 autoresearch machinery. The hypothesis generator wraps the engine for dual P1/P1b runs (Rule 4 enforcement), while the driver owns the keep/revert decision, budget enforcement, and replication gating. This is the structural enforcement of Rule 4 (internal replication) that was previously only a documented convention.

Output: hypothesis_generator.py, driver.py, program.md, seeded hypothesis_config.json, frozen_features.json copied to references/, and comprehensive unit tests for both modules.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-stage-03-autoresearch/07-RESEARCH.md

@stages/04-backtest/autoresearch/driver.py
@stages/02-features/autoresearch/driver.py
@stages/03-hypothesis/CONTEXT.md
@stages/04-backtest/autoresearch/current_best/exit_params.json
@_config/period_config.md
@_config/statistical_gates.md
@tests/test_driver.py
@tests/test_stage02_driver.py

<interfaces>
<!-- Key contracts the executor needs from existing code -->

From stages/04-backtest/autoresearch/driver.py (TEMPLATE — adapt for Stage 03):
```python
def parse_program_md(path) -> dict:  # returns {metric, keep_rule, budget}
def _generate_run_id(archetype, timestamp, experiment_n) -> str:  # SHA1[:7]
def _git_commit(repo_root, files, message) -> str:  # non-fatal
def _count_tsv_rows(tsv_path) -> int:  # excludes header
def _append_tsv_row(tsv_path, row_values) -> None:  # creates header if missing
def _read_baseline_metric(results_tsv_path) -> float:  # last kept/seeded pf_p1
def propose_next_params(current_best_config, results_tsv_path) -> dict:  # random perturbation
def validate_trail_steps(steps) -> bool:  # trail step validation
def run_loop(autoresearch_dir, repo_root, max_iterations=None) -> None:
```

From stages/04-backtest/autoresearch/current_best/exit_params.json (SEED for hypothesis_config.json):
```json
{
  "version": "v1", "instrument": "NQ",
  "touches_csv": "stages/01-data/data/touches/ZRA_Hist_P1.csv",
  "bar_data": "stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt",
  "scoring_model_path": "shared/scoring_models/zone_touch_v1.json",
  "archetype": {"name": "zone_touch", "simulator_module": "zone_touch_simulator", "scoring_adapter": "BinnedScoringAdapter"},
  "active_modes": ["M1"],
  "routing": {"score_threshold": 0, "seq_limit": 3},
  "M1": {"stop_ticks": 174, "leg_targets": [60, 81, 249], "trail_steps": [...], "time_cap_bars": 85}
}
```

From _config/period_config.md:
- P1a = 2025-09-16 to 2025-10-31
- P1b = 2025-11-01 to 2025-12-14
- replication_gate: flag_and_review

From _config/statistical_gates.md:
- Stage 03 budget: 200 experiments
- Keep rule from CONTEXT.md: PF improves by > 0.1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hypothesis_generator.py, program.md, seed baseline config, and tests</name>
  <files>
    stages/03-hypothesis/autoresearch/hypothesis_generator.py,
    stages/03-hypothesis/autoresearch/program.md,
    stages/03-hypothesis/autoresearch/current_best/hypothesis_config.json,
    stages/03-hypothesis/references/frozen_features.json,
    tests/test_hypothesis_generator.py
  </files>
  <behavior>
    - TestP1bFilter: P1b date filter on a mock touches CSV with mixed P1a/P1b dates produces only rows in [2025-11-01, 2025-12-14] range
    - TestP1bFilter: Empty P1b result (all dates in P1a) raises ValueError with descriptive message
    - TestRunnerOutputs: Given a mock subprocess that writes result.json and result_p1b.json, calling run() produces both output files
    - TestRunnerOutputs: P1b config temp file uses the P1b-filtered touches CSV path, not the original
    - TestRunnerOutputs: Temp files are cleaned up after run() completes (both success and failure paths)
    - TestRunnerOutputs: Engine failure (non-zero exit) propagates as subprocess.CalledProcessError
  </behavior>
  <action>
    1. Copy `stages/02-features/output/frozen_features.json` to `stages/03-hypothesis/references/frozen_features.json` (if not already present).

    2. Copy `stages/04-backtest/autoresearch/current_best/exit_params.json` to `stages/03-hypothesis/autoresearch/current_best/hypothesis_config.json`. This is the seeded baseline — identical schema to exit_params.json per Research Pitfall 1 recommendation (no translation layer).

    3. Create `tests/test_hypothesis_generator.py` (RED phase):
       - Import pattern: add `stages/03-hypothesis/autoresearch` to sys.path, import `hypothesis_generator`
       - TestP1bFilter class: test `write_p1b_filtered_csv()` with a tmp CSV containing rows spanning P1a and P1b dates — verify only P1b rows remain. Test empty P1b result raises ValueError.
       - TestRunnerOutputs class: mock subprocess.run to write fake result.json/result_p1b.json. Verify run() produces both files, P1b config uses temp touches path, temp files cleaned up. Test engine failure path.

    4. Create `stages/03-hypothesis/autoresearch/hypothesis_generator.py` (GREEN phase):
       - Line 1: `# archetype: zone_touch` (per CLAUDE.md convention)
       - Read P1b dates from `_config/period_config.md` at module load using regex (pattern from Research: `re.search(r"P1b = (\d{4}-\d{2}-\d{2}) to (\d{4}-\d{2}-\d{2})")` on the computed split comment line). Fallback constants: P1B_START=2025-11-01, P1B_END=2025-12-14 with comment pointing to period_config.md as source of truth.
       - `write_p1b_filtered_csv(touches_csv_path: str) -> str`: reads CSV, filters DateTime to P1b range, writes temp file, returns path. Raises ValueError if empty.
       - `run(config_path: str, result_path: str, result_p1b_path: str, engine_path: str, repo_root: str) -> None`: (a) Run engine with original config for full P1 result. (b) Write P1b-filtered touches to temp CSV. (c) Write temp config JSON pointing to P1b touches. (d) Run engine with temp config for P1b result. (e) Clean up both temp files in finally block. Use `check=True` for engine calls — driver handles CalledProcessError as EXPERIMENT_ANOMALY.
       - `_REPO_ROOT = Path(__file__).resolve().parents[3]` (3 levels: autoresearch -> 03-hypothesis -> stages -> root)
       - CLI: `if __name__ == "__main__"` with argparse for --config, --output, --output-p1b flags

    5. Create `stages/03-hypothesis/autoresearch/program.md` (30 lines max):
       ```
       # Stage 03 Hypothesis Generation — Program
       # Max 30 lines. Machine-readable fields must stay at top.

       ## Direction
       Edit hypothesis_config.json to propose a new hypothesis variant.
       Each experiment: change ONE structural parameter (stop_ticks, leg_targets, trail_steps, or routing).
       Read references/frozen_features.json before each experiment — features are locked from Stage 02.
       Read references/prior_results.md to avoid repeating failed hypothesis structures.

       ## Machine-Readable Fields
       METRIC: pf
       KEEP RULE: 0.1
       BUDGET: 200

       ## Constraints
       - Only edit hypothesis_config.json (do not touch hypothesis_generator.py)
       - Do not modify features — frozen_features.json is locked from Stage 02
       - A hypothesis advancing to P2 must have passed P1b replication (Rule 4)
       - Budget: 200 experiments maximum (statistical_gates.md)
       ```

    6. Run tests — all must pass (GREEN).
  </action>
  <verify>
    <automated>pytest tests/test_hypothesis_generator.py -x -q</automated>
  </verify>
  <done>
    - hypothesis_generator.py exists with write_p1b_filtered_csv() and run() functions
    - program.md has METRIC/KEEP RULE/BUDGET machine-readable fields
    - current_best/hypothesis_config.json seeded from Stage 04 exit_params.json
    - frozen_features.json copied to references/
    - All tests in test_hypothesis_generator.py pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Stage 03 driver.py with replication enforcement and tests</name>
  <files>
    stages/03-hypothesis/autoresearch/driver.py,
    tests/test_stage03_driver.py
  </files>
  <behavior>
    - test_parse_program_md: program.md with METRIC=pf, KEEP RULE=0.1, BUDGET=200 parses correctly
    - TestBudgetEnforcement: driver stops when n_prior_tests >= budget (set budget=3, pre-populate 3 rows, verify loop exits immediately)
    - TestBudgetEnforcement: driver runs exactly budget experiments when starting from 0 (budget=3 -> 3 experiments)
    - TestReplicationEnforcement::test_p1b_fail_reverts: hypothesis passes P1 (PF improvement > 0.1) but P1b PF < 1.0 -> reverted with verdict='p1b_replication_fail' when replication_gate='hard_block'
    - TestReplicationEnforcement::test_p1b_pass_keeps: hypothesis passes P1 and P1b PF > 1.0 with n_trades >= 10 -> kept with replication_pass=True in TSV row
    - TestReplicationEnforcement::test_weak_replication_flagged: P1 passes but P1b fails with replication_gate='flag_and_review' -> verdict='kept_weak_replication', replication_pass=False in TSV
    - TestReplicationEnforcement::test_p1_fail_skips_p1b: hypothesis fails P1 keep rule -> reverted immediately, replication_pass='' (not checked)
    - TestTsvLayout: results.tsv has 25 columns with replication_pass as column 25
  </behavior>
  <action>
    1. Create `tests/test_stage03_driver.py` (RED phase):
       - Follow test_driver.py pattern: tmp_path isolation, mock subprocess, helper to build autoresearch dir
       - Import from `stages/03-hypothesis/autoresearch/driver` (add to sys.path)
       - Helper `_make_autoresearch_dir(tmp_path)`: creates autoresearch dir with program.md, current_best/hypothesis_config.json (copy of SEEDED_CONFIG constant), results.tsv with seeded row
       - test_parse_program_md: parse the created program.md, verify metric='pf', keep_rule=0.1, budget=200
       - TestBudgetEnforcement: mock subprocess to return valid result.json + result_p1b.json. Pre-populate results.tsv with rows to test budget exhaustion.
       - TestReplicationEnforcement: 4 test methods as described in behavior. Mock subprocess to return different PF/n_trades values for P1 and P1b. Verify verdict column and replication_pass column in TSV.
       - TestTsvLayout: run 1 experiment, read results.tsv, verify 25 tab-separated columns, header includes 'replication_pass' as last column.

    2. Create `stages/03-hypothesis/autoresearch/driver.py` (GREEN phase):
       - Line 1: `# archetype: zone_touch`
       - Adapt from Stage 04 driver.py with these changes:
         (a) **Import hypothesis_generator** module (importlib.util.spec_from_file_location pattern from Stage 02 — OR simply subprocess call like Stage 04 calls engine). Use subprocess call to hypothesis_generator.py (keeps same isolation pattern as Stage 04).
         (b) **parse_program_md**: identical to Stage 04 (3 fields: metric, keep_rule, budget). Stage 03 does NOT need NEW_FEATURE field (that's Stage 02 only).
         (c) **propose_next_params**: identical to Stage 04 — random perturbation of CANDIDATE fields (stop_ticks, leg_targets, trail_steps, time_cap_bars). validate_trail_steps identical.
         (d) **TSV_HEADER**: 25 columns — add `\treplication_pass` after `notes`. This is Stage 03-specific; does not affect Stage 02/04 TSV headers.
         (e) **read_replication_gate(repo_root)**: read from `_config/period_config.md` using regex `r"^replication_gate:\s*(\S+)"`. Default: "flag_and_review".
         (f) **Replication constants**: `REPLICATION_PF_THRESHOLD = 1.0`, `REPLICATION_MIN_TRADES = 10`
         (g) **run_loop changes**: After engine call via hypothesis_generator.py subprocess, read result.json for P1 metrics. If P1 passes keep rule (PF > current_best + keep_rule AND n_trades >= 30): read result_p1b.json, compute `replication_pass = (pf_p1b > 1.0) and (n_trades_p1b >= 10)`. Apply replication_gate logic:
           - hard_block + fail: verdict='p1b_replication_fail', revert
           - flag_and_review + fail: verdict='kept_weak_replication', keep but flag
           - pass: verdict='kept'
           If P1 fails keep rule: verdict='reverted', replication_pass='' (not checked)
         (h) **TSV row**: 25 values. Column 25 = replication_pass (True/False/empty string). Notes column includes pf_p1b value when replication was checked: `replication_pass:{bool}|pf_p1b:{value}|git:{hash}`
         (i) **_log_experiment_anomaly**: stage='03-hypothesis' (not '04-backtest')
         (j) **Subprocess call**: `[sys.executable, str(generator_path), "--config", str(config_path), "--output", str(result_json_path), "--output-p1b", str(result_p1b_path)]`
         (k) **current_best path**: `current_best/hypothesis_config.json` (not exit_params.json)
         (l) **config path**: `hypothesis_config.json` (the agent-editable file)
         (m) **_read_hypothesis_name**: read from `promoted_hypothesis.json` if exists, fallback to archetype name
         (n) **Lockfile**: `.autoresearch_running` same as Stage 04
         (o) **CLI**: argparse with --autoresearch-dir, --repo-root, --n-experiments (optional hard cap for testing)

    3. Run tests — all must pass (GREEN).
  </action>
  <verify>
    <automated>pytest tests/test_stage03_driver.py -x -q</automated>
  </verify>
  <done>
    - driver.py exists with run_loop(), propose_next_params(), replication enforcement logic
    - Budget enforcement stops at 200 experiments
    - P1b replication fail with hard_block reverts the hypothesis
    - P1b replication fail with flag_and_review keeps but flags as weak_replication
    - results.tsv has 25 columns with replication_pass as column 25
    - All tests in test_stage03_driver.py pass
    - Full existing test suite still passes: `pytest tests/ -x -q`
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pytest tests/test_hypothesis_generator.py tests/test_stage03_driver.py -x -q` — all Stage 03 tests pass
2. `pytest tests/ -x -q` — full suite green (no regressions)
3. `python -c "import json; c=json.load(open('stages/03-hypothesis/autoresearch/current_best/hypothesis_config.json')); assert c['archetype']['name']=='zone_touch'"` — seeded config valid
4. `python -c "import json; f=json.load(open('stages/03-hypothesis/references/frozen_features.json')); assert 'zone_width' in f['features']"` — frozen features copied
</verification>

<success_criteria>
- hypothesis_generator.py wraps backtest_engine.py for P1 + P1b dual runs without modifying the engine
- driver.py enforces 200-experiment budget, P1b replication gating (hard_block/flag_and_review), and standard keep/revert logic
- program.md parses with METRIC=pf, KEEP RULE=0.1, BUDGET=200
- current_best/hypothesis_config.json seeded from Stage 04 current best
- frozen_features.json present in references/
- results.tsv uses 25-column format with replication_pass column
- All unit tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-03-autoresearch/07-01-SUMMARY.md`
</output>
