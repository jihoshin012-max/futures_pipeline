---
phase: 01.2-bar-type-registry-and-subfolder-structure
plan: "01"
subsystem: data
tags: [bar-data, data-registry, subfolder-structure, schema, filesystem]

# Dependency graph
requires:
  - phase: 01-scaffold
    provides: data_registry.md with initial bar_data entry and zone_csv_v2_schema.md naming convention
provides:
  - Bar data physically located at stages/01-data/data/bar_data/volume/
  - Typed source_ids in data_registry.md (bar_data_volume, bar_data_time, bar_data_tick)
  - bar_data_volume_schema.md with accurate volume bar description
  - Placeholder subfolders for future bar types (time/, tick/)
  - Smoke test suite for all phase behaviors (tests/test_bar_type_registry.py)
affects:
  - 02-features (consumes bar_data_volume source_id for feature engineering)
  - 04-backtest (engine config expects bar_data/volume/ path — was blocked without this)
  - Stage 01 validation (data_manifest.json generation uses bar_data_volume as source_id key)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - source_id = schema filename convention (bar_data_volume -> bar_data_volume_schema.md)
    - bar data subfolder convention (bar_data/<bar_type>/ vs <source_id>/ for other sources)

key-files:
  created:
    - tests/test_bar_type_registry.py
    - stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt
    - stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P2.txt
    - stages/01-data/data/bar_data/time/.gitkeep
    - stages/01-data/data/bar_data/tick/.gitkeep
    - stages/01-data/references/bar_data_volume_schema.md
  modified:
    - _config/data_registry.md
  deleted:
    - stages/01-data/references/bar_data_schema.md

key-decisions:
  - "bar_data_volume source_id used (not bar_data) — encodes bar type, matches schema filename convention, aligns with architecture doc registry rows"
  - "bar_data_time and bar_data_tick registered as placeholder rows with periods=none — documents intent without claiming coverage, avoids Stage 01 validation false failures"
  - "bar_data subfolder convention differs from other sources: bar_data/<bar_type>/ not <source_id>/ — documented in data_registry.md To Add a New Source workflow"
  - "Files were untracked (not in git) — used regular mv + git add instead of git mv; result is identical for history purposes"

patterns-established:
  - "Bar type subfolder pattern: bar_data/<bar_type>/ (volume/, time/, tick/)"
  - "Placeholder rows use periods=none and required_by=TBD until files exist"

requirements-completed: [BAR-01, BAR-02, BAR-03, BAR-04]

# Metrics
duration: 2min
completed: 2026-03-14
---

# Phase 01.2 Plan 01: Bar Type Registry and Subfolder Structure Summary

**Migrated 250-vol bar data files from flat bar_data/ into bar_data/volume/ subfolder, replaced single bar_data registry entry with typed bar_data_volume/time/tick source_ids, and renamed schema file to bar_data_volume_schema.md — unblocking Phase 4 engine config path resolution**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-14T02:30:15Z
- **Completed:** 2026-03-14T02:32:36Z
- **Tasks:** 2
- **Files modified:** 7 (created/moved/updated/deleted)

## Accomplishments

- 10 smoke tests written and confirmed RED before any migration, then GREEN after — confirms tests check real filesystem/registry conditions
- NQ_BarData_250vol_P{1,2}.txt moved to volume/ subfolder with git-tracked staging
- data_registry.md updated from single undifferentiated bar_data row to three typed rows; To Add a New Source workflow updated with bar_data subfolder convention
- bar_data_schema.md renamed to bar_data_volume_schema.md; schema description corrected from "1-min tick bars" to "250-vol volume bars"
- 23 total tests pass with no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Write smoke tests for bar type registry (Wave 0)** - `458f513` (test)
2. **Task 2: Migrate files, update registry, rename schema** - `ed9636a` (feat)

## Files Created/Modified

- `tests/test_bar_type_registry.py` - 10 filesystem/registry smoke tests using only pathlib and re
- `stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt` - P1 bar data in typed subfolder
- `stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P2.txt` - P2 bar data in typed subfolder
- `stages/01-data/data/bar_data/time/.gitkeep` - placeholder for future time bar type
- `stages/01-data/data/bar_data/tick/.gitkeep` - placeholder for future tick bar type
- `_config/data_registry.md` - replaced bar_data row with 3 typed source_id rows
- `stages/01-data/references/bar_data_volume_schema.md` - renamed + corrected schema (volume bars, not tick/1-min)
- `stages/01-data/references/bar_data_schema.md` - deleted (renamed to bar_data_volume_schema.md)

## Decisions Made

- Used `bar_data_volume` as source_id (not `bar_data_vol` or `volume_bars`) — matches architecture doc exactly
- Registered `bar_data_time` and `bar_data_tick` as placeholder rows with `periods=none` — architecture intent is clear, no Stage 01 validation risk since no script exists yet
- Files were untracked (git status showed `??`) so `git mv` was not applicable; used shell `mv` + `git add` for the new paths and `git rm` for the deleted schema file

## Deviations from Plan

None — plan executed exactly as written. The `git mv` vs regular `mv` difference was an implementation detail (files were untracked, making `git mv` a no-op), not a deviation from intent.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 4 (Backtest Engine) can now resolve `bar_data/volume/` paths in engine config
- Stage 01 validation script (when written in Phase 4 ENGINE-02) should use `bar_data_volume` source_id
- data_manifest.json does not exist yet (output/.gitkeep only) — will be generated when Stage 01 validation runs

---
*Phase: 01.2-bar-type-registry-and-subfolder-structure*
*Completed: 2026-03-14*
