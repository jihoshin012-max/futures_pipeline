---
phase: 02-hmm-regime-fitter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_hmm_regime_fitter.py
  - stages/01-data/hmm_regime_fitter.py
autonomous: true
requirements:
  - HMM-01
  - HMM-02
  - HMM-03

must_haves:
  truths:
    - "hmm_regime_fitter.py fits GaussianHMM exclusively on P1 bar data (P2 rows never passed to fit())"
    - "predict_proba() (filtered posteriors) is used for label assignment, not Viterbi predict()"
    - "Three independent HMMs are fitted: trend (2 states), volatility (3 states), macro (calendar-based)"
    - "Feature standardization uses P1 statistics only — no P2 leakage in scaler"
    - "All functions are importable and testable in isolation"
  artifacts:
    - path: "stages/01-data/hmm_regime_fitter.py"
      provides: "HMM regime fitter script with all functions"
      min_lines: 150
    - path: "tests/test_hmm_regime_fitter.py"
      provides: "7 test functions covering HMM-01, HMM-02, HMM-03"
      min_lines: 80
  key_links:
    - from: "stages/01-data/hmm_regime_fitter.py"
      to: "stages/01-data/data/bar_data/NQ_BarData_*.txt"
      via: "load_bar_file() reads CSV"
      pattern: "read_csv.*bar_data"
    - from: "stages/01-data/hmm_regime_fitter.py"
      to: "hmmlearn.hmm.GaussianHMM"
      via: "import and fit()"
      pattern: "GaussianHMM.*fit"
    - from: "tests/test_hmm_regime_fitter.py"
      to: "stages/01-data/hmm_regime_fitter.py"
      via: "import module functions"
      pattern: "from.*hmm_regime_fitter import"
---

<objective>
Write tests and implement hmm_regime_fitter.py using TDD — the script that fits three independent GaussianHMM models on P1 bar data and produces regime labels and a serialized model dict.

Purpose: This is the core regime-labeling infrastructure that all downstream stages consume. Correctness of the P1-only fit boundary and filtered-posterior label assignment are hard pipeline constraints.

Output: Working hmm_regime_fitter.py with all functions importable, plus passing test suite covering P1-only fit, convergence, label format, and pkl serialization.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hmm-regime-fitter/02-RESEARCH.md

@_config/regime_definitions.md
@_config/period_config.md
@stages/01-data/data/bar_data/NQ_BarData_20250916_20251214.txt (first 5 lines for column format)

<interfaces>
<!-- Bar data format (verified from actual file): -->
<!-- Date, Time, Open, High, Low, Last, Volume, NumberOfTrades, BidVolume, AskVolume -->
<!-- Date format: 2025/9/16, Time format: 00:08:35.782 -->

<!-- Period boundaries from _config/period_config.md: -->
<!-- P1: 2025-09-16 to 2025-12-14 (IS, 77 trading days) -->
<!-- P2: 2025-12-15 to 2026-03-02 (OOS, 67 trading days) -->
<!-- Total: 144 trading days -->

<!-- regime_labels.csv output format from regime_definitions.md: -->
<!-- Columns: date | trend | volatility | macro -->
<!-- trend: "trending" or "ranging" -->
<!-- volatility: "high_vol", "normal_vol", or "low_vol" -->
<!-- macro: "event_day" or "normal_day" -->

<!-- pkl format: {'trend': GaussianHMM, 'volatility': GaussianHMM} dict -->
<!-- macro is calendar-based, not HMM — not stored in pkl -->

<!-- hmmlearn 0.3.3 API (verified live): -->
<!-- GaussianHMM(n_components, covariance_type='diag', n_iter=100, random_state=42) -->
<!-- model.fit(X) — X shape (n_samples, n_features) -->
<!-- model.predict_proba(X) — returns (n_samples, n_components) posterior probabilities -->
<!-- model.monitor_.converged — bool, True if EM converged -->
<!-- model.means_ — (n_components, n_features) array -->
</interfaces>
</context>

<feature>
  <name>HMM Regime Fitter</name>
  <files>stages/01-data/hmm_regime_fitter.py, tests/test_hmm_regime_fitter.py</files>
  <behavior>
    - test_fit_uses_p1_only: Given bar data spanning P1+P2, fit() receives only rows where date <= 2025-12-14. Assert X_p1 shape[0] == 77 (P1 trading days).
    - test_model_converges: After fitting on P1 data, model.monitor_.converged is True for both trend and volatility HMMs.
    - test_no_degenerate_states: After predicting on full data, trend labels have exactly 2 unique values, volatility labels have exactly 3 unique values.
    - test_labels_cover_full_range: regime_labels DataFrame has exactly 144 rows covering all trading days from P1 start to P2 end.
    - test_label_values_valid: trend column contains only {"trending", "ranging"}, volatility only {"high_vol", "normal_vol", "low_vol"}, macro only {"event_day", "normal_day"}.
    - test_pkl_round_trip: Serialized pkl loads back and contains keys "trend" and "volatility", each a GaussianHMM with correct n_components.
    - test_pkl_model_predicts: Loaded model from pkl can call predict_proba() on new data without error and returns correct shape.
  </behavior>
  <implementation>
    Implement hmm_regime_fitter.py at stages/01-data/hmm_regime_fitter.py with these functions (all importable):

    1. **load_all_bars(repo_root: Path) -> pd.DataFrame** — Concatenate P1 and P2 bar data files from stages/01-data/data/bar_data/. Strip column whitespace. Parse datetime with format='mixed'.

    2. **aggregate_to_daily(bar_df: pd.DataFrame) -> pd.DataFrame** — Group tick bars by date. Return columns: date, open, high, low, close, volume. Use named aggregation syntax (pandas 3.0 compatible): `agg(open=('Open', 'first'), high=('High', 'max'), low=('Low', 'min'), close=('Last', 'last'), volume=('Volume', 'sum'))`.

    3. **compute_trend_features(daily: pd.DataFrame) -> np.ndarray** — Compute ADX (14-period) and 5-day return direction. Pure pandas/numpy implementation (no TA library). Return shape (n_days, 2). ADX formula from RESEARCH.md Pattern 4.

    4. **compute_volatility_features(daily: pd.DataFrame) -> np.ndarray** — Compute ATR ratio (daily ATR / 20-day rolling mean ATR) and log volatility change (log of day range / prev day range). Return shape (n_days, 2).

    5. **compute_macro_flags(dates: pd.Series) -> np.ndarray** — Hard-code FOMC, CPI, NFP dates for 2025-09-16 to 2026-03-02 as a Python set. Return 0/1 array. Document each event date in code comments.

    6. **fit_hmm(X_p1: np.ndarray, n_components: int) -> GaussianHMM** — Standardize using P1 mean/std. Fit GaussianHMM(n_components=n_components, covariance_type='diag', n_iter=100, random_state=42). Assert convergence. Return fitted model.

    7. **assign_state_names_trend(model: GaussianHMM, adx_col: int) -> dict** — Map integer states to "trending"/"ranging" by sorting on mean ADX. Higher mean ADX = "trending". Log assignment.

    8. **assign_state_names_volatility(model: GaussianHMM, atr_ratio_col: int) -> dict** — Map integer states to "high_vol"/"normal_vol"/"low_vol" by sorting on mean ATR ratio. Log assignment.

    9. **generate_labels(daily, trend_model, vol_model, trend_features_all, vol_features_all, trend_p1_stats, vol_p1_stats, macro_flags, trend_names, vol_names) -> pd.DataFrame** — Apply predict_proba() on standardized full-range features (using P1 stats for standardization). Argmax for hard labels. Map to string names. Return DataFrame with columns: date, trend, volatility, macro.

    10. **main(repo_root: Path)** — Orchestrator: load bars, aggregate, compute features, split P1 mask (date <= 2025-12-14), fit HMMs, assign names, generate labels, save CSV to stages/01-data/data/labels/regime_labels.csv, serialize pkl to shared/scoring_models/hmm_regime_v1.pkl as {'trend': model, 'volatility': model} dict with protocol=4.

    CRITICAL constraints:
    - P2 rows NEVER passed to fit() — slice with p1_mask BEFORE fitting
    - Standardization uses P1 mean/std only — apply same transform to P2 rows
    - Use predict_proba() (filtered posteriors), NOT predict() (Viterbi)
    - random_state=42 for reproducibility
    - Assert model.monitor_.converged after each fit
    - Assert len(np.unique(labels)) == n_components after each predict (no all-one-state)
    - Script must be runnable as `python stages/01-data/hmm_regime_fitter.py` from repo root
    - All functions importable for testing: use `if __name__ == '__main__': main(Path('.'))` guard

    Tests should use synthetic data (small random arrays) to avoid depending on real bar data files. Use monkeypatch or fixtures to control inputs. Tests must run in < 15 seconds.
  </implementation>
</feature>

<verification>
python -m pytest tests/test_hmm_regime_fitter.py -v
All 7 tests pass. No warnings about deprecated pandas syntax.
</verification>

<success_criteria>
- tests/test_hmm_regime_fitter.py exists with 7 test functions
- stages/01-data/hmm_regime_fitter.py exists with all 10 functions importable
- `python -m pytest tests/test_hmm_regime_fitter.py -v` shows 7 passed
- No P2 data touches fit() in any code path
- predict_proba() used, not predict()
- Macro dates hard-coded with comments
</success_criteria>

<output>
After completion, create `.planning/phases/02-hmm-regime-fitter/02-01-SUMMARY.md`
</output>
