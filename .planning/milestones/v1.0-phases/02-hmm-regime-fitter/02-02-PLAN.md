---
phase: 02-hmm-regime-fitter
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - stages/01-data/data/labels/regime_labels.csv
  - shared/scoring_models/hmm_regime_v1.pkl
  - stages/03-hypothesis/references/strategy_archetypes.md
autonomous: true
requirements:
  - HMM-02
  - HMM-03

must_haves:
  truths:
    - "regime_labels.csv exists with exactly 144 rows covering P1+P2 date range"
    - "regime_labels.csv has valid string labels in all three columns (no integer codes)"
    - "hmm_regime_v1.pkl is loadable and contains trend + volatility GaussianHMM models"
    - "hmm_regime_v1.pkl models can predict_proba() on new data without error"
    - "strategy_archetypes.md references hmm_regime_v1.pkl as available scoring model"
  artifacts:
    - path: "stages/01-data/data/labels/regime_labels.csv"
      provides: "Regime labels for all 144 trading days"
      contains: "date,trend,volatility,macro"
    - path: "shared/scoring_models/hmm_regime_v1.pkl"
      provides: "Serialized HMM models for trend and volatility"
    - path: "stages/03-hypothesis/references/strategy_archetypes.md"
      provides: "Registration of hmm_regime_v1.pkl as available model"
      contains: "hmm_regime_v1.pkl"
  key_links:
    - from: "stages/01-data/data/labels/regime_labels.csv"
      to: "_config/regime_definitions.md"
      via: "Column names and label values match definitions"
      pattern: "date.*trend.*volatility.*macro"
    - from: "shared/scoring_models/hmm_regime_v1.pkl"
      to: "stages/01-data/hmm_regime_fitter.py"
      via: "Generated by main() function"
      pattern: "pickle.dump"
---

<objective>
Run hmm_regime_fitter.py on real NQ bar data to produce regime_labels.csv and hmm_regime_v1.pkl, then validate outputs against requirements and register the model in strategy_archetypes.md.

Purpose: Generate the actual regime labeling artifacts that downstream stages consume. Until this plan completes, no stage can use regime-based features or breakdowns.

Output: regime_labels.csv (144 rows), hmm_regime_v1.pkl (serialized dict), updated strategy_archetypes.md.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hmm-regime-fitter/02-RESEARCH.md
@.planning/phases/02-hmm-regime-fitter/02-01-SUMMARY.md

@_config/regime_definitions.md
@_config/period_config.md
@stages/03-hypothesis/references/strategy_archetypes.md

<interfaces>
<!-- From Plan 01 (hmm_regime_fitter.py): -->
<!-- main(repo_root: Path) — runs full pipeline, writes CSV + pkl -->
<!-- Callable as: python stages/01-data/hmm_regime_fitter.py -->

<!-- Output: stages/01-data/data/labels/regime_labels.csv -->
<!-- Columns: date,trend,volatility,macro -->
<!-- 144 rows (77 P1 + 67 P2) -->

<!-- Output: shared/scoring_models/hmm_regime_v1.pkl -->
<!-- pickle dict: {'trend': GaussianHMM, 'volatility': GaussianHMM} -->

<!-- strategy_archetypes.md current structure: -->
<!-- Template for archetypes with fields: Scoring model, Required data, etc. -->
<!-- HMM regime model should be noted as available shared resource -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fitter and validate outputs</name>
  <files>stages/01-data/data/labels/regime_labels.csv, shared/scoring_models/hmm_regime_v1.pkl</files>
  <action>
    Run the fitter script from repo root:
    ```
    python stages/01-data/hmm_regime_fitter.py
    ```

    After execution, validate the outputs with these checks (run as inline Python or separate validation script):

    1. **regime_labels.csv validation:**
       - File exists at stages/01-data/data/labels/regime_labels.csv
       - Has exactly 144 rows (excluding header)
       - Columns are exactly: date, trend, volatility, macro
       - First date is 2025-09-16, last date is 2026-03-02
       - trend values are exclusively {"trending", "ranging"}
       - volatility values are exclusively {"high_vol", "normal_vol", "low_vol"}
       - macro values are exclusively {"event_day", "normal_day"}
       - No NaN or empty values
       - macro column has at least 5 event_day entries (FOMC/CPI/NFP coverage check)
       - trend and volatility columns each have more than one unique value (no all-one-state)

    2. **hmm_regime_v1.pkl validation:**
       - File exists at shared/scoring_models/hmm_regime_v1.pkl
       - Loads with pickle.load() without error
       - Contains keys "trend" and "volatility"
       - trend model has n_components == 2
       - volatility model has n_components == 3
       - Both models can call predict_proba() on a small random array without error

    3. **P1-only fit verification:**
       - Print the model means for trend and volatility HMMs
       - Log which state indices map to which labels (for human audit trail)

    If the fitter script fails, diagnose the error. Common issues per RESEARCH.md:
    - Non-convergence: check model.monitor_.converged output
    - All-one-state: check unique label counts
    - Date parsing: check format='mixed' usage
    - pandas 3.0 agg syntax: check named aggregation

    If outputs validate, run the full test suite to confirm nothing regressed:
    ```
    python -m pytest tests/ -v
    ```
  </action>
  <verify>
    <automated>python -c "import pandas as pd; df = pd.read_csv('stages/01-data/data/labels/regime_labels.csv'); assert len(df) == 144, f'Expected 144 rows, got {len(df)}'; assert list(df.columns) == ['date','trend','volatility','macro'], f'Wrong columns: {list(df.columns)}'; assert set(df['trend'].unique()) == {'trending','ranging'}, f'Bad trend values: {df[\"trend\"].unique()}'; assert set(df['volatility'].unique()) == {'high_vol','normal_vol','low_vol'}, f'Bad vol values: {df[\"volatility\"].unique()}'; print('regime_labels.csv: VALID')" && python -c "import pickle; m = pickle.load(open('shared/scoring_models/hmm_regime_v1.pkl','rb')); assert 'trend' in m and 'volatility' in m; assert m['trend'].n_components == 2; assert m['volatility'].n_components == 3; print('hmm_regime_v1.pkl: VALID')" && python -m pytest tests/ -v</automated>
  </verify>
  <done>regime_labels.csv has 144 valid rows with correct label values. hmm_regime_v1.pkl loads with correct model structure. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Register HMM model in strategy_archetypes.md</name>
  <files>stages/03-hypothesis/references/strategy_archetypes.md</files>
  <action>
    Add a "Shared Scoring Models" section to strategy_archetypes.md, ABOVE the future archetype template section. This section registers hmm_regime_v1.pkl as an available shared resource that archetypes can reference.

    Add the following content after the "---" separator below the intake checklist note:

    ```markdown
    ## Shared Scoring Models

    ### HMM Regime Model v1
    - Path: shared/scoring_models/hmm_regime_v1.pkl
    - Generated by: stages/01-data/hmm_regime_fitter.py
    - Fit period: P1 only (2025-09-16 to 2025-12-14) — frozen, do not re-fit
    - Labels file: stages/01-data/data/labels/regime_labels.csv (144 rows, P1+P2)
    - Dimensions: trend (2 states), volatility (3 states), macro (calendar)
    - Usage: Available to Stage 02 as feature, Stage 05 for regime breakdown
    - Status: active
    ```

    Do NOT modify the simulator interface contract section or the archetype template. Only add the shared scoring models section.
  </action>
  <verify>
    <automated>python -c "content = open('stages/03-hypothesis/references/strategy_archetypes.md').read(); assert 'hmm_regime_v1.pkl' in content, 'pkl not registered'; assert 'Shared Scoring Models' in content, 'section missing'; assert 'Simulator Interface Contract' in content, 'contract section accidentally removed'; print('strategy_archetypes.md: VALID')"</automated>
  </verify>
  <done>strategy_archetypes.md contains a Shared Scoring Models section registering hmm_regime_v1.pkl with path, generation source, fit period, and usage notes. Existing content preserved.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` — all tests green (HMM + scaffold adapter)
2. regime_labels.csv has 144 rows, correct columns, valid label strings
3. hmm_regime_v1.pkl loads and models have correct n_components
4. strategy_archetypes.md references the pkl file
5. No P2 data was used in fit() (verified by test_fit_uses_p1_only in Plan 01)
</verification>

<success_criteria>
- regime_labels.csv exists at stages/01-data/data/labels/regime_labels.csv with 144 rows
- hmm_regime_v1.pkl exists at shared/scoring_models/hmm_regime_v1.pkl and is loadable
- strategy_archetypes.md registers the HMM model
- All tests pass: `python -m pytest tests/ -v`
- HMM convergence confirmed (no warnings)
- No all-one-state degenerate output in any dimension
</success_criteria>

<output>
After completion, create `.planning/phases/02-hmm-regime-fitter/02-02-SUMMARY.md`
</output>
