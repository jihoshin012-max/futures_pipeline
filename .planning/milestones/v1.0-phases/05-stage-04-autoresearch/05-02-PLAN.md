---
phase: 05-stage-04-autoresearch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - stages/02-features/autoresearch/evaluate_features.py
  - shared/archetypes/zone_touch/feature_evaluator.py
  - tests/test_evaluate_features.py
  - tests/test_feature_evaluator.py
autonomous: true
requirements: [AUTO-04, AUTO-05]

must_haves:
  truths:
    - "evaluate_features.py dispatches to the correct archetype evaluator based on program.md"
    - "feature_evaluator.py returns a dict matching the standard interface contract"
    - "evaluate_features.py writes feature_evaluation.json with results from evaluator"
  artifacts:
    - path: "stages/02-features/autoresearch/evaluate_features.py"
      provides: "Pure dispatcher that loads archetype evaluator via importlib"
      min_lines: 20
    - path: "shared/archetypes/zone_touch/feature_evaluator.py"
      provides: "Standard interface evaluate() -> dict"
      exports: ["evaluate"]
    - path: "tests/test_evaluate_features.py"
      provides: "Tests for dispatcher"
      min_lines: 30
    - path: "tests/test_feature_evaluator.py"
      provides: "Tests for evaluator interface"
      min_lines: 20
  key_links:
    - from: "stages/02-features/autoresearch/evaluate_features.py"
      to: "shared/archetypes/zone_touch/feature_evaluator.py"
      via: "importlib.import_module dynamic dispatch"
      pattern: "importlib.*feature_evaluator"
    - from: "stages/02-features/autoresearch/evaluate_features.py"
      to: "stages/02-features/autoresearch/feature_evaluation.json"
      via: "json.dump output"
      pattern: "feature_evaluation\\.json"
---

<objective>
Build the evaluate_features.py dispatcher and zone_touch feature_evaluator.py with the standard interface contract. The dispatcher dynamically loads the archetype-specific evaluator and writes feature_evaluation.json.

Purpose: This infrastructure is required before the Stage 02 autoresearch driver (Phase 6) can be built.
Output: evaluate_features.py dispatcher, feature_evaluator.py with evaluate() function, and test suites for both.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-stage-04-autoresearch/05-RESEARCH.md

@shared/archetypes/zone_touch/zone_touch_simulator.py
@stages/02-features/CONTEXT.md

<interfaces>
<!-- From RESEARCH.md Pattern 3 and 4 -->

evaluate_features.py dispatcher (~30 lines):
```python
# Pure dispatcher — reads archetype from program.md, loads evaluator, calls evaluate(), writes JSON
archetype = read_archetype_from_program_md()
evaluator = load_module(f"shared/archetypes/{archetype}/feature_evaluator.py")
result = evaluator.evaluate()
write_evaluation_json(result)
```

feature_evaluator.py interface contract:
```python
def evaluate() -> dict:
    # Returns: {"features": [{"name": str, "spread": float, "mwu_p": float, "kept": bool}], "n_touches": int}
    ...
```

Output JSON (feature_evaluation.json):
```json
{
  "timestamp": "...",
  "features_evaluated": [
    {"name": "feature_1", "spread": 0.34, "p_value": 0.02, "bins": 3, "result": "kept"},
    {"name": "feature_2", "spread": 0.08, "p_value": 0.41, "result": "reverted"}
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests and implement evaluate_features.py dispatcher</name>
  <files>tests/test_evaluate_features.py, stages/02-features/autoresearch/evaluate_features.py</files>
  <behavior>
    - test_dispatcher_loads_evaluator: create a mock feature_evaluator.py in tmp archetype dir; verify dispatcher calls evaluate() and writes feature_evaluation.json
    - test_dispatcher_reads_archetype: create a program.md with archetype line; verify dispatcher extracts correct archetype name
    - test_dispatcher_output_schema: verify feature_evaluation.json has "timestamp" and "features_evaluated" keys
    - test_dispatcher_missing_evaluator: point to non-existent archetype; verify clear error message naming the missing evaluator
  </behavior>
  <action>
    **Tests first (RED):**
    Create tests/test_evaluate_features.py. Tests should use tmp_path to create mock program.md files and mock archetype directories with simple feature_evaluator.py stubs. Do NOT import the real zone_touch evaluator — test the dispatcher in isolation.

    The dispatcher needs a program.md in Stage 02 that contains an archetype line. For Phase 5, create the program.md parsing to read `ARCHETYPE: zone_touch` from program.md. The Stage 02 program.md does not exist yet (Phase 6 work), so the dispatcher should accept a `--program-md` argument or default to `stages/02-features/autoresearch/program.md`.

    **Implementation (GREEN):**
    Create stages/02-features/autoresearch/evaluate_features.py (~30 lines):

    1. Parse `--archetype` CLI argument (required) and `--output` (default: `stages/02-features/autoresearch/feature_evaluation.json`)
    2. Resolve repo root (same pattern as engine: parents traversal)
    3. Add `shared/archetypes/{archetype}/` to sys.path
    4. `importlib.import_module("feature_evaluator")` — load fresh each time (Pitfall 6)
    5. Call `evaluator.evaluate()` — returns dict
    6. Wrap in `{"timestamp": ISO_now, "features_evaluated": result["features"]}` and write to output JSON
    7. Print summary: N features evaluated, N kept
    8. `if __name__ == "__main__"` block with argparse

    If the module cannot be imported, print a clear error: `"ERROR: feature_evaluator.py not found for archetype '{archetype}' at {path}"` and sys.exit(1).

    Add `# archetype: zone_touch` as line 1 comment per CLAUDE.md convention (this is a shared file, not archetype-specific, but the convention applies to archetype-aware files).
  </action>
  <verify>
    <automated>python -m pytest tests/test_evaluate_features.py -x -v</automated>
  </verify>
  <done>evaluate_features.py dispatches to archetype evaluator via importlib, writes feature_evaluation.json, handles missing evaluator with clear error; all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests and implement zone_touch feature_evaluator.py</name>
  <files>tests/test_feature_evaluator.py, shared/archetypes/zone_touch/feature_evaluator.py</files>
  <behavior>
    - test_evaluate_returns_dict: evaluate() returns dict with "features" (list) and "n_touches" (int) keys
    - test_evaluate_features_list_schema: each item in "features" list has "name", "spread", "mwu_p", "kept" keys
    - test_evaluate_empty_features: with no features registered, returns empty list and n_touches > 0 (data loads but no features to evaluate)
    - test_evaluate_is_callable: feature_evaluator module has a callable evaluate() at module level
  </behavior>
  <action>
    **Tests first (RED):**
    Create tests/test_feature_evaluator.py. Import evaluate from shared.archetypes.zone_touch.feature_evaluator. Tests verify interface contract. For Phase 5, evaluate() returns empty features list (no feature_engine.py exists yet — that is Phase 6 work). The test verifies the interface shape, not real feature evaluation.

    **Implementation (GREEN):**
    Create shared/archetypes/zone_touch/feature_evaluator.py (~40 lines):

    1. Line 1: `# archetype: zone_touch` (per CLAUDE.md convention)
    2. Import pathlib, sys for repo root resolution
    3. `def evaluate() -> dict:` — the standard interface
       - Resolve repo root from file location (parents[3]: shared/archetypes/zone_touch/ -> repo root)
       - Load P1 touch data using `shared.data_loader.load_touches` (the function already exists and is tested)
       - Count n_touches = len(touch_df)
       - Return `{"features": [], "n_touches": n_touches}` — empty features list because no features are registered yet
       - This satisfies AUTO-05 interface contract. Real feature evaluation is Phase 6 (AUTO-06/07/08)
    4. Add docstring explaining: "Phase 5 placeholder — loads data and returns empty features list. Phase 6 adds real MWU spread computation."

    The evaluate() function must be stateless — no module-level state, no caching. Each call loads fresh data.
  </action>
  <verify>
    <automated>python -m pytest tests/test_feature_evaluator.py -x -v</automated>
  </verify>
  <done>feature_evaluator.py has evaluate() returning dict matching interface contract; data loading works with real P1 touch file; all tests pass</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_evaluate_features.py tests/test_feature_evaluator.py -x -v` — all tests pass
2. `python -m pytest tests/ -v` — full suite still green
3. `python stages/02-features/autoresearch/evaluate_features.py --archetype zone_touch` — produces feature_evaluation.json with empty features list and n_touches > 0
</verification>

<success_criteria>
- evaluate_features.py dispatches to zone_touch evaluator via importlib
- feature_evaluator.py evaluate() returns {"features": [], "n_touches": N} where N > 0
- feature_evaluation.json written with timestamp and features_evaluated
- Missing evaluator produces clear error message
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-stage-04-autoresearch/05-02-SUMMARY.md`
</output>
