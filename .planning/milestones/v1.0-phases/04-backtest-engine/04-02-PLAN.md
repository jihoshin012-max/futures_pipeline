---
phase: 04-backtest-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/data_loader.py
  - shared/scoring_models/scoring_adapter.py
  - shared/scoring_models/zone_touch_v1.json
  - tests/test_data_loader.py
  - tests/test_scoring_adapter_impl.py
autonomous: true
requirements: [ENGINE-02, ENGINE-09]

must_haves:
  truths:
    - "load_data(touches_csv, bars_path) returns two DataFrames with expected columns and correct dtypes"
    - "load_scoring_adapter('binned', path) returns a BinnedScoringAdapter instance"
    - "BinnedScoringAdapter.score() returns pd.Series[float] aligned to touch_df.index"
    - "Calling score() on an unimplemented adapter stub raises NotImplementedError (preserved behavior)"
    - "load_scoring_adapter with unknown adapter_type raises a clear error"
  artifacts:
    - path: "shared/data_loader.py"
      provides: "Parameterized data loading for touches and bar data"
      exports: ["load_data", "load_touches", "load_bars", "parse_instruments_md"]
      min_lines: 40
    - path: "shared/scoring_models/scoring_adapter.py"
      provides: "Scoring adapter factory and BinnedScoringAdapter implementation"
      exports: ["load_scoring_adapter", "BinnedScoringAdapter"]
    - path: "shared/scoring_models/zone_touch_v1.json"
      provides: "Placeholder scoring model with all-zero weights for end-to-end pass"
      contains: "bin_edges"
    - path: "tests/test_data_loader.py"
      provides: "Unit tests for data loader"
      min_lines: 30
    - path: "tests/test_scoring_adapter_impl.py"
      provides: "Unit tests for scoring adapter factory and BinnedScoringAdapter"
      min_lines: 30
  key_links:
    - from: "shared/data_loader.py"
      to: "stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt"
      via: "pd.read_csv with column strip"
      pattern: "columns.*strip"
    - from: "shared/data_loader.py"
      to: "_config/instruments.md"
      via: "parse_instruments_md for cost_ticks"
      pattern: "cost_ticks"
    - from: "shared/scoring_models/scoring_adapter.py"
      to: "shared/scoring_models/zone_touch_v1.json"
      via: "json.load in BinnedScoringAdapter.__init__"
      pattern: "json.load"
---

<objective>
Create the data loading module and implement the scoring adapter factory with BinnedScoringAdapter placeholder logic.

Purpose: These are the two core dependencies the engine needs before it can be written. The data loader replaces the nonexistent data_loader.py (Pitfall 1 from RESEARCH.md). The scoring adapter factory enables dynamic adapter loading. Both come with tests.
Output: shared/data_loader.py, updated scoring_adapter.py, zone_touch_v1.json placeholder, test files
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backtest-engine/04-RESEARCH.md

@shared/scoring_models/scoring_adapter.py
@_config/instruments.md
@stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt (first 2 lines for column format)
@stages/01-data/data/touches/ZRA_Hist_P1.csv (first 2 lines for column format)

<interfaces>
<!-- Bar data format (verified from actual file headers) -->
<!-- Header: Date, Time, Open, High, Low, Last, Volume, NumberOfTrades, BidVolume, AskVolume -->
<!-- Note: space after comma in header — must strip column names -->
<!-- Date format: 2025/9/16, Time: 00:08:35.782 — use pd.to_datetime(format="mixed") -->

<!-- Touch data format (verified from actual file headers) -->
<!-- Header: DateTime,BarIndex,TouchType,ApproachDir,TouchPrice,ZoneTop,ZoneBot,... (32 cols) -->
<!-- DateTime format: 9/16/2025 2:26 -->

<!-- Scoring adapter protocol from scoring_adapter.py -->
```python
class ScoringAdapter(Protocol):
    def score(self, touch_df: pd.DataFrame) -> "pd.Series[float]": ...

class BinnedScoringAdapter:
    def __init__(self, model_path: str) -> None: ...
    def score(self, touch_df: pd.DataFrame) -> "pd.Series[float]": ...

def load_scoring_adapter(model_path: str, adapter_type: str) -> ScoringAdapter: ...
```

<!-- Scoring model template from _template.json -->
```json
{
  "model_id": "...",
  "frozen_date": "...",
  "features": [...],
  "weights": {...},
  "bin_edges": {...}
}
```

<!-- Instruments.md cost_ticks format -->
<!-- Cost model (round trip): 3 ticks = $15.00 -->
<!-- Must parse this line per instrument to get cost_ticks integer -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create data_loader.py with tests</name>
  <files>shared/data_loader.py, tests/test_data_loader.py</files>
  <behavior>
    - load_bars("stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt") returns DataFrame with stripped columns [Date, Time, Open, High, Low, Last, Volume, NumberOfTrades, BidVolume, AskVolume] and a parsed datetime column
    - load_touches("stages/01-data/data/touches/ZRA_Hist_P1.csv") returns DataFrame with DateTime parsed, all 32 columns present
    - load_data(touches_csv, bars_path) returns (touches_df, bars_df) tuple — convenience wrapper
    - parse_instruments_md("NQ") returns dict with cost_ticks=3, tick_size=0.25, tick_value=5.0
    - parse_instruments_md("UNKNOWN") raises ValueError with clear message
    - Bar DataFrame is sorted by datetime (determinism requirement from Pitfall 5)
  </behavior>
  <action>
Write tests/test_data_loader.py first:
- test_load_bars_columns: Load actual P1 bar file, assert columns match expected after strip
- test_load_bars_sorted: Assert bars are sorted by datetime
- test_load_touches_columns: Load actual P1 touch file, assert 32 columns, DateTime parsed
- test_load_data_returns_tuple: Convenience wrapper returns (touches, bars) tuple
- test_parse_instruments_nq: Parse NQ from instruments.md, assert cost_ticks=3
- test_parse_instruments_unknown: Assert ValueError on unknown instrument

Then implement shared/data_loader.py:
- load_bars(path: str) -> pd.DataFrame: Read CSV, strip column names (spaces after commas in header), parse datetime from Date+Time columns using pd.to_datetime(format="mixed"), sort by datetime, return
- load_touches(path: str) -> pd.DataFrame: Read CSV, parse DateTime column using pd.to_datetime(format="mixed"), return
- load_data(touches_csv: str, bars_path: str) -> tuple[pd.DataFrame, pd.DataFrame]: Return (load_touches(touches_csv), load_bars(bars_path))
- parse_instruments_md(instrument: str, config_path: str = "_config/instruments.md") -> dict: Parse markdown to extract tick_size, tick_value, cost_ticks for the given instrument symbol. Use regex to find the instrument section and extract "Cost model (round trip): N ticks" to get cost_ticks as int.

IMPORTANT: No hardcoded paths in the module — all paths come from function arguments. The engine resolves paths from config JSON; this module is a pure loader.
  </action>
  <verify>
    <automated>python -m pytest tests/test_data_loader.py -x -v</automated>
  </verify>
  <done>All data loader tests pass; load_bars returns sorted DataFrame with stripped columns; load_touches returns 32-column DataFrame with parsed datetime; parse_instruments_md extracts cost_ticks from instruments.md</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scoring adapter factory and BinnedScoringAdapter placeholder</name>
  <files>shared/scoring_models/scoring_adapter.py, shared/scoring_models/zone_touch_v1.json, tests/test_scoring_adapter_impl.py</files>
  <behavior>
    - load_scoring_adapter("shared/scoring_models/zone_touch_v1.json", "BinnedScoringAdapter") returns BinnedScoringAdapter instance
    - load_scoring_adapter(path, "SklearnScoringAdapter") returns SklearnScoringAdapter instance (still stub)
    - load_scoring_adapter(path, "ONNXScoringAdapter") returns ONNXScoringAdapter instance (still stub)
    - load_scoring_adapter(path, "UnknownAdapter") raises SystemExit with clear error naming the adapter
    - BinnedScoringAdapter.score(empty_df_with_columns) returns pd.Series of floats (all zeros for placeholder model)
    - BinnedScoringAdapter.score(touch_df_with_known_features) returns pd.Series aligned to touch_df.index
    - SklearnScoringAdapter.score() still raises NotImplementedError
    - ONNXScoringAdapter.score() still raises NotImplementedError
  </behavior>
  <action>
Write tests/test_scoring_adapter_impl.py first:
- test_factory_binned: load_scoring_adapter with "BinnedScoringAdapter" returns BinnedScoringAdapter
- test_factory_sklearn: load_scoring_adapter with "SklearnScoringAdapter" returns SklearnScoringAdapter (stub)
- test_factory_onnx: load_scoring_adapter with "ONNXScoringAdapter" returns ONNXScoringAdapter (stub)
- test_factory_unknown: load_scoring_adapter with "FooAdapter" raises SystemExit
- test_binned_score_returns_series: BinnedScoringAdapter.score(df) returns pd.Series with same index
- test_sklearn_stub_raises: SklearnScoringAdapter.score() raises NotImplementedError
- test_onnx_stub_raises: ONNXScoringAdapter.score() raises NotImplementedError

Then update shared/scoring_models/scoring_adapter.py:
- Implement load_scoring_adapter factory: map adapter_type string to class, instantiate with model_path, return. On unknown type: raise SystemExit naming the adapter and listing valid types.
- Implement BinnedScoringAdapter.__init__: Load JSON model from model_path (json.load), store weights dict and bin_edges dict.
- Implement BinnedScoringAdapter.score(): For the placeholder model (all weights=0), return pd.Series of zeros aligned to touch_df.index. The real binning logic will be implemented when calibration data exists (Phase 5+ work). For now: return pd.Series(0.0, index=touch_df.index) — this is correct because all weights are zero in the placeholder model.
- Keep SklearnScoringAdapter and ONNXScoringAdapter as stubs (NotImplementedError preserved) — ENGINE-09 depends on this behavior.

Create shared/scoring_models/zone_touch_v1.json as placeholder:
```json
{
  "model_id": "zone_touch_v1",
  "frozen_date": "uncalibrated",
  "features": [],
  "weights": {},
  "bin_edges": {}
}
```
Document in a comment at top of config_schema.md (Plan 01) or in the JSON itself that this is an uncalibrated baseline — real calibration is Phase 5+ work.

IMPORTANT per Pitfall 4: BinnedScoringAdapter.score() must return pd.Series(scores, index=touch_df.index) — never a plain list or numpy array.
  </action>
  <verify>
    <automated>python -m pytest tests/test_scoring_adapter_impl.py -x -v</automated>
  </verify>
  <done>load_scoring_adapter factory works for all 3 adapter types + unknown error; BinnedScoringAdapter returns pd.Series aligned to index; stubs still raise NotImplementedError; zone_touch_v1.json exists as placeholder</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_data_loader.py tests/test_scoring_adapter_impl.py -v passes all tests
- shared/data_loader.py has no hardcoded paths
- scoring_adapter.py factory correctly dispatches to all adapter types
- zone_touch_v1.json is valid JSON matching _template.json schema
</verification>

<success_criteria>
Data loader and scoring adapter are tested and ready for the engine to import. Data loader can load both bar and touch data from any path. Scoring adapter factory can instantiate any registered adapter type. BinnedScoringAdapter returns properly-typed pd.Series output.
</success_criteria>

<output>
After completion, create `.planning/phases/04-backtest-engine/04-02-SUMMARY.md`
</output>
