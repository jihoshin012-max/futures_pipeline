---
phase: 04-backtest-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stages/04-backtest/references/backtest_engine_qa.md
  - shared/archetypes/zone_touch/simulation_rules.md
  - stages/04-backtest/references/config_schema.json
  - stages/04-backtest/references/config_schema.md
autonomous: true
requirements: [ENGINE-01, ENGINE-04, ENGINE-05, ENGINE-08]

must_haves:
  truths:
    - "backtest_engine_qa.md contains non-empty answers for Q1 through Q6"
    - "simulation_rules.md describes the zone_touch simulator behavior including trail mechanics, BE trigger, multi-leg exits, and time cap"
    - "config_schema.json is valid JSON with all fields from the architecture spec including trail_steps array"
    - "config_schema.md has a documentation row for every key in config_schema.json with FIXED vs CANDIDATE designation"
  artifacts:
    - path: "stages/04-backtest/references/backtest_engine_qa.md"
      provides: "Q1-Q6 answers for engine design decisions"
      contains: "## Q1"
    - path: "shared/archetypes/zone_touch/simulation_rules.md"
      provides: "Simulator behavior reference for zone_touch archetype"
      contains: "trail_steps"
    - path: "stages/04-backtest/references/config_schema.json"
      provides: "Reference config with all engine fields"
      min_lines: 20
    - path: "stages/04-backtest/references/config_schema.md"
      provides: "Field-by-field documentation with FIXED vs CANDIDATE"
      contains: "FIXED"
  key_links:
    - from: "stages/04-backtest/references/config_schema.json"
      to: "stages/04-backtest/references/config_schema.md"
      via: "every JSON key documented in md table"
      pattern: "trail_steps.*CANDIDATE"
---

<objective>
Create the four reference documents that define the engine's design decisions, config contract, and simulator behavior rules.

Purpose: These docs are prerequisites for both the engine implementation (Plan 03) and the end-to-end verification (Plan 04). They codify Q&A answers, config field semantics, and simulation rules so the engine and simulator can be implemented against explicit contracts.
Output: backtest_engine_qa.md, simulation_rules.md, config_schema.json, config_schema.md
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backtest-engine/04-RESEARCH.md

@shared/archetypes/zone_touch/exit_templates.md
@stages/04-backtest/CONTEXT.md
@_config/instruments.md
@_config/period_config.md
@_config/statistical_gates.md
@stages/03-hypothesis/references/strategy_archetypes.md

<interfaces>
<!-- Simulator interface contract from strategy_archetypes.md -->
```python
def run(bar_df, touch_row, config, bar_offset) -> SimResult

@dataclass
class SimResult:
    pnl_ticks: float
    win: bool
    exit_reason: str
    bars_held: int
```

<!-- Scoring adapter interface from scoring_adapter.py -->
```python
class ScoringAdapter(Protocol):
    def score(self, touch_df: pd.DataFrame) -> "pd.Series[float]": ...

def load_scoring_adapter(model_path: str, adapter_type: str) -> ScoringAdapter: ...
```

<!-- Config JSON structure from RESEARCH.md Pattern 1 -->
```json
{
  "version": "v1",
  "instrument": "NQ",
  "touches_csv": "...",
  "bar_data": "...",
  "scoring_model_path": "...",
  "archetype": { "name": "...", "simulator_module": "...", "scoring_adapter": "..." },
  "active_modes": ["M1", "M3", "M4", "M5"],
  "routing": { "score_threshold": 48, "seq_limit": 3 },
  "M1": { "stop_ticks": ..., "leg_targets": [...], "trail_steps": [...], "time_cap_bars": ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write backtest_engine_qa.md and simulation_rules.md</name>
  <files>stages/04-backtest/references/backtest_engine_qa.md, shared/archetypes/zone_touch/simulation_rules.md</files>
  <action>
Create backtest_engine_qa.md in stages/04-backtest/references/ with Q1-Q6 answers transcribed from the architecture document and RESEARCH.md findings:

- Q1: Does backtest_engine.py read data_manifest.json? NO — takes explicit paths from config JSON.
- Q2: How does BinnedScoringAdapter work? 14 features (7 static / 7 percentile-binned), weights and bin_edges from JSON model file.
- Q3: Is be_trigger_ticks a separate config field? NO — trail_steps[0] with new_stop_ticks=0 IS the BE trigger.
- Q4: How does the engine resolve cost_ticks? Reads from _config/instruments.md at startup using config.instrument to look up the correct instrument section. NEVER hardcoded (CLAUDE.md Rule 5).
- Q5: How does the holdout guard work? Checks for holdout_locked_P2.flag existence AND scans config paths for case-insensitive "p2". Aborts with SystemExit — cannot be bypassed by config.
- Q6: What is the output schema? result.json with pf, n_trades, win_rate, total_pnl_ticks, max_drawdown_ticks, and per_mode breakdown keyed by mode name.

Each Q gets its own ## heading with a 3-5 line answer. Include `last_reviewed: 2026-03-14` frontmatter.

Create simulation_rules.md in shared/archetypes/zone_touch/ with:
- Line 1: `# archetype: zone_touch` (per CLAUDE.md convention)
- Section: Entry mechanics (touch_row provides entry price, direction from ApproachDir, mode from routing)
- Section: Exit mechanics — multi-leg partial exit (leg_targets array), single-leg mode, stop_ticks as initial stop
- Section: Trail mechanics — trail_steps array, each step has trigger_ticks and new_stop_ticks, stop ratchets up never down, BE is trail_steps[0] with new_stop_ticks=0
- Section: Time cap — exit at market after time_cap_bars
- Section: Cost model — cost_ticks from instruments.md applied to PF calculation (not to individual trade P&L; trade pnl_ticks is raw, cost applied during metrics aggregation)
- Section: SimResult contract — pure function, no I/O, returns SimResult(pnl_ticks, win, exit_reason, bars_held)
- Keep under 60 lines total
  </action>
  <verify>
    <automated>python -c "
import pathlib
qa = pathlib.Path('stages/04-backtest/references/backtest_engine_qa.md').read_text()
for q in ['Q1','Q2','Q3','Q4','Q5','Q6']:
    assert f'## {q}' in qa, f'Missing {q}'
sr = pathlib.Path('shared/archetypes/zone_touch/simulation_rules.md').read_text()
assert 'trail_steps' in sr
assert 'SimResult' in sr
assert 'archetype: zone_touch' in sr
print('PASS: Both docs verified')
"</automated>
  </verify>
  <done>backtest_engine_qa.md has all 6 Q&A sections; simulation_rules.md covers entry/exit/trail/cost/SimResult for zone_touch</done>
</task>

<task type="auto">
  <name>Task 2: Write config_schema.json and config_schema.md</name>
  <files>stages/04-backtest/references/config_schema.json, stages/04-backtest/references/config_schema.md</files>
  <action>
Create config_schema.json as a complete, valid reference config for the zone_touch archetype on NQ P1 data. Use the exact structure from RESEARCH.md "Config JSON Schema" section:

```json
{
  "version": "v1",
  "instrument": "NQ",
  "touches_csv": "stages/01-data/data/touches/ZRA_Hist_P1.csv",
  "bar_data": "stages/01-data/data/bar_data/volume/NQ_BarData_250vol_P1.txt",
  "scoring_model_path": "shared/scoring_models/zone_touch_v1.json",
  "archetype": {
    "name": "zone_touch",
    "simulator_module": "zone_touch_simulator",
    "scoring_adapter": "BinnedScoringAdapter"
  },
  "active_modes": ["M1"],
  "routing": {
    "score_threshold": 48,
    "seq_limit": 3
  },
  "M1": {
    "stop_ticks": 135,
    "leg_targets": [50, 120, 240],
    "trail_steps": [
      {"trigger_ticks": 30, "new_stop_ticks": 0},
      {"trigger_ticks": 60, "new_stop_ticks": 20},
      {"trigger_ticks": 120, "new_stop_ticks": 50},
      {"trigger_ticks": 200, "new_stop_ticks": 100}
    ],
    "time_cap_bars": 80
  }
}
```

Start with M1 only as the minimal valid config. Document that additional modes (M3, M4, M5) follow the same per-mode schema.

Create config_schema.md with a markdown table documenting every field:

| Field | Type | FIXED/CANDIDATE | Valid Range | Description |
|-------|------|-----------------|-------------|-------------|

Mark as FIXED: version, instrument, touches_csv, bar_data, scoring_model_path, archetype.*, routing.*
Mark as CANDIDATE: Per-mode fields (stop_ticks, leg_targets, trail_steps, time_cap_bars)

Include a "Trail Step Validation Rules" section documenting the 5 rules:
1. 1-6 steps allowed
2. trigger_ticks strictly monotonically increasing
3. new_stop_ticks < trigger_ticks for each step
4. new_stop_ticks monotonically non-decreasing across steps
5. new_stop_ticks[0] >= 0

Include `last_reviewed: 2026-03-14` frontmatter on the .md file.
  </action>
  <verify>
    <automated>python -c "
import json, pathlib
# Validate JSON is parseable
cfg = json.loads(pathlib.Path('stages/04-backtest/references/config_schema.json').read_text())
assert 'version' in cfg
assert 'archetype' in cfg
assert 'trail_steps' in cfg.get('M1', {})
assert len(cfg['M1']['trail_steps']) == 4
# Validate doc coverage
doc = pathlib.Path('stages/04-backtest/references/config_schema.md').read_text()
for key in ['version','instrument','touches_csv','bar_data','scoring_model_path','archetype','active_modes','routing','stop_ticks','leg_targets','trail_steps','time_cap_bars']:
    assert key in doc, f'Missing field docs for {key}'
assert 'FIXED' in doc
assert 'CANDIDATE' in doc
assert 'Trail Step Validation' in doc
print('PASS: Config schema and docs verified')
"</automated>
  </verify>
  <done>config_schema.json parses cleanly with all required fields; config_schema.md documents every key with FIXED/CANDIDATE designation and trail step validation rules</done>
</task>

</tasks>

<verification>
- backtest_engine_qa.md exists with ## Q1 through ## Q6 headings, each with non-empty answer
- simulation_rules.md exists with archetype: zone_touch header and covers trail_steps, SimResult
- config_schema.json parses as valid JSON with all required fields
- config_schema.md covers every key in config_schema.json and includes FIXED/CANDIDATE designation
</verification>

<success_criteria>
All four reference documents exist and pass automated content checks. config_schema.json is a valid, loadable config that Plan 03 will use as the contract for the engine's validate_config() function.
</success_criteria>

<output>
After completion, create `.planning/phases/04-backtest-engine/04-01-SUMMARY.md`
</output>
