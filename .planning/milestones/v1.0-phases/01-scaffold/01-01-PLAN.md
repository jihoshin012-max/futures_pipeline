---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PREREQ-01, PREREQ-02, PREREQ-03, PREREQ-04]

must_haves:
  truths:
    - "RinDig ICM repo is cloned locally and CONTEXT.md conventions reviewed"
    - "karpathy/autoresearch repo is cloned locally and program.md + keep/revert logic understood"
    - "NQ bar data files exist in stages/01-data/data/bar_data/ for both P1 and P2"
    - "Touch/signal data files exist in stages/01-data/data/touches/ for both P1 and P2"
  artifacts:
    - path: "../Interpreted-Context-Methodology/"
      provides: "ICM convention reference"
    - path: "../autoresearch/"
      provides: "Autoresearch pattern reference"
    - path: "stages/01-data/data/bar_data/"
      provides: "NQ bar data for P1 and P2"
    - path: "stages/01-data/data/touches/"
      provides: "Touch/signal data for P1 and P2"
  key_links: []
---

<objective>
Fetch reference repos (ICM + autoresearch) and migrate source data to canonical pipeline locations.

Purpose: These are hard gates before any scaffold files can be written. The ICM repo conventions may affect CONTEXT.md format, and data must be in place before data_registry cross-references are authored.
Output: Cloned repos reviewed, data files migrated to canonical paths.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Futures_Pipeline_Functional_Spec.md (Tasks P0-2, P0-3, 1-23)
@Futures_Pipeline_Architecture_ICM.md (ICM repo reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone reference repos and create initial directory structure</name>
  <files>stages/, _config/, shared/, dashboard/, archive/, audit/, ../Interpreted-Context-Methdology/, ../autoresearch/</files>
  <action>
1. Clone the ICM repo to the parent directory of the pipeline project:
   ```
   cd .. && git clone https://github.com/RinDig/Interpreted-Context-Methdology
   ```
   Note: the repo name has a typo ("Methdology" not "Methodology") -- use the exact URL.

2. Clone the autoresearch repo to the same parent directory:
   ```
   cd .. && git clone https://github.com/karpathy/autoresearch
   ```

3. Create the pipeline directory structure (needed for data migration in next task). Run from the project root:
   ```bash
   mkdir -p stages/{01-data/{references,data/{touches,bar_data,labels},output},02-features/{references,autoresearch/current_best,output},03-hypothesis/{references,autoresearch/current_best,output/promoted_hypotheses},04-backtest/{references,autoresearch/current_best,output,p2_holdout},05-assessment/{references,output},06-deployment/{references,output},07-live/{data,output,triggers}}
   mkdir -p _config shared/{scoring_models,archetypes/zone_touch} dashboard archive audit
   ```
   Use `zone_touch` as the archetype name per architecture doc convention.

4. Review the ICM repo structure: check for CONTEXT.md format conventions, front-matter requirements, stage contract patterns. Note any differences from the functional spec's examples.

5. Review the autoresearch repo: read program.md format, train.py keep/revert logic, overnight run protocol. Note patterns the Phase 5-7 driver scripts must mirror.
  </action>
  <verify>
    <automated>ls ../Interpreted-Context-Methdology/ 2>/dev/null && echo "ICM OK" || echo "ICM MISSING"; ls ../autoresearch/program.md 2>/dev/null && echo "Autoresearch OK" || echo "Autoresearch MISSING"; ls stages/01-data/data/bar_data/ 2>/dev/null && echo "bar_data dir OK" || echo "bar_data dir MISSING"; ls _config/ 2>/dev/null && echo "_config dir OK" || echo "_config dir MISSING"; ls dashboard/ 2>/dev/null && echo "dashboard dir OK" || echo "dashboard dir MISSING"; ls audit/ 2>/dev/null && echo "audit dir OK" || echo "audit dir MISSING"</automated>
  </verify>
  <done>Both repos cloned. Directory structure created (stages/, _config/, shared/, dashboard/, archive/, audit/). ICM conventions and autoresearch keep/revert patterns reviewed and any convention differences from spec noted.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Migrate NQ bar data and touch data</name>
  <files>stages/01-data/data/bar_data/, stages/01-data/data/touches/</files>
  <action>
Human action required: migrate data files to canonical locations. Source paths are not in the spec -- only the user knows where the data files currently reside.

Steps for the user:
1. Copy NQ bar data files (pattern: NQ_BarData_*.txt) for P1 and P2 into stages/01-data/data/bar_data/
2. Copy touch/signal data files for P1 and P2 into stages/01-data/data/touches/
3. If there are derived label files, copy them to stages/01-data/data/labels/

After copying, verify:
   ls stages/01-data/data/bar_data/
   ls stages/01-data/data/touches/

Both should show files covering P1 (2025-09-16 to 2025-12-14) and P2 (2025-12-15 to 2026-03-02).
  </action>
  <verify>
    <automated>ls stages/01-data/data/bar_data/ 2>/dev/null | head -5 && echo "---" && ls stages/01-data/data/touches/ 2>/dev/null | head -5</automated>
  </verify>
  <done>Bar data files present in stages/01-data/data/bar_data/ for P1 and P2. Touch/signal data files present in stages/01-data/data/touches/ for P1 and P2.</done>
</task>

</tasks>

<verification>
- Both reference repos accessible locally
- stages/01-data/data/bar_data/ contains NQ bar data for P1 and P2
- stages/01-data/data/touches/ contains touch/signal data for P1 and P2
- ICM conventions reviewed; no blocking conflicts with spec
</verification>

<success_criteria>
All four PREREQ requirements verified: repos fetched and reviewed, bar data and touch data migrated to canonical locations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
