---
phase: 01-scaffold
plan: 06
type: execute
wave: 4
depends_on: [01-03, 01-04, 01-05]
files_modified:
  - dashboard/results_master.tsv
  - dashboard/index.html
  - audit/audit_log.md
  - audit/audit_entry.sh
  - stages/03-hypothesis/references/strategy_archetypes.md
autonomous: true
requirements: [SCAF-22, SCAF-23, SCAF-24, SCAF-25, SCAF-26]

must_haves:
  truths:
    - "results_master.tsv has exactly 24 tab-delimited columns in header row"
    - "dashboard/index.html renders a placeholder page in browser"
    - "audit_log.md has APPEND-ONLY header and first MANUAL_NOTE entry"
    - "audit_entry.sh supports promote, deploy, note, and fill commands"
    - "strategy_archetypes.md has template with simulator interface contract (run() signature)"
  artifacts:
    - path: "dashboard/results_master.tsv"
      provides: "24-column header for all experiment results"
      contains: "run_id"
    - path: "dashboard/index.html"
      provides: "Dashboard stub"
      contains: "Futures Pipeline"
    - path: "audit/audit_log.md"
      provides: "Append-only audit trail"
      contains: "APPEND-ONLY"
    - path: "audit/audit_entry.sh"
      provides: "CLI for audit entries"
      contains: "promote.*deploy.*note.*fill"
    - path: "stages/03-hypothesis/references/strategy_archetypes.md"
      provides: "Archetype registration with simulator contract"
      contains: "def run"
  key_links:
    - from: "audit/audit_entry.sh"
      to: "audit/audit_log.md"
      via: "Appends templated entries"
      pattern: "AUDIT_LOG"
    - from: "stages/03-hypothesis/references/strategy_archetypes.md"
      to: "shared/archetypes/zone_touch/"
      via: "Archetype entry references simulator module and archetype files"
      pattern: "simulator_module"
    - from: "dashboard/results_master.tsv"
      to: "stages/05-assessment/references/verdict_criteria.md"
      via: "TSV columns align with verdict metrics"
      pattern: "pf_p1.*verdict"
---

<objective>
Create dashboard stubs, audit infrastructure, and strategy archetypes registration.

Purpose: Dashboard results_master.tsv is the cross-stage experiment ledger. Audit infrastructure (log + entry script) provides append-only decision lineage. strategy_archetypes.md is the registration system for new strategy types. These are the final scaffold files.
Output: results_master.tsv (24-col header), index.html stub, audit_log.md + audit_entry.sh, strategy_archetypes.md.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Futures_Pipeline_Functional_Spec.md (Tasks 1-21, 1-22, 1-25, 1-26, 1-27)
@Futures_Pipeline_Architecture_ICM.md (audit_entry.sh full script lines 1215-1295, strategy_archetypes.md schema)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard stubs and audit infrastructure</name>
  <files>dashboard/results_master.tsv, dashboard/index.html, audit/audit_log.md, audit/audit_entry.sh</files>
  <action>
1. Write dashboard/results_master.tsv (Task 1-21):
   Create file with EXACTLY this single header row (24 tab-delimited columns):
   ```
   run_id	stage	timestamp	hypothesis_name	archetype	version	features	pf_p1	pf_p2	trades_p1	trades_p2	mwu_p	perm_p	pctile	n_prior_tests	verdict	sharpe_p1	max_dd_ticks	avg_winner_ticks	dd_multiple	win_rate	regime_breakdown	api_cost_usd	notes
   ```
   CRITICAL: Use literal tab characters between columns, not spaces. Verify with awk. Pitfall 5: TSV is hand-authored and tabs are invisible -- create programmatically or verify column count.

2. Write dashboard/index.html (Task 1-22):
   Minimal HTML stub:
   ```html
   <!DOCTYPE html>
   <html>
   <head><title>Futures Pipeline Dashboard</title></head>
   <body>
   <h1>Futures Pipeline Results</h1>
   <p>Dashboard stub -- implement after first autoresearch run produces results.</p>
   <p>Load: results_master.tsv | Sort by: PF, verdict, date | Filter by: archetype, verdict</p>
   </body>
   </html>
   ```

3. Write audit/audit_log.md (Task 1-25):
   ```markdown
   # Futures Pipeline Audit Log
   # APPEND-ONLY. Never delete or modify entries. Add correction notes instead.
   # Entries are generated by: pre-commit hook, post-commit hook, audit_entry.sh

   ---

   ## {current_date} | MANUAL_NOTE
   - subject: Pipeline created
   - detail: Futures Pipeline scaffold built per Futures_Pipeline_Functional_Spec.md v1.0.
             Pass 1 complete. All five pipeline rules in effect.
             No strategies in autoresearch yet. Gap A controls will activate before first autoresearch run.
   - human: Ji
   ```
   Replace {current_date} with actual date.

4. Write audit/audit_entry.sh (Task 1-26):
   Transcribe the full script from Futures_Pipeline_Architecture_ICM.md lines 1215-1295. Four commands:
   - `promote`: prompts for hypothesis_id, pf_p1, n_trades_p1, reason, alternatives_considered; appends HYPOTHESIS_PROMOTED entry
   - `deploy`: prompts for hypothesis_id, note; appends DEPLOYMENT_APPROVED with output_commit from git
   - `note`: prompts for subject, detail; appends MANUAL_NOTE
   - `fill`: opens audit_log.md at first TODO line in $EDITOR
   All write to `$(git rev-parse --show-toplevel)/audit/audit_log.md`.
   Make executable: chmod +x
  </action>
  <verify>
    <automated>awk -F'\t' 'NR==1{print NF}' dashboard/results_master.tsv && echo "---" && test -f dashboard/index.html && echo "index.html EXISTS" && echo "---" && grep -c "APPEND-ONLY" audit/audit_log.md && echo "---" && grep -c "promote\|deploy\|note\|fill" audit/audit_entry.sh && echo "---" && test -x audit/audit_entry.sh && echo "EXECUTABLE" || echo "NOT EXECUTABLE"</automated>
  </verify>
  <done>results_master.tsv has exactly 24 columns (awk confirms). index.html exists with placeholder. audit_log.md has APPEND-ONLY header and first MANUAL_NOTE. audit_entry.sh has all 4 commands and is executable (confirmed by test -x).</done>
</task>

<task type="auto">
  <name>Task 2: Create strategy_archetypes.md</name>
  <files>stages/03-hypothesis/references/strategy_archetypes.md</files>
  <action>
Write stages/03-hypothesis/references/strategy_archetypes.md (Task 1-27):

- Front-matter with last_reviewed
- Note: "Add new archetype here before running Stage 03 autoresearch for it."
- Note: "Required fields: simulator module, required data, optimization surface."

- Since this is a new strategy from scratch, do NOT populate with an active archetype entry yet. The spec says: "[Add first archetype here after completing NEW ARCHETYPE INTAKE checklist]". Leave this placeholder.

- Future archetype template block with ALL required fields:
  Description, Instrument (from instruments.md), Required data (source_ids from data_registry.md), simulator_module (conforming to run() interface), scoring_adapter (Binned|Sklearn|ONNX), feature_evaluator path, feature_engine path, Scoring model path, Optimization surface (params agent may vary), Structural reference path, Current status

- Simulator Interface Contract section (Option B -- dynamic dispatch):
  "backtest_engine.py loads simulators by module name at runtime via config.archetype.simulator_module."
  Interface: `def run(bar_df, touch_row, config, bar_offset) -> SimResult`
  Rules:
  - Pure function -- no I/O, no side effects, no global state
  - Returns SimResult dataclass with: pnl_ticks, win (bool), exit_reason, bars_held
  - Violation breaks backtest_engine.py and all autoresearch loops
  - Adding a new strategy = write a new module conforming to this interface

IMPORTANT (Pitfall from research): Do NOT create an archetype entry without a registered simulator module. The template is fine as a template. An actual entry requires the simulator to exist or be pointed to a valid module path.
  </action>
  <verify>
    <automated>grep -c "def run" stages/03-hypothesis/references/strategy_archetypes.md && echo "---" && grep -c "SimResult" stages/03-hypothesis/references/strategy_archetypes.md && echo "---" && grep "simulator_module" stages/03-hypothesis/references/strategy_archetypes.md | head -2</automated>
  </verify>
  <done>strategy_archetypes.md has future template with all required fields. Simulator Interface Contract present with run(bar_df, touch_row, config, bar_offset) -> SimResult signature. No active archetype entry created (correct for from-scratch build -- intake checklist required first).</done>
</task>

</tasks>

<verification>
- awk confirms results_master.tsv has exactly 24 columns
- index.html opens in browser with placeholder text
- audit_log.md has APPEND-ONLY header + first MANUAL_NOTE
- audit_entry.sh is executable (test -x confirms) with 4 commands
- strategy_archetypes.md has template and simulator interface contract
- No archetype entry without a registered simulator (anti-pattern avoided)
</verification>

<success_criteria>
SCAF-22 through SCAF-26 complete. All scaffold files for the pipeline are committed. Phase 1 scaffold layer is structurally complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-06-SUMMARY.md`
</output>
