---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - CLAUDE.md
  - CONTEXT.md
  - _config/instruments.md
  - _config/data_registry.md
  - _config/period_config.md
  - _config/pipeline_rules.md
  - _config/statistical_gates.md
  - _config/regime_definitions.md
  - _config/context_review_protocol.md
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03, SCAF-04, SCAF-05, SCAF-06, SCAF-07, SCAF-08, SCAF-09, SCAF-10]

must_haves:
  truths:
    - "Root directory structure exists with all stage dirs, _config, shared, dashboard, archive, audit"
    - "CLAUDE.md has 5 rules in first 20 lines and is <=60 lines"
    - "Root CONTEXT.md routes to active stage and is <=80 lines"
    - "All 7 _config/ files exist with correct content and NQ registered as active instrument"
    - "period_config.md has P1a/P1b split committed before any evaluation file"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Agent identity and pipeline rules"
      min_lines: 20
    - path: "CONTEXT.md"
      provides: "Pipeline routing file"
      min_lines: 15
    - path: "_config/instruments.md"
      provides: "NQ instrument registration"
      contains: "Tick size: 0.25"
    - path: "_config/data_registry.md"
      provides: "Data source registry"
      contains: "source_id"
    - path: "_config/period_config.md"
      provides: "IS/OOS period boundaries"
      contains: "P1a"
    - path: "_config/pipeline_rules.md"
      provides: "Five pipeline rules"
      contains: "Rule 4"
    - path: "_config/statistical_gates.md"
      provides: "Verdict thresholds and iteration budgets"
      contains: "Bonferroni"
    - path: "_config/regime_definitions.md"
      provides: "Three regime dimensions"
      contains: "Volatility"
    - path: "_config/context_review_protocol.md"
      provides: "Lost-in-middle mitigations"
      contains: "FRONT-LOADING"
  key_links:
    - from: "CONTEXT.md"
      to: "stages/*/CONTEXT.md"
      via: "stage routing table"
      pattern: "01-data.*Active"
    - from: "_config/period_config.md"
      to: "_config/statistical_gates.md"
      via: "P1a/P1b boundary used by replication rule"
      pattern: "P1a.*2025-09-16.*2025-10-31"
---

<objective>
Create the root directory structure, CLAUDE.md, root CONTEXT.md, and all 7 _config/ files.

Purpose: This is the foundation layer. Every downstream phase reads _config/ files. Period_config.md with P1a/P1b split must be committed early (STATE.md blocker). The _config/ files define the single source of truth for instruments, periods, rules, gates, and conventions.
Output: Complete _config/ directory, CLAUDE.md, root CONTEXT.md -- all committed.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Futures_Pipeline_Functional_Spec.md (Tasks 1-01 through 1-10 — exact content for every file)
@Futures_Pipeline_Architecture_ICM.md (directory tree, period_config full spec)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root structure, CLAUDE.md, CONTEXT.md, and period_config.md</name>
  <files>CLAUDE.md, CONTEXT.md, _config/period_config.md</files>
  <action>
1. Verify the directory structure from Plan 01 exists. If any directories are missing, create them:
   ```bash
   mkdir -p stages/{01-data/{references,data/{touches,bar_data,labels},output},02-features/{references,autoresearch/current_best,output},03-hypothesis/{references,autoresearch/current_best,output/promoted_hypotheses},04-backtest/{references,autoresearch/current_best,output,p2_holdout},05-assessment/{references,output},06-deployment/{references,output},07-live/{data,output,triggers}}
   mkdir -p _config shared/{scoring_models,archetypes/zone_touch} dashboard archive audit
   ```

2. Write CLAUDE.md with exact content from Functional Spec Task 1-02. Must be <=60 lines. Five pipeline rules in first 20 lines. Hard prohibitions section. Stage routing section. Autoresearch rule. Conventions line (archetype comment on line 1).

3. Write root CONTEXT.md with exact content from Functional Spec Task 1-03. Must be <=80 lines. First 5 lines after front-matter identify current active stage. Stage status table. Human checkpoints section.

4. Write _config/period_config.md with exact content from Functional Spec Task 1-06. Must include:
   - P1 IS: 2025-09-16 to 2025-12-14
   - P2 OOS: 2025-12-15 to 2026-03-02
   - Rolling forward instructions
   - Internal Replication Sub-periods: P1a = 2025-09-16 to 2025-10-31, P1b = 2025-11-01 to 2025-12-14
   - Grandfathering note

CRITICAL: period_config.md must be committed in this task BEFORE any evaluation file exists in the repository. This is a STATE.md blocker. Commit these three files immediately after writing.
  </action>
  <verify>
    <automated>wc -l CLAUDE.md && echo "---" && wc -l CONTEXT.md && echo "---" && wc -l _config/period_config.md && echo "---" && head -20 CLAUDE.md | grep -c "Rule\|rule" && echo "---" && grep "P1a" _config/period_config.md</automated>
  </verify>
  <done>CLAUDE.md <=60 lines with 5 rules in first 20. CONTEXT.md <=80 lines with active stage in first 5. period_config.md committed with P1a/P1b split before any evaluation files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create all remaining _config/ files</name>
  <files>_config/instruments.md, _config/data_registry.md, _config/pipeline_rules.md, _config/statistical_gates.md, _config/regime_definitions.md, _config/context_review_protocol.md</files>
  <action>
Write each file with exact content from the Functional Spec tasks:

1. _config/instruments.md (Task 1-04): NQ, ES, GC all registered with all required fields (Symbol, Tick size, Tick value, Session, Cost model, Bar data prefix, Margin). Template for new instruments at bottom. Cost model present with warning note.

2. _config/data_registry.md (Task 1-05): THE RULE section (no hardcoded paths). Registered Sources table with at minimum bar_data (type: price) registered. Also register the touch/signal data source -- use source_id consistent with what Plan 01 discovered about the actual data files. Include Data Type Taxonomy table (touches, price, label, orderflow, fundamental, alt). Include "To Add a New Source" workflow.

   IMPORTANT: Each source_id registered here MUST have a matching schema file created in Plan 04 (stages/01-data/references/{source_id}_schema.md). Use exact string match for source_id.

3. _config/pipeline_rules.md (Task 1-07): All five rules:
   - Rule 1: P1 Calibrate
   - Rule 2: P2 One-Shot
   - Rule 3: Entry-Time Only
   - Rule 4: Internal Replication (with grandfathering note)
   - Rule 5: Instrument Constants from Registry

4. _config/statistical_gates.md (Task 1-08): Baseline Verdict Thresholds table (PF, Min trades, MWU p, Permutation p, Percentile rank, Max drawdown). Drawdown Gate Notes. Multiple Testing Controls with Iteration Budgets (Stage 02=300, Stage 03=200, Stage 04=500) and Bonferroni-Adjusted P-value Gates table (4 rows). n_prior_tests Implementation note.

5. _config/regime_definitions.md (Task 1-09): Purpose section. Tagging section (regime_labels.csv location, columns). Three dimensions: Trend (ADX thresholds), Volatility (ATR relative thresholds), Macro (event_day/normal_day). Stage 05 Usage rules (n_trades >= 20 per bucket, flag regime-dependence).

6. _config/context_review_protocol.md (Task 1-10): FILE LENGTH LIMITS table (CLAUDE.md 60, CONTEXT.md 80, program.md 30). FRONT-LOADING RULE with good/bad examples. LOCAL REPETITION RULE. STALENESS FLAG (front-matter spec). WHEN TO REVIEW triggers. REVIEW CHECKLIST. PERSISTENT STATE VS WORKING MEMORY note.

Apply lost-in-middle authoring to every file: operative instruction in first 5 lines after any front-matter.
  </action>
  <verify>
    <automated>for f in instruments.md data_registry.md pipeline_rules.md statistical_gates.md regime_definitions.md context_review_protocol.md; do echo "=== $f ==="; test -f "_config/$f" && echo "EXISTS" || echo "MISSING"; done && echo "---" && grep -c "^## Rule" _config/pipeline_rules.md && echo "---" && grep -c "Tick size\|Tick value" _config/instruments.md</automated>
  </verify>
  <done>All 7 _config/ files exist. instruments.md has NQ with all fields. pipeline_rules.md has 5 rules with grandfathering note. statistical_gates.md has thresholds + budgets + Bonferroni gates. data_registry.md has registered sources with source_ids matching future schema files.</done>
</task>

</tasks>

<verification>
- `find . -type d | head -40` shows full directory tree
- CLAUDE.md <=60 lines, 5 rules in first 20 lines
- CONTEXT.md <=80 lines, active stage in first 5 lines
- All 7 _config/ files present with required content
- period_config.md committed before any evaluation file (check git log)
- grep confirms: 5 rules in pipeline_rules.md, 3 iteration budget rows in statistical_gates.md, 3 regime dimensions in regime_definitions.md
</verification>

<success_criteria>
SCAF-01 through SCAF-10 complete. Root structure, CLAUDE.md, CONTEXT.md, and all _config/ files committed and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-02-SUMMARY.md`
</output>
