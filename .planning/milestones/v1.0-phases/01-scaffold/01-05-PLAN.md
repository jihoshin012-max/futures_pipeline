---
phase: 01-scaffold
plan: 05
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - stages/05-assessment/CONTEXT.md
  - stages/05-assessment/references/verdict_criteria.md
  - stages/05-assessment/references/statistical_tests.md
  - stages/06-deployment/CONTEXT.md
  - stages/06-deployment/references/context_package_spec.md
  - stages/06-deployment/assemble_context.sh
  - stages/07-live/CONTEXT.md
  - stages/07-live/triggers/review_triggers.md
autonomous: true
requirements: [SCAF-19, SCAF-20, SCAF-21]

must_haves:
  truths:
    - "Stage 05 CONTEXT.md computes metrics deterministically with multiple testing adjustment"
    - "verdict_criteria.md thresholds match statistical_gates.md values"
    - "statistical_tests.md specifies MWU, permutation test, and random percentile rank"
    - "Stage 06 CONTEXT.md defines deployment assembly with human gate"
    - "assemble_context.sh assembles 6 sections from pipeline artifacts without hardcoded archetype names"
    - "Stage 07 CONTEXT.md is monitor-only with review triggers in separate file"
  artifacts:
    - path: "stages/05-assessment/CONTEXT.md"
      provides: "Stage 05 assessment contract"
      contains: "verdict_report.md"
    - path: "stages/05-assessment/references/verdict_criteria.md"
      provides: "Verdict thresholds"
      contains: "YES.*CONDITIONAL.*NO"
    - path: "stages/05-assessment/references/statistical_tests.md"
      provides: "3 statistical test specs"
      contains: "Mann-Whitney"
    - path: "stages/06-deployment/CONTEXT.md"
      provides: "Stage 06 deployment contract"
      contains: "assemble_context.sh"
    - path: "stages/06-deployment/references/context_package_spec.md"
      provides: "ACSIL generation context spec"
      contains: "Frozen Parameters"
    - path: "stages/06-deployment/assemble_context.sh"
      provides: "Context assembly script"
      contains: "frozen_params.json"
    - path: "stages/07-live/CONTEXT.md"
      provides: "Stage 07 monitoring contract"
      contains: "paper_trades.csv"
    - path: "stages/07-live/triggers/review_triggers.md"
      provides: "Live monitoring trigger thresholds"
      contains: "Consecutive stop-outs"
  key_links:
    - from: "stages/05-assessment/references/verdict_criteria.md"
      to: "_config/statistical_gates.md"
      via: "Local copy of gates for agent access"
      pattern: "statistical_gates.md"
    - from: "stages/06-deployment/assemble_context.sh"
      to: "stages/04-backtest/output/frozen_params.json"
      via: "Reads archetype from frozen_params at runtime"
      pattern: "frozen_params.json"
---

<objective>
Create CONTEXT.md files for Stages 05-07 plus all their reference and supporting files.

Purpose: Stage 05 needs verdict criteria matching statistical_gates.md and statistical test specs. Stage 06 needs the context assembly script and package spec. Stage 07 needs review triggers in a separate file. These complete the stage contract layer.
Output: 3 stage CONTEXT.md files, verdict_criteria.md, statistical_tests.md, context_package_spec.md, assemble_context.sh, review_triggers.md.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Futures_Pipeline_Functional_Spec.md (Tasks 1-18, 1-18b, 1-19, 1-19b, 1-20, 1-20b)
@Futures_Pipeline_Architecture_ICM.md (assemble_context.sh full implementation)

<interfaces>
<!-- From _config/statistical_gates.md (created in Plan 02): -->
<!-- verdict_criteria.md must use identical thresholds -->
<!-- PF >= 2.5 (YES), >= 1.5 (CONDITIONAL); MWU p < 0.05 (YES), < 0.10 (CONDITIONAL) -->
<!-- Bonferroni gates: <=10 -> 0.05, 11-50 -> 0.02, 51-200 -> 0.01, >200 -> NO -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Stage 05 CONTEXT.md and reference files</name>
  <files>stages/05-assessment/CONTEXT.md, stages/05-assessment/references/verdict_criteria.md, stages/05-assessment/references/statistical_tests.md</files>
  <action>
1. Write stages/05-assessment/CONTEXT.md (Task 1-18):
   - Front-matter, <=80 lines
   - First 5 lines: "Stage 05: Statistical Assessment" + "YOUR TASK: Compute metrics and render verdict. Deterministic -- no exploration." + "CONSTRAINT: You do not modify backtest outputs. You read them and compute."
   - I/O table: Inputs (trade_log.csv, data_manifest.json), Process (compute PF, MWU, permutation, percentile; apply adjusted p-value gate), Outputs (verdict_report.md, verdict_report.json, statistical_summary.md, feedback_to_hypothesis.md), Human checkpoint (none -- deterministic)
   - MULTIPLE TESTING local repeat: read n_prior_tests, apply adjusted gate, >200 auto-NO
   - statistical_summary.md OUTPUT SPEC with two sections:
     a. Verdict metrics (gates apply): profit_factor, n_trades, win_rate, mwu_p, permutation_p, percentile_rank, max_drawdown_ticks, avg_winner_ticks, drawdown_multiple
     b. Reporting metrics (logged, not gated): sharpe_ratio (trade-level, no annualization), avg_loser_ticks, win_loss_ratio, longest_losing_streak, longest_flat_bars, regime_breakdown
   - Sharpe implementation note: mean(trade_pnl_ticks) / std(trade_pnl_ticks) * sqrt(n_trades). Flag unreliable if n_trades < 30.

2. Write stages/05-assessment/references/verdict_criteria.md (Task 1-18b):
   - Note: "Source of truth: _config/statistical_gates.md. This file is a local copy for agent access."
   - Verdict logic (all gates must pass):
     YES: PF >= 2.5 AND trades >= 50 AND MWU p < 0.05 AND perm p < 0.05 AND pctile >= 99 AND dd_multiple < 3
     CONDITIONAL: PF >= 1.5 AND trades >= 30 AND MWU p < 0.10 AND perm p < 0.10 AND pctile >= 95 AND dd_multiple < 5
     NO: anything below, OR n_prior_tests > 200, OR replication_pass == false
   - Drawdown gate: dd_multiple = max_drawdown_ticks / avg_winner_ticks
   - Multiple testing adjustment: Bonferroni-adjusted thresholds from statistical_gates.md

3. Write stages/05-assessment/references/statistical_tests.md (Task 1-18b):
   - Mann-Whitney U test: null hypothesis (PnL not different from zero-mean), input (trade_pnl_ticks), two-tailed, 4 decimal places, flag unreliable if n_trades < 20
   - Permutation test: shuffle 10,000 times, recompute PF, report percentile rank and p-value
   - Random percentile rank: compare to 10,000 random strategies on same data, same trade count, report percentile
  </action>
  <verify>
    <automated>wc -l stages/05-assessment/CONTEXT.md && echo "---" && grep -c "YES\|CONDITIONAL\|NO" stages/05-assessment/references/verdict_criteria.md && echo "---" && grep -c "Mann-Whitney\|Permutation\|percentile" stages/05-assessment/references/statistical_tests.md</automated>
  </verify>
  <done>Stage 05 CONTEXT.md <=80 lines with deterministic assessment contract. verdict_criteria.md thresholds match statistical_gates.md. statistical_tests.md has all 3 test specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Create Stage 06-07 CONTEXT.md files and supporting files</name>
  <files>stages/06-deployment/CONTEXT.md, stages/06-deployment/references/context_package_spec.md, stages/06-deployment/assemble_context.sh, stages/07-live/CONTEXT.md, stages/07-live/triggers/review_triggers.md</files>
  <action>
1. Write stages/06-deployment/CONTEXT.md (Task 1-19):
   - Front-matter, <=80 lines
   - First 5 lines: "Stage 06: Deployment Builder" + "YOUR TASK: Assemble context package for ACSIL generation. Human fires the prompt." + "CONSTRAINT: Human must create deployment_ready.flag. You never create it."
   - I/O table: Inputs (frozen_params.json, scoring_models/, feature_definitions.md), Process (assemble_context.sh -> human fires Claude Code prompt), Outputs (.cpp files, deployment_checklist.md), Human gate
   - WHAT THIS STAGE IS: context assembly, not automated code generation
   - DEPLOYMENT CHECKLIST: assemble_context.sh run, Claude Code prompt fired, .cpp reviewed, compiled, replay verified, audit_entry.sh deploy invoked, deployment_ready.flag created

2. Write stages/06-deployment/references/context_package_spec.md (Task 1-19b):
   - 5 required sections in order: Strategy identity (hypothesis_id, archetype, verdict), Frozen parameters (copy verbatim), Features used (relevant entries only), Structural reference (from strategy_archetypes.md), Output instructions (folder, file count, no magic numbers)
   - What NOT to include: experiment history, trade logs, IS/OOS config

3. Write stages/06-deployment/assemble_context.sh (Task 1-19b):
   - Full bash script from architecture doc lines 1215-1360
   - Usage: bash assemble_context.sh <strategy_id>
   - Reads archetype from frozen_params.json at runtime (NOT hardcoded -- Pitfall 6)
   - Reads structural_reference path from strategy_archetypes.md for the active archetype
   - Assembles 6 sections: Verdict, Frozen Parameters, Scoring Model, Features Used, Generation Instructions, Structural Reference
   - Make executable: chmod +x

4. Write stages/07-live/CONTEXT.md (Task 1-20):
   - Front-matter, <=80 lines
   - First 5 lines: "Stage 07: Live Performance Monitor" + "YOUR TASK: Compare live trades to backtest expectations. Monitor only." + "CONSTRAINT: No autoresearch. No code generation. No backtest runs."
   - I/O table: Inputs (paper_trades.csv), Baseline (verdict_report.json), Outputs (live_assessment.md, drift_report.md), Human checkpoint (monthly review + immediate on trigger)
   - REVIEW TRIGGERS: reference triggers/review_triggers.md file (not inline)
   - LIVE DATA PROMOTION: after 200+ trades, flag for IS promotion

5. Write stages/07-live/triggers/review_triggers.md (Task 1-20b):
   - Active Triggers table: Live PF vs backtest PF (diverges >40% after 50+ trades), Consecutive stop-outs (8+), Max drawdown exceeded (>2x backtest DD), Trade count below expected (<5 signals/month)
   - Escalation: create MANUAL_NOTE in audit_log.md first
   - Promotion Trigger: 200+ live trades -> flag for IS promotion evaluation
  </action>
  <verify>
    <automated>for f in stages/06-deployment/CONTEXT.md stages/06-deployment/references/context_package_spec.md stages/06-deployment/assemble_context.sh stages/07-live/CONTEXT.md stages/07-live/triggers/review_triggers.md; do echo "=== $f ==="; wc -l "$f" 2>/dev/null || echo "MISSING"; done && echo "---" && test -x stages/06-deployment/assemble_context.sh 2>/dev/null && echo "EXECUTABLE" || echo "NOT EXECUTABLE"</automated>
  </verify>
  <done>Stage 06 CONTEXT.md <=80 lines with deployment checklist. context_package_spec.md has 5 required sections. assemble_context.sh is executable and reads archetype from frozen_params.json at runtime. Stage 07 CONTEXT.md <=80 lines, monitor-only. review_triggers.md has 4 trigger thresholds and escalation rules.</done>
</task>

</tasks>

<verification>
- All 3 stage CONTEXT.md files <=80 lines with front-matter and operative instruction in first 5 lines
- verdict_criteria.md thresholds match statistical_gates.md
- statistical_tests.md has 3 test specs
- assemble_context.sh is executable and does not hardcode archetype names
- review_triggers.md has trigger table and escalation rules
</verification>

<success_criteria>
SCAF-19 through SCAF-21 complete. Stages 05-07 have CONTEXT.md files with all supporting reference docs and scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-05-SUMMARY.md`
</output>
