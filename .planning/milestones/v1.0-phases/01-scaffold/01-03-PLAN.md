---
phase: 01-scaffold
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - shared/feature_definitions.md
  - stages/02-features/references/feature_rules.md
  - stages/02-features/references/feature_catalog.md
  - shared/scoring_models/_template.json
  - shared/scoring_models/scoring_adapter.py
autonomous: true
requirements: [SCAF-11, SCAF-12, SCAF-13, SCAF-14]

must_haves:
  truths:
    - "feature_definitions.md exists with entry-time rule and empty registered features"
    - "feature_rules.md has 5 rules and is <=30 lines"
    - "feature_catalog.md has active/dropped/dead-end table structure"
    - "scoring_models/ has _template.json with all schema fields and scoring_adapter.py with 3 adapter stubs"
  artifacts:
    - path: "shared/feature_definitions.md"
      provides: "Feature registration with entry-time rule"
      contains: "Entry-time computable"
    - path: "stages/02-features/references/feature_rules.md"
      provides: "5 feature rules for Stage 02 agent"
      min_lines: 15
    - path: "stages/02-features/references/feature_catalog.md"
      provides: "Feature history tracking"
      contains: "Dead ends"
    - path: "shared/scoring_models/_template.json"
      provides: "Scoring model JSON schema"
      contains: "bin_edges"
    - path: "shared/scoring_models/scoring_adapter.py"
      provides: "Protocol + 3 adapter stubs + factory"
      contains: "class BinnedScoringAdapter"
  key_links:
    - from: "stages/02-features/references/feature_rules.md"
      to: "shared/feature_definitions.md"
      via: "Rule 5 requires registration in feature_definitions.md"
      pattern: "feature_definitions.md"
    - from: "shared/scoring_models/scoring_adapter.py"
      to: "shared/scoring_models/_template.json"
      via: "BinnedScoringAdapter loads JSON matching template schema"
      pattern: "model_path"
---

<objective>
Create shared resources: feature definitions, feature rules, feature catalog, and scoring model infrastructure.

Purpose: These files define the cross-stage contracts for feature management and scoring. Stage 02 autoresearch reads feature_rules.md and feature_catalog.md. The scoring adapter interface is consumed by backtest_engine.py in Phase 4.
Output: shared/feature_definitions.md, feature rules/catalog in 02-features/references/, scoring model template + adapter stubs.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Futures_Pipeline_Functional_Spec.md (Tasks 1-11, 1-11b, 1-11c, 1-12)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feature definitions, rules, and catalog</name>
  <files>shared/feature_definitions.md, stages/02-features/references/feature_rules.md, stages/02-features/references/feature_catalog.md</files>
  <action>
1. Write shared/feature_definitions.md (Task 1-11):
   - Front-matter with last_reviewed date
   - Entry-time computability hard rule stated at top (first 5 lines)
   - "From scratch" note: starts with template only, no pre-registered features
   - Registered Features section showing "(empty)"
   - Template for new features section with fields: Source, Computation, Bin edges, Entry-time computable (YES/NO), Used by

2. Write stages/02-features/references/feature_rules.md (Task 1-11b):
   - MUST be <=30 lines total
   - Front-matter + note that Stage 02 agent reads before each experiment
   - Rule 1: Entry-time only (Pipeline Rule 3)
   - Rule 2: Registered sources only (from data_registry.md)
   - Rule 3: Keep threshold (spread > 0.15 AND MWU p < 0.10)
   - Rule 4: One feature per experiment
   - Rule 5: Register before use (add to feature_definitions.md)

3. Write stages/02-features/references/feature_catalog.md (Task 1-11c):
   - No line limit (reference doc)
   - Front-matter
   - Active features table (empty -- "none yet")
   - Explored and dropped table (empty -- "populate as Stage 02 runs")
   - Dead ends section (empty -- "Human fills after overnight runs")
  </action>
  <verify>
    <automated>wc -l stages/02-features/references/feature_rules.md && echo "---" && grep -c "Entry-time" shared/feature_definitions.md && echo "---" && grep -c "Dead ends" stages/02-features/references/feature_catalog.md</automated>
  </verify>
  <done>feature_definitions.md exists with entry-time rule and empty features. feature_rules.md <=30 lines with 5 rules. feature_catalog.md has 3-table structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create scoring model template and adapter stubs</name>
  <files>shared/scoring_models/_template.json, shared/scoring_models/scoring_adapter.py</files>
  <action>
1. Write shared/scoring_models/_template.json (Task 1-12):
   ```json
   {
     "model_id": "{archetype}_{variant}",
     "frozen_date": "YYYY-MM-DD",
     "max_score": 0,
     "threshold": 0,
     "description": "Frozen scoring weights for {archetype} {variant} -- do not modify after freezing",
     "weights": {
       "{feature_name}": { "bins": ["low","mid","high"], "scores": [0, 0, 0] }
     },
     "bin_edges": {
       "{feature_name}": [0.0, 0.0]
     }
   }
   ```

2. Write shared/scoring_models/scoring_adapter.py (Task 1-12):
   - Module docstring: "All adapters expose: score(touch_df) -> pd.Series[float]"
   - ScoringAdapter Protocol with score() method
   - BinnedScoringAdapter class with __init__(model_path: str) and score(touch_df) -> pd.Series
   - SklearnScoringAdapter class with same interface
   - ONNXScoringAdapter class with same interface
   - load_scoring_adapter(model_path, adapter_type) -> ScoringAdapter factory function

   Method bodies should raise NotImplementedError("Implement in Phase 4") -- NOT pass or return None. This follows Pitfall 4 from the research: stubs must raise, not silently succeed.

   Import Protocol from typing and pd from pandas. These are stubs; no actual implementation beyond the raise.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('shared/scoring_models/scoring_adapter.py').read()); print('SYNTAX OK')" && echo "---" && grep -c "class.*Adapter" shared/scoring_models/scoring_adapter.py && echo "---" && python3 -c "import json; j=json.load(open('shared/scoring_models/_template.json')); print('bin_edges' in j and 'weights' in j)"</automated>
  </verify>
  <done>_template.json has all required fields (model_id, frozen_date, max_score, threshold, weights, bin_edges). scoring_adapter.py has Protocol + 3 adapter classes + factory, all with NotImplementedError stubs. Python syntax validates.</done>
</task>

</tasks>

<verification>
- All 4 files exist at correct paths
- feature_rules.md <=30 lines
- scoring_adapter.py parses as valid Python
- _template.json parses as valid JSON
- grep confirms 3+ adapter classes in scoring_adapter.py
</verification>

<success_criteria>
SCAF-11 through SCAF-14 complete. Feature management and scoring model infrastructure committed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-03-SUMMARY.md`
</output>
