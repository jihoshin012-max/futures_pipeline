---
phase: 06-stage-02-autoresearch
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - stages/02-features/autoresearch/current_best/feature_engine.py
  - stages/02-features/freeze_features.py
  - stages/02-features/output/.gitkeep
autonomous: false
requirements: [AUTO-06, AUTO-08]

must_haves:
  truths:
    - "Driver runs 20 experiments end-to-end against real P1 data and results.tsv has 20+ rows with spread values populated"
    - "An entry-time violation canary (feature reading post-entry bar) is detected and logged as entry_time_violation verdict"
    - "Budget enforcement stops the driver when n_prior_tests >= budget (verified with budget=3)"
    - "Freeze script reads current_best/feature_engine.py and writes frozen_features.json"
  artifacts:
    - path: "stages/02-features/autoresearch/results.tsv"
      provides: "Experiment log with spread values"
      contains: "02-features"
    - path: "stages/02-features/autoresearch/current_best/feature_engine.py"
      provides: "Seeded baseline for overnight run"
      contains: "compute_features"
    - path: "stages/02-features/freeze_features.py"
      provides: "Human-triggered freeze script"
      contains: "frozen_features.json"
  key_links:
    - from: "stages/02-features/autoresearch/driver.py"
      to: "stages/02-features/autoresearch/evaluate_features.py"
      via: "subprocess invocation"
      pattern: "evaluate_features.py"
    - from: "stages/02-features/autoresearch/driver.py"
      to: "stages/02-features/autoresearch/results.tsv"
      via: "_append_tsv_row"
      pattern: "results.tsv"
---

<objective>
Wire driver + evaluator end-to-end, seed the baseline, run overnight smoke test, and deliver the freeze script.

Purpose: Validate the complete Stage 02 autoresearch pipeline works on real data before handing off for overnight runs. The smoke test proves the loop, budget enforcement, and entry-time canary all work together.

Output: Seeded current_best/, populated results.tsv, freeze_features.py, human-verified overnight smoke test
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stage-02-autoresearch/06-CONTEXT.md
@.planning/phases/06-stage-02-autoresearch/06-RESEARCH.md
@.planning/phases/06-stage-02-autoresearch/06-01-SUMMARY.md
@.planning/phases/06-stage-02-autoresearch/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From shared/archetypes/zone_touch/feature_engine.py:
```python
def compute_features(bar_df, touch_row) -> dict:
    # Returns {'zone_width': float, ...}
```

From shared/archetypes/zone_touch/feature_evaluator.py:
```python
def evaluate() -> dict:
    # Returns {"features": [{"name", "spread", "mwu_p", "kept", "entry_time_violation"}], "n_touches": int}
    # entry_time_violation is per-feature boolean, flows through dispatcher unchanged
```

<!-- From Plan 02 outputs -->
From stages/02-features/autoresearch/driver.py:
```python
def run_loop(autoresearch_dir, repo_root, max_iterations=None)
# CLI: python driver.py --archetype zone_touch --n-experiments 20
```

From stages/02-features/autoresearch/driver.py:
```python
def parse_program_md(path) -> dict  # keys: metric, keep_rule, budget, new_feature
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed baseline, create freeze script, and run budget enforcement smoke test</name>
  <files>stages/02-features/autoresearch/current_best/feature_engine.py, stages/02-features/freeze_features.py, stages/02-features/output/.gitkeep</files>
  <action>
**Step 1 — Seed current_best/feature_engine.py:**
Copy shared/archetypes/zone_touch/feature_engine.py to stages/02-features/autoresearch/current_best/feature_engine.py. This is the baseline that the driver will revert to on failed experiments.

**Step 2 — Create output/ directory:**
Create stages/02-features/output/.gitkeep (empty file to track the directory in git). This is where frozen_features.json will be written after human approval.

**Step 3 — Create freeze_features.py** at stages/02-features/freeze_features.py:
- Purpose: Human runs this after reviewing results.tsv to freeze approved features
- Implementation:
  1. Load current_best/feature_engine.py via importlib.util.spec_from_file_location
  2. Create a dummy bar_df (5 rows of OHLCV data) and dummy touch_row (with ZoneTop, ZoneBot, BarIndex)
  3. Call compute_features(dummy_bar_df, dummy_touch_row) to get feature name keys
  4. Write frozen_features.json to stages/02-features/output/ with: {"features": ["zone_width", ...], "frozen_date": "YYYY-MM-DD", "source": "current_best/feature_engine.py"}
  5. Print instructions: "Copy frozen_features.json to stages/03-hypothesis/references/ for Stage 03 consumption"
- CLI: `python stages/02-features/freeze_features.py [--current-best-dir DIR]`

**Step 4 — Run budget enforcement smoke test:**
Temporarily set program.md BUDGET to 3. Run: `python stages/02-features/autoresearch/driver.py --archetype zone_touch --n-experiments 5`
Verify: driver stops at 3 experiments (budget cap), not 5 (hard cap). Check results.tsv has exactly 3 rows. Then reset program.md BUDGET back to 300.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('stages/02-features/autoresearch/current_best/feature_engine.py'); assert p.exists(), 'current_best not seeded'; p2=Path('stages/02-features/freeze_features.py'); assert p2.exists(), 'freeze script missing'; p3=Path('stages/02-features/output/.gitkeep'); assert p3.exists(), 'output dir missing'; print('OK')"</automated>
  </verify>
  <done>
    - current_best/feature_engine.py seeded with zone_width baseline
    - freeze_features.py can extract feature names and write frozen_features.json
    - output/ directory exists for frozen_features.json
    - Budget enforcement verified: driver stops at budget=3
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of 20-experiment overnight smoke test</name>
  <files>stages/02-features/autoresearch/results.tsv</files>
  <action>
Human verifies the complete Stage 02 autoresearch pipeline works end-to-end on real data. Claude runs the 20-experiment smoke test before presenting for verification.

Before checkpoint: Run `python stages/02-features/autoresearch/driver.py --archetype zone_touch --n-experiments 20` and confirm results.tsv has 20 rows. Also run `python stages/02-features/freeze_features.py` to verify frozen_features.json generation.

Human verification steps:
1. Run 20-experiment overnight smoke test:
   `python stages/02-features/autoresearch/driver.py --archetype zone_touch --n-experiments 20`
2. Check results.tsv has 20 rows with spread values:
   `type stages\02-features\autoresearch\results.tsv`
   Verify: each row has stage='02-features', a float spread value in pf_p1 column, mwu_p populated, verdict is kept/reverted (not errors)
3. Verify entry-time canary (optional — tests already cover this):
   The canary was validated by automated tests in Plan 01. If you want manual confirmation, temporarily edit feature_engine.py to add `bar_df.iloc[int(touch_row['BarIndex']) + 100]` and re-run one experiment — should see 'entry_time_violation' verdict.
4. Verify freeze script works:
   `python stages/02-features/freeze_features.py`
   `type stages\02-features\output\frozen_features.json`
   Should show JSON with "features": ["zone_width"], "frozen_date", "source"
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('stages/02-features/autoresearch/results.tsv'); assert p.exists(), 'results.tsv missing'; lines=[l for l in p.read_text().splitlines() if l.strip()]; assert len(lines) >= 21, f'Expected 21+ lines (1 header + 20 data rows), got {len(lines)}'; cols=lines[1].split('\t'); spread_col=[c for c in cols if c.replace('.','',1).replace('-','',1).isdigit()]; print(f'results.tsv: {len(lines)-1} data rows OK')"</automated>
  </verify>
  <done>
    - 20-experiment smoke test completes without error
    - results.tsv has 21+ lines (header + 20 data rows) with numeric spread values
    - Human approves the end-to-end pipeline behavior
    - freeze_features.py writes valid frozen_features.json
  </done>
</task>

</tasks>

<verification>
- results.tsv exists with 20+ data rows, each with spread and mwu_p populated
- Budget enforcement works (verified with budget=3)
- freeze_features.py writes valid frozen_features.json
- Full test suite green: `pytest tests/test_feature_evaluator.py tests/test_stage02_driver.py -x -q`
</verification>

<success_criteria>
- 20-experiment smoke test completes without error
- Every results.tsv row has a numeric spread value (not empty, not NaN)
- Entry-time violations are logged correctly (tested in Plan 01)
- Driver stops at budget limit (tested in Task 1)
- Human approves the end-to-end pipeline behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-stage-02-autoresearch/06-03-SUMMARY.md`
</output>
