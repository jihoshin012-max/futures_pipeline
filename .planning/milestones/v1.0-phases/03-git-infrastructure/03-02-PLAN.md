---
phase: 03-git-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - autocommit.sh
autonomous: false
requirements:
  - GIT-04

must_haves:
  truths:
    - "A file change followed by 30s produces a new auto: commit in git log"
    - "A commit touching p2_holdout/ is rejected by pre-commit hook"
    - "A commit deleting audit_log.md lines is rejected by pre-commit hook"
    - "commit_log.txt is written after every commit"
  artifacts:
    - path: ".git/hooks/pre-commit"
      provides: "Executable pre-commit hook"
    - path: ".git/hooks/post-commit"
      provides: "Executable post-commit hook"
    - path: "autocommit.sh"
      provides: "Polling autocommit watcher"
    - path: ".git/commit_log.txt"
      provides: "Commit log written by post-commit hook"
  key_links:
    - from: "autocommit.sh"
      to: ".git/hooks/pre-commit"
      via: "git commit triggers pre-commit"
      pattern: "git commit"
    - from: "autocommit.sh"
      to: ".git/hooks/post-commit"
      via: "successful commit triggers post-commit"
      pattern: "git commit"
---

<objective>
Verify git infrastructure end-to-end: run the automated test suite, then perform the manual verification sequence from the functional spec to confirm autocommit fires, holdout guard blocks, and commit log is written in the live repo.

Purpose: GIT-04 requires infrastructure verification before Phase 3 can be marked complete. Automated tests cover hook logic; manual verification confirms the autocommit watcher works in real-time.

Output: Verified and operational git infrastructure.
</objective>

<execution_context>
@C:/Users/jshin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jshin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-git-infrastructure/03-RESEARCH.md
@.planning/phases/03-git-infrastructure/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files>autocommit.sh</files>
  <action>
Run the full test suite to confirm all git infrastructure tests pass:

1. `python -m pytest tests/test_git_infrastructure.py -v` -- all hook tests must pass
2. `python -m pytest tests/ -x -q` -- full suite must pass (no regressions from Plan 01)
3. Verify file permissions: `ls -la .git/hooks/pre-commit .git/hooks/post-commit autocommit.sh`
4. Verify no grep -P in hooks: `grep -c "grep -P" .git/hooks/post-commit .git/hooks/pre-commit` should return 0
5. Verify no python3 in hooks: `grep -c "python3" .git/hooks/post-commit .git/hooks/pre-commit` should return 0

If any test fails, diagnose and fix before proceeding. Do not skip tests.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Full test suite passes. All hook files are executable. No grep -P or python3 in hooks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of git infrastructure</name>
  <files>autocommit.sh, .git/hooks/pre-commit, .git/hooks/post-commit</files>
  <action>
Human verifies autocommit watcher fires, holdout guard blocks, and commit log is written in the live repo. This is the GIT-04 verification sequence from the functional spec.
  </action>
  <verify>Human confirms all three manual tests pass per the how-to-verify steps below.</verify>
  <done>Autocommit fires within 30s. Holdout guard rejects p2_holdout commits. Commit log is written.</done>
  <what-built>
Git infrastructure: autocommit.sh watcher, pre-commit holdout guard + audit enforcement, post-commit commit log + audit auto-entries. All automated tests pass.
  </what-built>
  <how-to-verify>
**Test 1 -- Autocommit fires (GIT-01):**
1. Open a terminal in C:\Projects\pipeline
2. Run: `bash autocommit.sh &`
3. Run: `echo "test autocommit" >> test_autocommit_temp.txt`
4. Wait 35 seconds
5. Run: `git log --oneline -3`
6. Confirm: most recent commit starts with `auto:` and mentions `test_autocommit_temp.txt`
7. Clean up: `git rm test_autocommit_temp.txt && git commit -m "manual: clean up autocommit test file"` then kill the autocommit background process

**Test 2 -- Holdout guard blocks (GIT-02):**
1. Run: `touch stages/04-backtest/p2_holdout/test_holdout.txt`
2. Run: `git add stages/04-backtest/p2_holdout/test_holdout.txt`
3. Run: `git commit -m "test: should be blocked"`
4. Confirm: commit is rejected with error mentioning "holdout" or "P2"
5. Clean up: `git reset HEAD stages/04-backtest/p2_holdout/test_holdout.txt && rm stages/04-backtest/p2_holdout/test_holdout.txt`

**Test 3 -- Commit log exists (GIT-03):**
1. Run: `cat .git/commit_log.txt | tail -3`
2. Confirm: recent commits are logged with timestamps and commit hashes
  </how-to-verify>
  <resume-signal>Type "approved" if all three tests pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` -- full suite green
- Manual verification of autocommit, holdout guard, and commit log per the checkpoint steps above
</verification>

<success_criteria>
All automated tests pass. Human confirms: autocommit fires within 30s, holdout guard blocks p2_holdout commits, commit log is written. Phase 3 git infrastructure is operational.
</success_criteria>

<output>
After completion, create `.planning/phases/03-git-infrastructure/03-02-SUMMARY.md`
</output>
